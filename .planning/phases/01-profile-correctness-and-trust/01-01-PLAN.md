---
phase: 01-profile-correctness-and-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings_FO4_CBBE.json
  - src/BS2BG.App/BS2BG.App.csproj
  - src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs
  - tests/BS2BG.Tests/TemplateProfileCatalogFactoryTests.cs
  - tests/BS2BG.Tests/TemplateGenerationServiceTests.cs
autonomous: true
requirements:
  - PROF-01
  - PROF-04
must_haves:
  truths:
    - "User can select `Fallout 4 CBBE` as a bundled profile backed by its own root-level JSON file (D-01, D-04)."
    - "Fallout 4 CBBE defaults, multipliers, and inverted-slider tables are not reused from Skyrim CBBE or Skyrim UUNP (D-02, D-13)."
    - "D-03: Existing Skyrim profile files stay at repository root in Phase 1; no `profiles/` folder migration occurs."
    - "Template generation continues through existing Core services without editing golden expected files (D-14)."
  artifacts:
    - path: "settings_FO4_CBBE.json"
      provides: "Distinct bundled Fallout 4 CBBE profile data"
      contains: "\"Inverted\": []"
    - path: "src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs"
      provides: "Runtime registration of all bundled profiles"
      contains: "settings_FO4_CBBE.json"
    - path: "src/BS2BG.App/BS2BG.App.csproj"
      provides: "Content-copy registration for the FO4 profile JSON"
      contains: "settings_FO4_CBBE.json"
    - path: "tests/BS2BG.Tests/TemplateProfileCatalogFactoryTests.cs"
      provides: "Proof that FO4 loads from distinct bundled data"
      contains: "Fallout4Cbbe"
  key_links:
    - from: "src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs"
      to: "settings_FO4_CBBE.json"
      via: "LoadRequiredProfile"
      pattern: "Fallout4Cbbe[\\s\\S]*settings_FO4_CBBE\\.json"
    - from: "src/BS2BG.App/BS2BG.App.csproj"
      to: "settings_FO4_CBBE.json"
      via: "Content Include"
      pattern: "Content Include=.*settings_FO4_CBBE\\.json"
---

<objective>
Create the distinct bundled Fallout 4 CBBE profile data path.

Purpose: Phase 1 cannot be trustworthy while `Fallout 4 CBBE` reuses Skyrim CBBE profile data. This plan makes FO4 a first-class bundled profile while keeping the Phase 1 root-file layout locked by D-01/D-03.
Output: A root-level `settings_FO4_CBBE.json`, app content-copy wiring, factory registration, and tests proving the FO4 profile is distinct.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
@.planning/phases/01-profile-correctness-and-trust/01-RESEARCH.md
@.planning/phases/01-profile-correctness-and-trust/01-VALIDATION.md
@PRD.md
@src/BS2BG.Core/Models/ProjectProfileMapping.cs
@src/BS2BG.Core/Generation/TemplateProfileCatalog.cs
@src/BS2BG.Core/Generation/SliderProfileJsonService.cs
@src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs
@src/BS2BG.App/BS2BG.App.csproj
@tests/BS2BG.Tests/TemplateGenerationServiceTests.cs

<interfaces>
From `src/BS2BG.Core/Models/ProjectProfileMapping.cs`:
```csharp
public const string SkyrimCbbe = "Skyrim CBBE";
public const string SkyrimUunp = "Skyrim UUNP";
public const string Fallout4Cbbe = "Fallout 4 CBBE";
public static string Resolve(string? profileName, bool isUunp);
public static string FromLegacyIsUunp(bool isUunp);
public static bool ToLegacyIsUunp(string? profileName);
```

From `src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs`:
```csharp
public static TemplateProfileCatalog CreateDefault();
public static TemplateProfileCatalog CreateDefault(IEnumerable<string> searchDirectories);
```

From `src/BS2BG.Core/Generation/SliderProfileJsonService.cs`:
```csharp
public static SliderProfile Load(string path);
public static SliderProfile LoadFromString(string json);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove FO4 CBBE loads from a distinct bundled profile</name>
  <files>tests/BS2BG.Tests/TemplateProfileCatalogFactoryTests.cs, tests/BS2BG.Tests/TemplateGenerationServiceTests.cs</files>
  <read_first>
    - tests/BS2BG.Tests/TemplateGenerationServiceTests.cs
    - tests/BS2BG.Tests/ProjectFileServiceTests.cs
    - src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs
    - src/BS2BG.Core/Models/ProjectProfileMapping.cs
    - .planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `TemplateProfileCatalogFactory.CreateDefault()` exposes exactly the bundled display names `Skyrim CBBE`, `Skyrim UUNP`, and `Fallout 4 CBBE` in that order per D-04 and UI-SPEC.
    - Test 2: `Fallout 4 CBBE` has FO4-only defaults such as `BreastCenterBig`, `ButtNew`, `ShoulderTweak`, `HipBack`, and `ChubbyWaist`, proving it does not reuse Skyrim CBBE or Skyrim UUNP default tables per D-02/D-13.
    - Test 3: `Fallout 4 CBBE` has an empty inverted list and explicit multiplier behavior of `1.0` for seeded FO4 sliders per D-02.
    - Test 4: Existing `GenerateTemplatesUsesDefaultFallout4ProfileSettings` continues to pass using the factory-registered `Fallout 4 CBBE` profile without changing `tests/fixtures/expected/**` per D-14.
  </behavior>
  <action>Create `tests/BS2BG.Tests/TemplateProfileCatalogFactoryTests.cs` using xUnit v3 and FluentAssertions. Use temporary directory helpers where needed, but also include a direct `TemplateProfileCatalogFactory.CreateDefault()` test for repository output content. Do not use bare `Assert.*`. Do not edit `tests/fixtures/expected/**`. Extend `TemplateGenerationServiceTests` only if needed to assert the existing `GenerateTemplatesUsesDefaultFallout4ProfileSettings` scenario still targets `ProjectProfileMapping.Fallout4Cbbe` through the default catalog.</action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~TemplateProfileCatalogFactoryTests|FullyQualifiedName~TemplateGenerationServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BS2BG.Tests/TemplateProfileCatalogFactoryTests.cs` contains `ProjectProfileMapping.Fallout4Cbbe`.
    - `tests/BS2BG.Tests/TemplateProfileCatalogFactoryTests.cs` contains at least one FO4-only slider assertion for `BreastCenterBig` and `ButtNew`.
    - `tests/BS2BG.Tests/TemplateProfileCatalogFactoryTests.cs` contains an assertion that the FO4 inverted list is empty.
    - The automated test command above exits 0 after Task 2 implementation.
  </acceptance_criteria>
  <done>Tests fail before implementation because the factory still loads `settings.json` for FO4, then pass after Task 2 with no expected golden-file edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add root-level FO4 profile JSON and app catalog wiring</name>
  <files>settings_FO4_CBBE.json, src/BS2BG.App/BS2BG.App.csproj, src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs</files>
  <read_first>
    - settings.json
    - settings_UUNP.json
    - src/BS2BG.App/BS2BG.App.csproj
    - src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs
    - PRD.md
    - .planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
  </read_first>
  <behavior>
    - `settings_FO4_CBBE.json` exists at repository root beside `settings.json` and `settings_UUNP.json` per D-01/D-03.
    - FO4 defaults include the PRD §7.7 seed slider names with `valueSmall: 1.0` and `valueBig: 1.0` per D-02.
    - FO4 multipliers contain the same seeded slider names with value `1.0` per D-02.
    - FO4 inverted list is `[]` per D-02.
    - `TemplateProfileCatalogFactory` loads `ProjectProfileMapping.Fallout4Cbbe` from `settings_FO4_CBBE.json`, not `settings.json`.
  </behavior>
  <action>Add `settings_FO4_CBBE.json` at the repository root. Use the unchanged profile schema: top-level `Defaults`, `Multipliers`, and `Inverted`. Seed at least these FO4 CBBE slider names from PRD §7.7 with both defaults set to `1.0` and multipliers set to `1.0`: `Ankles`, `AppleCheeks`, `Arms`, `BackArch`, `Belly`, `BreastCenter`, `BreastCenterBig`, `BreastGravity`, `BreastGravity2`, `BreastHeight`, `BreastTopSlope`, `Breasts`, `BreastsTogether`, `Butt`, `ButtNew`, `CalfSize`, `ChubbyWaist`, `CrotchBack`, `HipBack`, `LegShape`, `NippleDown`, `NipplePerkiness`, `ShoulderTweak`, `ShoulderWidth`, `Thighs`, `Waist`, `WaistHeight`, `BreastsCleavage`, `NipBGone`, `PushUp`, and `BreastFlatness2`. Set `Inverted` to an empty array. Add a `Content Include="..\..\settings_FO4_CBBE.json" Link="settings_FO4_CBBE.json" CopyToOutputDirectory="PreserveNewest" CopyToPublishDirectory="PreserveNewest"` entry to `src/BS2BG.App/BS2BG.App.csproj`. Update `TemplateProfileCatalogFactory.CreateDefault` so `ProjectProfileMapping.Fallout4Cbbe` calls `LoadRequiredProfile("settings_FO4_CBBE.json", directories)`. Do not move existing Skyrim files into a folder per D-03.</action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~TemplateProfileCatalogFactoryTests|FullyQualifiedName~TemplateGenerationServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `settings_FO4_CBBE.json` contains `"BreastCenterBig"`, `"ButtNew"`, `"ShoulderTweak"`, and `"ChubbyWaist"`.
    - `settings_FO4_CBBE.json` contains `"Inverted": []` or an equivalent empty JSON array.
    - `src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs` contains `LoadRequiredProfile("settings_FO4_CBBE.json"`.
    - `src/BS2BG.App/BS2BG.App.csproj` contains `settings_FO4_CBBE.json`.
    - The automated test command exits 0.
  </acceptance_criteria>
  <done>`Fallout 4 CBBE` is always available as a bundled profile backed by distinct root-level FO4 JSON data; Skyrim profile files remain in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local profile JSON -> Core catalog | Bundled JSON files are loaded from the app base directory and influence generation math. |
| Catalog -> generation output | Profile defaults/multipliers/inversions affect generated BodyGen/BoS text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `settings_FO4_CBBE.json` | mitigate | Register the exact content file in `BS2BG.App.csproj`; factory tests assert FO4-only slider data and empty inverted list so accidental Skyrim reuse is caught. |
| T-01-02 | Information Disclosure | Profile catalog | accept | Profile JSON contains no secrets or PII; it is bundled deterministic slider metadata. |
| T-01-03 | Denial of Service | `TemplateProfileCatalogFactory.CreateDefault` | accept | Existing required-profile behavior throws if a bundled file is missing; this is acceptable startup/config failure for a local desktop app. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~TemplateProfileCatalogFactoryTests|FullyQualifiedName~TemplateGenerationServiceTests"` after both tasks. Before phase completion, run `dotnet test`.
</verification>

<success_criteria>
- FO4 CBBE profile data is a distinct root-level JSON file.
- Factory registration uses `settings_FO4_CBBE.json` for `ProjectProfileMapping.Fallout4Cbbe`.
- Tests prove FO4 tables are not reused from Skyrim CBBE/UUNP and that no sacred golden expected files changed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-profile-correctness-and-trust/01-01-SUMMARY.md`.
</output>
