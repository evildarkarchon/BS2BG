---
phase: 01-profile-correctness-and-trust
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - src/BS2BG.App/ViewModels/TemplatesViewModel.cs
  - tests/BS2BG.Tests/TemplatesViewModelTests.cs
autonomous: true
requirements:
  - PROF-02
  - PROF-03
  - PROF-04
must_haves:
  truths:
    - "Imported BodySlide XML presets use the currently selected profile and do not infer profile from file path, game folder, or slider names (D-09, D-10)."
    - "A selected preset with an unbundled saved profile keeps the original profile name until the user explicitly chooses a bundled profile (D-07, D-12)."
    - "Unresolved profile fallback is visible as neutral information, not a warning, and does not block preview or generation (D-05, D-07, D-08, D-16)."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/TemplatesViewModel.cs"
      provides: "Selected-profile import and neutral fallback ViewModel state"
      contains: "ProfileFallbackInformationText"
    - path: "tests/BS2BG.Tests/TemplatesViewModelTests.cs"
      provides: "ViewModel proof for import selection, unresolved profile preservation, and no mismatch warnings"
      contains: "Saved profile \"Community CBBE\" is not bundled"
  key_links:
    - from: "TemplatesViewModel.SelectedPreset"
      to: "TemplateProfileCatalog.ContainsProfile"
      via: "fallback visibility calculation"
      pattern: "ContainsProfile\(.*ProfileName"
    - from: "TemplatesViewModel.ImportPresetFilesCoreAsync"
      to: "SliderPreset.ProfileName"
      via: "SelectedProfileName assignment"
      pattern: "preset\.ProfileName = SelectedProfileName"
---

<objective>
Implement profile-selection and unresolved-profile fallback behavior in the Templates ViewModel.

Purpose: Users need profile-specific generation semantics without noisy mismatch heuristics. This plan keeps user-selected profile assignment, preserves unresolved saved profile names, and exposes the exact neutral fallback message required by the UI contract.
Output: `TemplatesViewModel` properties/logic and ViewModel tests for selected-profile import, fallback info, and absence of mismatch warnings.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
@.planning/phases/01-profile-correctness-and-trust/01-RESEARCH.md
@.planning/phases/01-profile-correctness-and-trust/01-UI-SPEC.md
@openspec/specs/template-generation-flow/spec.md
@openspec/specs/reactive-mvvm-conventions/spec.md
@src/BS2BG.App/ViewModels/TemplatesViewModel.cs
@tests/BS2BG.Tests/TemplatesViewModelTests.cs
@src/BS2BG.Core/Generation/TemplateProfileCatalog.cs

<interfaces>
From Plan 02 `TemplateProfileCatalog`:
```csharp
public IReadOnlyList<string> ProfileNames { get; }
public TemplateProfile DefaultProfile { get; }
public TemplateProfile GetProfile(string? name);
public bool ContainsProfile(string? name);
```

Existing `TemplatesViewModel` members to preserve:
```csharp
public IReadOnlyList<string> ProfileNames => profileCatalog.ProfileNames;
public string SelectedProfileName { get; set; }
public SliderPreset? SelectedPreset { get; set; }
public string PreviewTemplateText { get; }
public string GeneratedTemplateText { get; }
public string StatusMessage { get; }
public async Task ImportPresetFilesAsync(IReadOnlyList<string> files, CancellationToken cancellationToken = default);
public void GenerateTemplates();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ViewModel tests for selected-profile import and neutral fallback</name>
  <files>tests/BS2BG.Tests/TemplatesViewModelTests.cs</files>
  <read_first>
    - tests/BS2BG.Tests/TemplatesViewModelTests.cs
    - src/BS2BG.App/ViewModels/TemplatesViewModel.cs
    - .planning/phases/01-profile-correctness-and-trust/01-UI-SPEC.md
    - .planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Imported XML presets receive the currently selected `Fallout 4 CBBE` profile and no path/slider-name inference runs per D-09/D-10.
    - Test 2: Selecting a preset whose `ProfileName` is `Community CBBE` exposes `IsProfileFallbackInformationVisible == true` and exact text `Saved profile "Community CBBE" is not bundled. BS2BG is using Skyrim CBBE calculation rules for preview and generation until you choose a bundled profile.` per D-07/D-12/UI-SPEC.
    - Test 3: Generating templates for an unresolved saved profile succeeds, uses fallback calculation rules, leaves `SelectedPreset.ProfileName == "Community CBBE"`, and does not put warning/mismatch/experimental language into `StatusMessage`, `ValidationMessage`, or fallback text per D-05/D-06/D-08/D-16.
    - Test 4: When the user explicitly selects `Skyrim UUNP`, the selected preset `ProfileName` becomes `Skyrim UUNP` and fallback info hides per D-12.
  </behavior>
  <action>Add tests to `TemplatesViewModelTests.cs` using existing `CreateViewModel`, `CreateCatalog`, and temporary XML helper patterns. If the existing `CreateCatalog` lacks `Fallout 4 CBBE`, extend the helper to include `ProjectProfileMapping.Fallout4Cbbe` with a distinctive `SliderProfile` that has a default or multiplier only FO4 tests can observe. Use exact string assertions for the fallback copy from UI-SPEC line 97. Add negative assertions that no tested message contains `warning`, `mismatch`, or `experimental` (case-insensitive) for custom/unprofiled slider names per D-05/D-06/D-10/D-16.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~TemplatesViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BS2BG.Tests/TemplatesViewModelTests.cs` contains `ProjectProfileMapping.Fallout4Cbbe` in an import-selected-profile test.
    - `tests/BS2BG.Tests/TemplatesViewModelTests.cs` contains exact fallback text beginning `Saved profile "Community CBBE" is not bundled.`
    - `tests/BS2BG.Tests/TemplatesViewModelTests.cs` contains case-insensitive negative assertions for `warning`, `mismatch`, and `experimental`.
    - The automated test command exits 0 after Task 2 implementation.
  </acceptance_criteria>
  <done>ViewModel tests capture all D-05 through D-12 and D-16 behavior without relying on manual UI inspection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement neutral fallback state without overwriting saved profile names</name>
  <files>src/BS2BG.App/ViewModels/TemplatesViewModel.cs</files>
  <read_first>
    - src/BS2BG.App/ViewModels/TemplatesViewModel.cs
    - src/BS2BG.Core/Generation/TemplateProfileCatalog.cs
    - src/BS2BG.Core/Models/SliderPreset.cs
    - .planning/phases/01-profile-correctness-and-trust/01-UI-SPEC.md
    - .planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
  </read_first>
  <behavior>
    - Add `[Reactive(SetModifier = AccessModifier.Private)] private string _profileFallbackInformationText = string.Empty;` and `[ObservableAsProperty]` or `[Reactive(SetModifier = AccessModifier.Private)] private bool _isProfileFallbackInformationVisible;` following existing source-generator patterns.
    - When selected preset profile is bundled, `SelectedProfileName` equals that bundled profile, fallback text is empty, and fallback visibility is false.
    - When selected preset profile is unbundled, `SelectedProfileName` is set to `profileCatalog.GetProfile(savedName).Name` for calculation/selector fallback, `SelectedPreset.ProfileName` remains the unbundled saved name, and fallback text uses the exact UI-SPEC sentence.
    - When the user changes the profile selector while not syncing from a selected preset, set `SelectedPreset.ProfileName` to the selected bundled profile and hide fallback information.
  </behavior>
  <action>Modify `TemplatesViewModel` only. Add public read-only properties generated from private reactive fields: `ProfileFallbackInformationText` and `IsProfileFallbackInformationVisible`. Add a private helper such as `RefreshProfileFallbackInformation()` that checks `SelectedPreset?.ProfileName`, calls `profileCatalog.ContainsProfile(savedName)`, resolves `var fallbackName = profileCatalog.GetProfile(savedName).Name`, and sets exact copy `Saved profile "{savedName}" is not bundled. BS2BG is using {fallbackName} calculation rules for preview and generation until you choose a bundled profile.` only when the saved name is nonblank and unbundled. Update `SetSelectedProfileNameFromPreset` so unresolved saved names do not assign the raw unbundled name to the ComboBox selection; assign the fallback bundled profile name while `syncingProfileFromPreset` is true. Update `OnSelectedProfileNameChangedReactive` so explicit user selection writes the bundled `resolvedName` to `SelectedPreset.ProfileName` only when `syncingProfileFromPreset` is false. Call `RefreshProfileFallbackInformation()` after selected preset changes, profile changes, and selected preset `ProfileName` changes. Do not add warnings, modal dialogs, profile mismatch heuristics, path inference, slider-name inference, or FO4 experimental labels.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~TemplatesViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `src/BS2BG.App/ViewModels/TemplatesViewModel.cs` contains `ProfileFallbackInformationText`.
    - `src/BS2BG.App/ViewModels/TemplatesViewModel.cs` contains `IsProfileFallbackInformationVisible`.
    - `src/BS2BG.App/ViewModels/TemplatesViewModel.cs` contains the exact phrase `is not bundled. BS2BG is using`.
    - `src/BS2BG.App/ViewModels/TemplatesViewModel.cs` does not contain new strings `mismatch` or `experimental`.
    - The automated test command exits 0.
  </acceptance_criteria>
  <done>Unresolved saved profiles remain round-trip-safe, preview/generation use visible fallback calculation rules, and custom body-mod sliders are not treated as warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-selected XML files -> `TemplatesViewModel` | Imported presets and slider names are untrusted user input. |
| Project profile string -> ViewModel display | Saved profile names may be unknown or arbitrary text from `.jbs2bg` files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | `TemplatesViewModel.ImportPresetFilesCoreAsync` | mitigate | Tests assert imports use the current selected profile only; no file-path or slider-name inference can rewrite profile semantics. |
| T-01-08 | Spoofing | Fallback profile display | mitigate | Use exact saved profile name in neutral text and exact fallback bundled profile name from `TemplateProfileCatalog.GetProfile`. |
| T-01-09 | Denial of Service | Unprofiled/custom sliders | accept | Unknown slider names must not block generation or show mismatch warnings per D-05/D-10/D-16. |
</threat_model>

<verification>
Run `dotnet test --filter FullyQualifiedName~TemplatesViewModelTests`. Before phase completion, run `dotnet test`.
</verification>

<success_criteria>
- Imported presets use the selected profile.
- Unbundled saved profile names are preserved until explicit user selection.
- Neutral fallback information appears exactly when fallback math would otherwise be silent.
- No mismatch/experimental warning behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-profile-correctness-and-trust/01-03-SUMMARY.md`.
</output>
