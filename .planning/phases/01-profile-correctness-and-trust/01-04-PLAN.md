---
phase: 01-profile-correctness-and-trust
plan: 04
type: execute
wave: 3
depends_on:
  - 03
files_modified:
  - src/BS2BG.App/Themes/ThemeResources.axaml
  - src/BS2BG.App/Views/MainWindow.axaml
  - tests/BS2BG.Tests/AppShellTests.cs
  - docs/release/README.md
autonomous: true
requirements:
  - PROF-03
  - PROF-05
must_haves:
  truths:
    - "User can see neutral unresolved-profile fallback information directly below the Templates profile toolbar (D-07, D-12, D-15)."
    - "The main workflow, selector, fallback panel, and status text do not label Fallout 4 CBBE as experimental and do not add mismatch warnings (D-05, D-06, D-08, D-10, D-16)."
    - "Users can find FO4 calibration-confidence context outside the main workflow without changing in-app semantics (PROF-05 constrained by D-06/D-08)."
  artifacts:
    - path: "src/BS2BG.App/Themes/ThemeResources.axaml"
      provides: "Neutral informational brushes separate from warning brushes"
      contains: "BS2BGInfoBackgroundBrush"
    - path: "src/BS2BG.App/Views/MainWindow.axaml"
      provides: "Neutral fallback information panel"
      contains: "ProfileFallbackInformationPanel"
    - path: "tests/BS2BG.Tests/AppShellTests.cs"
      provides: "Headless UI proof of panel placement/resources and no experimental label"
      contains: "Profile fallback information"
    - path: "docs/release/README.md"
      provides: "Release-facing profile note outside the main workflow"
      contains: "Fallout 4 CBBE profile"
  key_links:
    - from: "MainWindow.axaml"
      to: "TemplatesViewModel.IsProfileFallbackInformationVisible"
      via: "IsVisible binding"
      pattern: "IsProfileFallbackInformationVisible"
    - from: "MainWindow.axaml"
      to: "TemplatesViewModel.ProfileFallbackInformationText"
      via: "Text binding"
      pattern: "ProfileFallbackInformationText"
---

<objective>
Expose the neutral unresolved-profile fallback UI and release-facing FO4 profile context.

Purpose: The ViewModel state from Plan 03 must be visible in the approved UI without warning styling, while `PROF-05` must be handled outside the main workflow because D-06/D-08 forbid in-app FO4 experimental labels.
Output: Theme resources, AXAML fallback panel, headless UI assertions, and a concise release-doc profile note.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
@.planning/phases/01-profile-correctness-and-trust/01-RESEARCH.md
@.planning/phases/01-profile-correctness-and-trust/01-UI-SPEC.md
@src/BS2BG.App/Themes/ThemeResources.axaml
@src/BS2BG.App/Views/MainWindow.axaml
@tests/BS2BG.Tests/AppShellTests.cs
@docs/release/README.md

<interfaces>
From Plan 03 `TemplatesViewModel`:
```csharp
public string ProfileFallbackInformationText { get; }
public bool IsProfileFallbackInformationVisible { get; }
public IReadOnlyList<string> ProfileNames { get; }
public string SelectedProfileName { get; set; }
```

Approved UI-SPEC exact copy:
```text
Saved profile "{SavedProfileName}" is not bundled. BS2BG is using {FallbackProfileName} calculation rules for preview and generation until you choose a bundled profile.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add headless UI tests for fallback panel and no FO4 experimental label</name>
  <files>tests/BS2BG.Tests/AppShellTests.cs</files>
  <read_first>
    - tests/BS2BG.Tests/AppShellTests.cs
    - src/BS2BG.App/Views/MainWindow.axaml
    - src/BS2BG.App/Themes/ThemeResources.axaml
    - .planning/phases/01-profile-correctness-and-trust/01-UI-SPEC.md
    - .planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `MainWindow` contains a `Border` named `ProfileFallbackInformationPanel` with automation name `Profile fallback information`.
    - Test 2: `MainWindow` contains exact profile option text source through the ViewModel profile names: `Skyrim CBBE`, `Skyrim UUNP`, `Fallout 4 CBBE`; no profile name contains `experimental`.
    - Test 3: MainWindow logical text/menu/control content does not contain `mismatch` or `experimental` for Phase 1 main workflow per D-05/D-06/D-08/D-10.
    - Test 4: Theme resources contain `BS2BGInfoBackgroundBrush`, `BS2BGInfoBorderBrush`, and `BS2BGInfoForegroundBrush`, distinct from existing `BS2BGWarning*` resources.
  </behavior>
  <action>Add tests to `AppShellTests.cs` using existing `[AvaloniaFact]` and service-provider patterns. Use `window.FindControl<Border>("ProfileFallbackInformationPanel")` for the panel and inspect `window.ViewModel.Templates.ProfileNames` for exact bundled display names. When checking absence of forbidden copy, aggregate logical descendant `TextBlock.Text`, `Button.Content`, `MenuItem.Header`, and `ComboBox` item strings where available; assert case-insensitively that `experimental` and `mismatch` are absent from the main workflow. Do not assert against release docs in this UI test because `PROF-05` docs are outside the app.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~AppShellTests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BS2BG.Tests/AppShellTests.cs` contains `ProfileFallbackInformationPanel`.
    - `tests/BS2BG.Tests/AppShellTests.cs` contains `Fallout 4 CBBE` and an assertion excluding `experimental`.
    - `tests/BS2BG.Tests/AppShellTests.cs` contains `BS2BGInfoBackgroundBrush`.
    - The automated test command exits 0 after Task 2 implementation.
  </acceptance_criteria>
  <done>UI tests define the approved fallback surface and prevent accidental warning/experimental labels in the main workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Add neutral fallback panel, info brushes, and release profile note</name>
  <files>src/BS2BG.App/Themes/ThemeResources.axaml, src/BS2BG.App/Views/MainWindow.axaml, docs/release/README.md</files>
  <read_first>
    - src/BS2BG.App/Themes/ThemeResources.axaml
    - src/BS2BG.App/Views/MainWindow.axaml
    - docs/release/README.md
    - .planning/phases/01-profile-correctness-and-trust/01-UI-SPEC.md
    - .planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
  </read_first>
  <action>Add neutral information brushes to `ThemeResources.axaml` using the UI-SPEC values, not existing warning brushes: Light `BS2BGInfoBackgroundBrush #EEF6FF`, `BS2BGInfoBorderBrush #8CBFE8`, `BS2BGInfoForegroundBrush #17324D`; Dark `BS2BGInfoBackgroundBrush #102A43`, `BS2BGInfoBorderBrush #4E8DBD`, `BS2BGInfoForegroundBrush #D8ECFF`. In `MainWindow.axaml`, change the Templates main content grid so the profile toolbar remains first, then add a `Border x:Name="ProfileFallbackInformationPanel"` immediately below it and above `TemplatePreviewTextBox`, with `Padding="8"`, `Margin="0,0,0,8"`, `Background`/`BorderBrush` bound to the new info brushes, `BorderThickness="1"`, `IsVisible="{Binding Templates.IsProfileFallbackInformationVisible}"`, `automation:AutomationProperties.Name="Profile fallback information"`, and a child `TextBlock Text="{Binding Templates.ProfileFallbackInformationText}" TextWrapping="Wrap" Foreground="{DynamicResource BS2BGInfoForegroundBrush}"`. Preserve compiled-binding validity. Do not use warning words, warning iconography, `BS2BGWarning*` brushes, modal dialogs, or confirmation prompts. Update `docs/release/README.md` with a concise `Fallout 4 CBBE profile` note stating that the bundled FO4 CBBE profile uses a distinct FO4 slider seed with `1.0` defaults/multipliers and no inverted sliders until authoritative calibration data is validated; keep this note outside the in-app main workflow per D-06/D-08.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~AppShellTests</automated>
  </verify>
  <acceptance_criteria>
    - `src/BS2BG.App/Themes/ThemeResources.axaml` contains `BS2BGInfoBackgroundBrush`, `BS2BGInfoBorderBrush`, and `BS2BGInfoForegroundBrush` in both Light and Dark dictionaries.
    - `src/BS2BG.App/Views/MainWindow.axaml` contains `x:Name="ProfileFallbackInformationPanel"`.
    - `src/BS2BG.App/Views/MainWindow.axaml` contains `Templates.IsProfileFallbackInformationVisible` and `Templates.ProfileFallbackInformationText`.
    - `src/BS2BG.App/Views/MainWindow.axaml` does not contain new `experimental` or `mismatch` strings.
    - `docs/release/README.md` contains `Fallout 4 CBBE profile`.
    - The automated test command exits 0.
  </acceptance_criteria>
  <done>Fallback information is visible, neutral, accessible, and test-covered; FO4 profile confidence context exists outside the main app workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewModel profile state -> AXAML display | Saved profile names and fallback text are displayed to users. |
| Release documentation -> user expectations | Documentation explains FO4 profile calibration state without changing app behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Spoofing | `ProfileFallbackInformationPanel` | mitigate | Display exact saved profile and exact fallback profile name from ViewModel text; do not imply the saved profile is bundled. |
| T-01-11 | Information Disclosure | Fallback panel | accept | Displayed data is project profile name already visible to the local user; no secrets are involved. |
| T-01-12 | Repudiation | Release profile note | mitigate | Document FO4 profile seed assumptions outside the app so `PROF-05` is covered while D-06/D-08 prevent in-app experimental labels. |
</threat_model>

<verification>
Run `dotnet test --filter FullyQualifiedName~AppShellTests`. Before phase completion, run `dotnet test`.
</verification>

<success_criteria>
- Neutral fallback panel appears in the Templates workflow and binds to Plan 03 ViewModel state.
- App UI has no FO4 experimental/mismatch warning labels.
- Release-facing docs explain the FO4 profile seed/calibration status outside the main workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/01-profile-correctness-and-trust/01-04-SUMMARY.md`.
</output>
