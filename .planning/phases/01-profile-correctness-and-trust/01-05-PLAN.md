---
phase: 01-profile-correctness-and-trust
plan: 05
type: execute
wave: 4
depends_on:
  - 03
  - 04
files_modified:
  - src/BS2BG.App/ViewModels/TemplatesViewModel.cs
  - tests/BS2BG.Tests/TemplatesViewModelTests.cs
autonomous: true
gap_closure: true
requirements:
  - PROF-02
  - PROF-03
  - PROF-04
must_haves:
  truths:
    - "A selected preset with an unbundled saved profile keeps the original profile name until the user explicitly chooses a bundled profile (D-07, D-12)."
    - "The profile selector does not silently adopt the fallback profile just because fallback math is being used (D-07, D-12)."
    - "Preview, missing-default rows, and selected BoS JSON still use the fallback calculation profile while the saved profile remains unbundled (D-12)."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/TemplatesViewModel.cs"
      provides: "Separated saved-profile fallback calculation from explicit bundled selector adoption"
      contains: "GetSelectedCalculationProfile"
    - path: "tests/BS2BG.Tests/TemplatesViewModelTests.cs"
      provides: "Regression coverage for fallback-profile adoption path"
      contains: "ChoosingDisplayedFallbackProfileAdoptsBundledProfile"
  key_links:
    - from: "TemplatesViewModel.SelectedPreset.ProfileName"
      to: "TemplateProfileCatalog.GetProfile"
      via: "calculation helper for unresolved saved profiles"
      pattern: "GetSelectedCalculationProfile"
    - from: "TemplatesViewModel.SelectedProfileName"
      to: "SliderPreset.ProfileName"
      via: "OnSelectedProfileNameChangedReactive only after user-selected bundled value"
      pattern: "SelectedPreset.ProfileName = resolvedName"
---

<objective>
Close the verified fallback-adoption behavior gap without adding warning or mismatch UX.

Purpose: Verification confirmed that preselecting the fallback profile prevents users from adopting that same bundled profile. This plan keeps D-07/D-12 round-trip preservation while making explicit bundled selection the only adoption path.
Output: `TemplatesViewModel` separates effective calculation profile from selector adoption, with focused ViewModel regression tests.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
@.planning/phases/01-profile-correctness-and-trust/01-RESEARCH.md
@.planning/phases/01-profile-correctness-and-trust/01-VERIFICATION.md
@.planning/phases/01-profile-correctness-and-trust/01-REVIEW.md
@src/BS2BG.App/ViewModels/TemplatesViewModel.cs
@tests/BS2BG.Tests/TemplatesViewModelTests.cs

<interfaces>
From `TemplatesViewModel` current contract:
```csharp
public IReadOnlyList<string> ProfileNames { get; }
public string SelectedProfileName { get; set; }
public SliderPreset? SelectedPreset { get; set; }
public string ProfileFallbackInformationText { get; }
public bool IsProfileFallbackInformationVisible { get; }
public void GenerateTemplates();
```

Current unresolved-profile problem from verification/review:
```text
SetSelectedProfileNameFromPreset preselects the fallback profile, so choosing the same ComboBox item again cannot update SelectedPreset.ProfileName.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for explicit fallback-profile adoption</name>
  <files>tests/BS2BG.Tests/TemplatesViewModelTests.cs</files>
  <read_first>
    - tests/BS2BG.Tests/TemplatesViewModelTests.cs
    - src/BS2BG.App/ViewModels/TemplatesViewModel.cs
    - .planning/phases/01-profile-correctness-and-trust/01-VERIFICATION.md
    - .planning/phases/01-profile-correctness-and-trust/01-REVIEW.md
    - .planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Selecting a preset with `ProfileName == "Community CBBE"` leaves `SelectedProfileName` empty (no bundled selector adoption yet) while `IsProfileFallbackInformationVisible == true`.
    - Test 2: The unresolved preset preview and selected BoS JSON still use the default fallback calculation profile while `preset.ProfileName` remains `Community CBBE`.
    - Test 3: Setting `SelectedProfileName = ProjectProfileMapping.SkyrimCbbe` after unresolved selection changes `preset.ProfileName` to `Skyrim CBBE`, hides fallback information, and refreshes preview/BoS JSON.
    - Test 4: Existing explicit non-default bundled profile adoption (`Skyrim UUNP`) still overwrites unbundled saved profile and hides fallback information.
  </behavior>
  <action>Add focused tests near the existing fallback tests. Use the existing `CreateViewModel()` helper and `ModelSliderPreset`/`ModelSetSlider` patterns. Name the main regression test `ChoosingDisplayedFallbackProfileAdoptsBundledProfile`. Keep negative language assertions for `warning`, `mismatch`, and `experimental`; this gap must not be closed by adding warning copy because D-05 through D-08 forbid that. The RED run should fail against current code because `SelectedProfileName` is preselected to `Skyrim CBBE` and setting it to `Skyrim CBBE` again does not update `preset.ProfileName`.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~TemplatesViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BS2BG.Tests/TemplatesViewModelTests.cs` contains `ChoosingDisplayedFallbackProfileAdoptsBundledProfile`.
    - The test file contains assertions for `Community CBBE`, `ProjectProfileMapping.SkyrimCbbe`, `IsProfileFallbackInformationVisible`, and `ProfileFallbackInformationText`.
    - The automated test command fails before Task 2 and exits 0 after Task 2.
  </acceptance_criteria>
  <done>Tests pin the intended adoption behavior and prove the verified WR-01 gap before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Separate fallback calculation from selector adoption</name>
  <files>src/BS2BG.App/ViewModels/TemplatesViewModel.cs</files>
  <read_first>
    - src/BS2BG.App/ViewModels/TemplatesViewModel.cs
    - tests/BS2BG.Tests/TemplatesViewModelTests.cs
    - .planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
  </read_first>
  <action>Implement the review-recommended separation without changing Core catalog fallback semantics. Add a private helper named `GetSelectedCalculationProfile()` that returns `profileCatalog.GetProfile(SelectedPreset.ProfileName)` when `SelectedPreset` exists and its saved `ProfileName` is unbundled, otherwise returns `profileCatalog.GetProfile(SelectedProfileName)`. Update `RefreshPreview`, `RefreshSelectedBosJson`, `RebuildSetSliderRows`, and missing-default refresh call sites so unresolved presets calculate from `GetSelectedCalculationProfile().Name`/profile while preserving `SelectedPreset.ProfileName`. Change `SetSelectedProfileNameFromPreset` so bundled saved names set `SelectedProfileName` to the bundled name, but unbundled saved names set `SelectedProfileName = string.Empty` while fallback information remains visible. Keep `OnSelectedProfileNameChangedReactive` as the explicit adoption path: when the user sets `SelectedProfileName` to a bundled name and `syncingProfileFromPreset` is false, write `SelectedPreset.ProfileName = resolvedName`, refresh missing defaults, fallback info, previews, and selected BoS JSON. Do not add modal warnings, mismatch heuristics, FO4 experimental labels, or generation/export blocking behavior per D-05 through D-08.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~TemplatesViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `src/BS2BG.App/ViewModels/TemplatesViewModel.cs` contains `GetSelectedCalculationProfile`.
    - `SetSelectedProfileNameFromPreset` contains a branch that assigns `SelectedProfileName = string.Empty` for unbundled saved profile names.
    - `TemplatesViewModel.cs` still contains `SelectedPreset.ProfileName = resolvedName` inside `OnSelectedProfileNameChangedReactive`.
    - `TemplatesViewModel.cs` does not contain new `mismatch` or `experimental` strings.
    - The automated test command exits 0.
  </acceptance_criteria>
  <done>Unresolved saved profiles calculate through fallback rules without silently adopting them; choosing the fallback bundled profile explicitly now clears the fallback state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Saved project profile -> UI selector | Unbundled profile names loaded from project files influence selector state and preview calculation. |
| UI selector -> project model | User profile selection mutates `SliderPreset.ProfileName` and project semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Tampering | `TemplatesViewModel.SetSelectedProfileNameFromPreset` | mitigate | Preserve unbundled saved profile names as inert strings until a user-selected bundled value is assigned. |
| T-01-14 | Repudiation | Profile selector adoption path | mitigate | Tests prove fallback adoption requires explicit `SelectedProfileName` assignment and no silent rewrite occurs on selection sync. |
| T-01-15 | Information Disclosure | Fallback information text | accept | The displayed profile name is local project data already visible to the user; no secrets are introduced. |
</threat_model>

<verification>
Run `dotnet test --filter FullyQualifiedName~TemplatesViewModelTests`, then run `dotnet test` before marking the gap closure complete.
</verification>

<success_criteria>
- The adoption-path gap from `01-VERIFICATION.md` truth 9 is closed.
- Unbundled profile names remain preserved until explicit bundled selection.
- Neutral fallback information remains non-blocking and warning-free.
</success_criteria>

<output>
After completion, create `.planning/phases/01-profile-correctness-and-trust/01-05-SUMMARY.md`.
</output>
