---
phase: 01-profile-correctness-and-trust
plan: 06
type: execute
wave: 4
depends_on:
  - 01
files_modified:
  - tests/BS2BG.Tests/SliderMathFormatterTests.cs
  - tests/BS2BG.Tests/ExportWriterTests.cs
  - tests/BS2BG.Tests/MorphCoreTests.cs
autonomous: true
gap_closure: true
requirements:
  - PROF-01
  - PROF-04
must_haves:
  truths:
    - "BoS JSON generation is protected by profile-specific tests for Skyrim CBBE, Skyrim UUNP, and Fallout 4 CBBE."
    - "BodyGen export coverage proves templates use profile-specific generation while morph output remains profile-independent by design."
    - "No sacred golden expected files under tests/fixtures/expected/** are modified (D-14)."
  artifacts:
    - path: "tests/BS2BG.Tests/SliderMathFormatterTests.cs"
      provides: "Profile-specific BoS JSON formatter coverage"
      contains: "BosJsonUsesBundledProfileSpecificSliderTables"
    - path: "tests/BS2BG.Tests/ExportWriterTests.cs"
      provides: "BodyGen export integration coverage for profile-specific templates plus morph output"
      contains: "BodyGenExportCombinesProfileSpecificTemplatesWithProfileIndependentMorphs"
    - path: "tests/BS2BG.Tests/MorphCoreTests.cs"
      provides: "Explicit morph profile-independence coverage"
      contains: "MorphGenerationDoesNotDependOnPresetProfileNames"
  key_links:
    - from: "BosJsonExportWriter.Write"
      to: "TemplateProfileCatalog.GetProfile"
      via: "per-preset profile lookup"
      pattern: "profileCatalog.GetProfile\(preset.ProfileName\)"
    - from: "BodyGenIniExportWriter.Write"
      to: "TemplateGenerationService.GenerateTemplates + MorphGenerationService.GenerateMorphs"
      via: "MainWindowViewModel export flow"
      pattern: "templates.ini"
---

<objective>
Close the verified profile-specific BoS JSON and morph/export test-coverage gap.

Purpose: Verification found that Phase 1 proves distinct FO4 template behavior but does not explicitly protect BoS JSON for each bundled profile or explain/test morph profile independence. This plan adds focused tests without changing sacred golden outputs.
Output: Profile-specific BoS JSON assertions, BodyGen export integration assertions, and morph profile-independence assertions.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
@.planning/phases/01-profile-correctness-and-trust/01-RESEARCH.md
@.planning/phases/01-profile-correctness-and-trust/01-VERIFICATION.md
@tests/fixtures/README.md
@tests/fixtures/inputs/minimal/MATH-WALKTHROUGH.md
@settings.json
@settings_UUNP.json
@settings_FO4_CBBE.json
@src/BS2BG.Core/Generation/TemplateGenerationService.cs
@src/BS2BG.Core/Generation/MorphGenerationService.cs
@src/BS2BG.Core/Export/BodyGenIniExportWriter.cs
@src/BS2BG.Core/Export/BosJsonExportWriter.cs
@tests/BS2BG.Tests/SliderMathFormatterTests.cs
@tests/BS2BG.Tests/ExportWriterTests.cs
@tests/BS2BG.Tests/MorphCoreTests.cs

<interfaces>
Core contracts used by the tests:
```csharp
public string TemplateGenerationService.PreviewBosJson(SliderPreset preset, TemplateProfile profile);
public string TemplateGenerationService.GenerateTemplates(IEnumerable<SliderPreset> presets, TemplateProfileCatalog profileCatalog, bool omitRedundantSliders);
public MorphGenerationResult MorphGenerationService.GenerateMorphs(ProjectModel project);
public BodyGenIniExportResult BodyGenIniExportWriter.Write(string directoryPath, string templatesText, string morphsText);
public virtual BosJsonExportResult BosJsonExportWriter.Write(string directoryPath, IEnumerable<SliderPreset> presets, TemplateProfileCatalog profileCatalog);
```

Known bundled-profile distinguishing data:
```text
settings_FO4_CBBE.json: includes BreastCenterBig, ButtNew, ShoulderTweak, HipBack, ChubbyWaist; Multipliers all 1.0; Inverted empty.
settings.json: Skyrim CBBE has Breasts valueSmall 0.2, Butt valueSmall 0, and inverted sliders.
settings_UUNP.json: Skyrim UUNP has Breasts valueSmall 1/valueBig 1 and inverted sliders.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add profile-specific BoS JSON formatter tests</name>
  <files>tests/BS2BG.Tests/SliderMathFormatterTests.cs</files>
  <read_first>
    - tests/BS2BG.Tests/SliderMathFormatterTests.cs
    - settings.json
    - settings_UUNP.json
    - settings_FO4_CBBE.json
    - .planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
    - .planning/phases/01-profile-correctness-and-trust/01-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: `BosJsonUsesBundledProfileSpecificSliderTables` loads the three root bundled profiles and formats a deterministic preset through BoS JSON for each.
    - Test 2: Fallout 4 CBBE BoS JSON contains FO4-only default slider names such as `BreastCenterBig`, `ButtNew`, `ShoulderTweak`, `HipBack`, and `ChubbyWaist`.
    - Test 3: Skyrim CBBE and Skyrim UUNP BoS JSON do not contain FO4-only slider names.
    - Test 4: A shared slider such as `Breasts` produces different JSON values across profiles due to distinct defaults/inversion; assert exact substrings rather than golden files.
  </behavior>
  <action>Add a focused test to `SliderMathFormatterTests.cs`; do not change the existing golden fixture theories or any file under `tests/fixtures/expected/**`. Add a helper that loads root-level bundled profile files by filename from `RepositoryRoot` (`settings.json`, `settings_UUNP.json`, `settings_FO4_CBBE.json`) using the existing JSON parsing shape from `LoadProfile`. Use `SliderMathFormatter.FormatBosJson` with deterministic in-memory presets. Assert exact profile-distinguishing substrings including FO4-only slider names and absence of those names from Skyrim profiles. Keep newline normalization local to assertions where needed; do not alter `JavaFloatFormatting`, `SliderMathFormatter`, or export writers.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~SliderMathFormatterTests</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BS2BG.Tests/SliderMathFormatterTests.cs` contains `BosJsonUsesBundledProfileSpecificSliderTables`.
    - The test contains `settings_FO4_CBBE.json`, `settings_UUNP.json`, and `settings.json`.
    - The test contains `BreastCenterBig`, `ButtNew`, `ShoulderTweak`, `HipBack`, and `ChubbyWaist` assertions.
    - `git diff -- tests/fixtures/expected` shows no changes.
    - The automated test command exits 0.
  </acceptance_criteria>
  <done>BoS JSON behavior is protected by distinct bundled-profile tests without rebasing sacred golden files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add BodyGen export and morph profile-independence coverage</name>
  <files>tests/BS2BG.Tests/ExportWriterTests.cs, tests/BS2BG.Tests/MorphCoreTests.cs</files>
  <read_first>
    - tests/BS2BG.Tests/ExportWriterTests.cs
    - tests/BS2BG.Tests/MorphCoreTests.cs
    - src/BS2BG.Core/Generation/TemplateGenerationService.cs
    - src/BS2BG.Core/Generation/MorphGenerationService.cs
    - src/BS2BG.Core/Export/BodyGenIniExportWriter.cs
    - .planning/phases/01-profile-correctness-and-trust/01-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: BodyGen export writes `templates.ini` content generated through a catalog containing at least two profile-specific profiles and writes `morphs.ini` content generated from assigned targets/NPCs.
    - Test 2: The exported `templates.ini` contains profile-distinguishing template lines for the selected profiles; the exported `morphs.ini` contains assignment lines such as `All|Female=Alpha` and is unaffected by preset profile names.
    - Test 3: `MorphGenerationDoesNotDependOnPresetProfileNames` proves morph output remains the same when assigned presets use `Skyrim CBBE`, `Skyrim UUNP`, and `Fallout 4 CBBE` profile names.
  </behavior>
  <action>In `ExportWriterTests.cs`, add an integration-style test named `BodyGenExportCombinesProfileSpecificTemplatesWithProfileIndependentMorphs`. Build in-memory `SliderPreset` instances with different `ProfileName` values and slider/default combinations that produce distinguishable template text through `TemplateGenerationService.GenerateTemplates`; generate morph text through `MorphGenerationService.GenerateMorphs`; write both through `BodyGenIniExportWriter.Write`; assert exact `templates.ini` and `morphs.ini` file contents with CRLF-normalized expected strings. In `MorphCoreTests.cs`, add `MorphGenerationDoesNotDependOnPresetProfileNames` proving that changing assigned presets' `ProfileName` values does not alter morph lines. Do not modify production generation/export code unless a test exposes an actual defect; this gap is missing coverage and design proof, not a known production logic bug.</action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ExportWriterTests|FullyQualifiedName~MorphCoreTests"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BS2BG.Tests/ExportWriterTests.cs` contains `BodyGenExportCombinesProfileSpecificTemplatesWithProfileIndependentMorphs`.
    - `tests/BS2BG.Tests/MorphCoreTests.cs` contains `MorphGenerationDoesNotDependOnPresetProfileNames`.
    - The export test asserts both `templates.ini` and `morphs.ini` contents.
    - The morph test contains all three profile constants: `SkyrimCbbe`, `SkyrimUunp`, and `Fallout4Cbbe`.
    - The automated test command exits 0.
  </acceptance_criteria>
  <done>Phase 1 has explicit coverage for profile-specific templates/BoS JSON and documented morph profile independence through tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled profile JSON -> formatter tests | Root-level profile data drives generated template/BoS assertions. |
| Project model -> export files | Generated template and morph strings are persisted to INI/JSON files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Tampering | Bundled profile tests | mitigate | Tests load root bundled profile files and assert FO4-only names are absent from Skyrim outputs. |
| T-01-17 | Repudiation | Morph profile independence | mitigate | Add explicit tests proving morph output ignores profile metadata by design. |
| T-01-18 | Tampering | Golden expected fixtures | mitigate | Acceptance gate requires `git diff -- tests/fixtures/expected` has no changes. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~SliderMathFormatterTests|FullyQualifiedName~ExportWriterTests|FullyQualifiedName~MorphCoreTests"`, then run `dotnet test` before marking the gap closure complete.
</verification>

<success_criteria>
- `01-VERIFICATION.md` truth 13 is covered by tests for BoS JSON, templates, and morph/export behavior.
- No sacred golden expected fixtures are edited.
- Full test suite remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-profile-correctness-and-trust/01-06-SUMMARY.md`.
</output>
