---
phase: 01-profile-correctness-and-trust
plan: 07
type: execute
wave: 4
depends_on:
  - 04
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - PROF-03
  - PROF-05
must_haves:
  truths:
    - "Phase 1 source contracts explicitly record that D-05 through D-08 constrain the original warning/experimental wording."
    - "The accepted Phase 1 contract is neutral unresolved-profile fallback information plus release-facing FO4 calibration context, not modal warnings or mismatch heuristics."
    - "Future verification can evaluate PROF-03/PROF-05 against the locked context decisions without reintroducing deferred warning/inference work."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1 goal and success criteria aligned with locked warning/confidence decisions"
      contains: "neutral unresolved-profile fallback information"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PROF-03/PROF-05 wording aligned with D-05 through D-08"
      contains: "neutral unresolved-profile fallback information"
  key_links:
    - from: ".planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md"
      to: ".planning/ROADMAP.md"
      via: "D-05 through D-08 context override reflected in success criteria"
      pattern: "D-05"
    - from: ".planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "PROF-03 and PROF-05 clarify neutral/info-only contract"
      pattern: "D-08"
---

<objective>
Close the roadmap/requirements contract mismatch that caused the verifier to demand warnings forbidden by locked user decisions.

Purpose: `01-VERIFICATION.md` correctly detected that ROADMAP/PROF-03 still asked for warnings and experimental/profile-risk states, while `01-CONTEXT.md` D-05 through D-08 explicitly forbid those UI behaviors in Phase 1. This plan records the accepted override in the phase source contracts instead of implementing prohibited warning UX.
Output: ROADMAP and REQUIREMENTS wording clarified so Phase 1 verification checks neutral fallback information and release-facing FO4 context.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
@.planning/phases/01-profile-correctness-and-trust/01-RESEARCH.md
@.planning/phases/01-profile-correctness-and-trust/01-VERIFICATION.md

<interfaces>
Locked context decisions that must not be simplified or reversed:
```text
D-05: Do not add general warnings for unprofiled or custom body mods.
D-06: Do not label Fallout 4 CBBE as experimental in the main workflow, selector, or warning UI.
D-07: Preserve unbundled profile names and use neutral informational fallback text.
D-08: Do not implement modal warnings, warning banners, mismatch heuristics, or FO4 experimental labels unless a later spec explicitly reverses this context.
D-10: Do not implement likely-mismatch detection from slider names in Phase 1.
D-16: Custom/unprofiled body-mod slider names do not produce mismatch warnings or block generation.
```

Verification gap source:
```text
Missing: Add/restore a clear warning or accepted override for the roadmap PROF-03 profile-risk contract.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clarify Phase 1 roadmap success criteria against locked warning decisions</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
    - .planning/phases/01-profile-correctness-and-trust/01-VERIFICATION.md
  </read_first>
  <action>Update only the Phase 1 section of `.planning/ROADMAP.md`. Replace the Phase 1 goal sentence with: `Users can generate output with explicit, profile-specific semantics, neutral unresolved-profile fallback information, and release-facing Fallout 4 profile confidence context.` Replace success criterion 3 with: `User can see neutral unresolved-profile fallback information when a saved project references an unbundled profile, while Phase 1 avoids profile inference, mismatch warnings, and in-app Fallout 4 experimental labels per D-05 through D-10.` Add an indented note below the Phase 1 success criteria: `Context override: Phase 1 CONTEXT.md D-05 through D-08 intentionally narrow the original warning/experimental wording to neutral fallback information plus release documentation; modal warnings, mismatch heuristics, and in-app FO4 experimental labels remain out of scope.` Keep existing plan list entries and add Plan 05 through Plan 07 if not already present. Do not alter Phase 2 through Phase 5 wording.</action>
  <verify>
    <automated>gsd-sdk query roadmap.get-phase "1"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 1 contains `neutral unresolved-profile fallback information`.
    - `.planning/ROADMAP.md` Phase 1 contains `D-05 through D-08`.
    - `.planning/ROADMAP.md` Phase 1 does not contain the old sentence `visible warnings when profile confidence is incomplete`.
    - `.planning/ROADMAP.md` Phase 2 heading remains `### Phase 2: Workflow Persistence, Filtering, and Undo Hardening`.
    - The automated command returns JSON with `found: true`.
  </acceptance_criteria>
  <done>The Phase 1 roadmap contract reflects the locked context decisions and no longer asks executors/verifiers to add prohibited warning UX.</done>
</task>

<task type="auto">
  <name>Task 2: Clarify PROF-03 and PROF-05 requirement wording with accepted override</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md
    - .planning/phases/01-profile-correctness-and-trust/01-VERIFICATION.md
  </read_first>
  <action>Update only the Phase 1 `Profile Correctness` requirements in `.planning/REQUIREMENTS.md`. Replace PROF-03 with: `User can see neutral unresolved-profile fallback information when a preset/project references an unbundled profile name, without profile inference, slider-name mismatch warnings, or generation/export blocking for custom body mods per Phase 1 decisions D-05 through D-12 and D-16.` Replace PROF-05 with: `User can understand the Fallout 4 CBBE profile seed/calibration status through release-facing documentation while the main workflow does not label Fallout 4 CBBE as experimental per D-06 and D-08.` Add a short paragraph immediately after PROF-05: `Accepted Phase 1 override: ROADMAP/requirement wording that previously mentioned warnings, inferred/mismatched states, or experimental in-app status is intentionally narrowed by CONTEXT.md D-05 through D-08; future phases may revisit diagnostics through new specs, but Phase 1 must not add the deferred mismatch/inference work.` Do not change v2 deferred requirements, out-of-scope rows, or non-Phase-1 requirement text.</action>
  <verify>
    <automated>gsd-sdk query init.plan-phase "1"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` PROF-03 contains `neutral unresolved-profile fallback information`.
    - `.planning/REQUIREMENTS.md` PROF-03 contains `D-05 through D-12 and D-16`.
    - `.planning/REQUIREMENTS.md` PROF-05 contains `release-facing documentation` and `does not label Fallout 4 CBBE as experimental`.
    - `.planning/REQUIREMENTS.md` contains `Accepted Phase 1 override`.
    - The automated command returns `phase_req_ids` including `PROF-01, PROF-02, PROF-03, PROF-04, PROF-05`.
  </acceptance_criteria>
  <done>Requirements express the accepted context-driven override rather than sending future agents back toward prohibited warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning source contracts -> execution/verification agents | ROADMAP and REQUIREMENTS determine what future agents build and verify. |
| User decisions -> source contract wording | Locked CONTEXT decisions constrain requirement interpretation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19 | Repudiation | ROADMAP/REQUIREMENTS | mitigate | Explicitly cite D-05 through D-08 in source contracts so the accepted override is auditable. |
| T-01-20 | Tampering | Phase scope | mitigate | Limit edits to Phase 1 PROF-03/PROF-05 and Phase 1 roadmap text; acceptance criteria protect later phases. |
| T-01-21 | Information Quality | Future verification | mitigate | Replace contradictory warning wording with exact neutral fallback/release-doc contract that matches implemented behavior. |
</threat_model>

<verification>
Run `gsd-sdk query roadmap.get-phase "1"` and `gsd-sdk query init.plan-phase "1"`. Then run the post-planning gap checks if available: `gsd-sdk query check.decision-coverage-plan ".planning/phases/01-profile-correctness-and-trust" ".planning/phases/01-profile-correctness-and-trust/01-CONTEXT.md"`.
</verification>

<success_criteria>
- The PROF-03 verification gap is closed by an accepted override that honors D-05 through D-08.
- ROADMAP and REQUIREMENTS no longer instruct agents to implement Phase 1 warning/inference/experimental UI that the user forbade.
- Phase 2 through Phase 5 contracts are untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-profile-correctness-and-trust/01-07-SUMMARY.md`.
</output>
