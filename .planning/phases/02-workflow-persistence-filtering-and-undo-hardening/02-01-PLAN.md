---
phase: 02-workflow-persistence-filtering-and-undo-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BS2BG.App/Services/UserPreferencesService.cs
  - src/BS2BG.App/ViewModels/TemplatesViewModel.cs
  - src/BS2BG.App/ViewModels/MainWindowViewModel.cs
  - tests/BS2BG.Tests/UserPreferencesServiceTests.cs
  - tests/BS2BG.Tests/TemplatesViewModelTests.cs
autonomous: true
requirements: [WORK-01]
must_haves:
  truths:
    - "User can restart BS2BG and keep the Omit Redundant Sliders workflow preference."
    - "Theme preference compatibility remains intact while workflow preferences expand."
    - "Preference load/save failure never blocks startup or template generation."
  artifacts:
    - path: "src/BS2BG.App/Services/UserPreferencesService.cs"
      provides: "Backward-compatible local workflow preference DTO and best-effort persistence"
      contains: "OmitRedundantSliders"
    - path: "src/BS2BG.App/ViewModels/TemplatesViewModel.cs"
      provides: "Preference hydration and best-effort save for template generation options"
      contains: "OmitRedundantSliders"
    - path: "tests/BS2BG.Tests/UserPreferencesServiceTests.cs"
      provides: "Preference compatibility and failure coverage"
  key_links:
    - from: "src/BS2BG.App/ViewModels/TemplatesViewModel.cs"
      to: "src/BS2BG.App/Services/UserPreferencesService.cs"
      via: "IUserPreferencesService.Load/Save"
      pattern: "preferencesService\\.(Load|Save)"
---

<objective>
Persist generation-affecting workflow preference state without changing project serialization.

Purpose: Users retain Omit Redundant Sliders across restarts while `.jbs2bg` remains a shared project file format, per D-01 and D-03.
Output: Extended local preferences contract, ViewModel hydration/save wiring, and regression tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-CONTEXT.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@openspec/specs/reactive-mvvm-conventions/spec.md

<interfaces>
From src/BS2BG.App/Services/UserPreferencesService.cs:
```csharp
public enum ThemePreference { System, Light, Dark }
public sealed class UserPreferences { public ThemePreference Theme { get; set; } = ThemePreference.System; }
public interface IUserPreferencesService
{
    UserPreferences Load();
    bool Save(UserPreferences preferences);
}
```

From src/BS2BG.App/ViewModels/TemplatesViewModel.cs:
```csharp
[Reactive] private bool _omitRedundantSliders;
public ReactiveCommand<Unit, Unit> GenerateTemplatesCommand { get; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend local preference DTO and compatibility tests</name>
  <files>src/BS2BG.App/Services/UserPreferencesService.cs, tests/BS2BG.Tests/UserPreferencesServiceTests.cs</files>
  <behavior>
    - Existing JSON containing only `Theme` still loads with `OmitRedundantSliders == false`.
    - Saving preferences writes both `Theme` and `OmitRedundantSliders`.
    - Corrupt JSON, missing files, IO failures, and unauthorized saves return defaults/false without throwing per D-03.
  </behavior>
  <action>Add `bool OmitRedundantSliders { get; set; }` to `UserPreferences` per D-01. Preserve the existing `Theme` property name and enum values so current preference files remain compatible. Keep the existing `Load()` fallback and `Save()` false-return behavior; preferences are convenience state and must not block workflows per D-03. Add or extend tests using FluentAssertions.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~UserPreferencesServiceTests</automated>
  </verify>
  <done>Preference JSON round-trips theme plus omit-redundant state, and failure cases continue returning defaults/false without throwing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hydrate and save Omit Redundant Sliders from Templates workflow</name>
  <files>src/BS2BG.App/ViewModels/TemplatesViewModel.cs, src/BS2BG.App/ViewModels/MainWindowViewModel.cs, tests/BS2BG.Tests/TemplatesViewModelTests.cs</files>
  <behavior>
    - Constructing `TemplatesViewModel` with loaded preferences sets `OmitRedundantSliders` before generation.
    - Changing `OmitRedundantSliders` saves local preferences without mutating `ProjectModel` or `.jbs2bg` serialization.
    - Failed preference saves produce only non-blocking status behavior and do not prevent `GenerateTemplatesCommand` from running.
  </behavior>
  <action>Inject or pass `IUserPreferencesService` consistently so `TemplatesViewModel` hydrates `OmitRedundantSliders` from local preferences per D-01. When the property changes, save a preferences object preserving the currently loaded `Theme`; do not add this setting to project serialization. If save fails, use the UI-spec non-blocking copy `This workflow preference could not be saved. BS2BG will continue using defaults for this session.` or an equivalent shell/template status path; do not show modal errors per D-03.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~TemplatesViewModelTests</automated>
  </verify>
  <done>Template generation uses the hydrated omit-redundant value after restart, and preference save failures do not block import, generation, save, or export workflows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local preferences JSON → App startup/ViewModels | Corrupt or user-edited local JSON crosses into workflow state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | D | UserPreferencesService.Load | mitigate | Catch JsonException, IOException, and UnauthorizedAccessException and return defaults per D-03. |
| T-02-01-02 | I | UserPreferences | mitigate | Keep workflow preferences in AppData only; do not write local preference values into `.jbs2bg` project files per D-01. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~UserPreferencesServiceTests|FullyQualifiedName~TemplatesViewModelTests"`, then `dotnet test` before phase verification.
</verification>

<success_criteria>
WORK-01 preference state is locally persisted for Omit Redundant Sliders, theme compatibility remains intact, and preference failures remain best-effort/non-blocking.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-01-SUMMARY.md`
</output>
