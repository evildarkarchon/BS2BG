---
phase: 02-workflow-persistence-filtering-and-undo-hardening
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/BS2BG.App/Services/UserPreferencesService.cs
  - src/BS2BG.App/Services/IFileDialogService.cs
  - src/BS2BG.App/Services/WindowFileDialogService.cs
  - src/BS2BG.App/AppBootstrapper.cs
  - tests/BS2BG.Tests/MainWindowViewModelTests.cs
  - tests/BS2BG.Tests/UserPreferencesServiceTests.cs
autonomous: true
requirements: [WORK-01]
must_haves:
  truths:
    - "User can restart BS2BG and retain separate project open/save, BodyGen export, and BoS JSON export folders."
    - "Project/export picker start folders are suggestions and invalid preference paths do not block workflows."
    - "Folder preferences remain local and are not written to `.jbs2bg` projects or generated outputs."
  artifacts:
    - path: "src/BS2BG.App/Services/UserPreferencesService.cs"
      provides: "Separate remembered folder preference fields"
      contains: "ProjectFolder"
    - path: "src/BS2BG.App/Services/WindowFileDialogService.cs"
      provides: "Remembered project, BodyGen, and BoS picker channels"
      contains: "SuggestedStartLocation"
  key_links:
    - from: "WindowFileDialogService.cs"
      to: "UserPreferencesService.cs"
      via: "project/export channel Load, SuggestedStartLocation, Save"
      pattern: "(ProjectFolder|BodyGenExportFolder|BosJsonExportFolder)"
---

<objective>
Remember separate project and export folders for shell-level file workflows.

Purpose: Users avoid repetitive navigation for project open/save and export folders while preferences remain local, best-effort, and non-blocking per D-02 and D-03.
Output: Preference fields, project/export picker service wiring, DI updates, and service/ViewModel tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-CONTEXT.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-RESEARCH.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-UI-SPEC.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-01-SUMMARY.md

<interfaces>
From src/BS2BG.App/Services/IFileDialogService.cs:
```csharp
public interface IFileDialogService
{
    Task<string?> PickOpenProjectFileAsync(CancellationToken cancellationToken);
    Task<string?> PickSaveProjectFileAsync(string? currentPath, CancellationToken cancellationToken);
    Task<string?> PickBodyGenExportFolderAsync(CancellationToken cancellationToken);
    Task<string?> PickBosJsonExportFolderAsync(CancellationToken cancellationToken);
}
```

Plan 01 provides `UserPreferences.Theme` and `UserPreferences.OmitRedundantSliders`; this plan adds the folder fields without changing preference failure semantics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add project and export folder preference fields</name>
  <files>src/BS2BG.App/Services/UserPreferencesService.cs, tests/BS2BG.Tests/UserPreferencesServiceTests.cs</files>
  <behavior>
    - Preferences store distinct nullable/string paths for `ProjectFolder`, `BodyGenExportFolder`, and `BosJsonExportFolder` per D-02.
    - Existing theme/omit-only preference JSON loads with folder fields unset.
    - Corrupt or unwritable preference files remain defaults/false per D-03.
  </behavior>
  <action>Extend `UserPreferences` with separate project/export folder properties: `ProjectFolder`, `BodyGenExportFolder`, and `BosJsonExportFolder`. Keep these local preference names only; do not add project serialization fields. Tests must prove all fields round-trip and missing fields default without errors.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~UserPreferencesServiceTests</automated>
  </verify>
  <done>Project and export folder channels persist independently and old preference files remain compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed and update project/export picker folders</name>
  <files>src/BS2BG.App/Services/IFileDialogService.cs, src/BS2BG.App/Services/WindowFileDialogService.cs, src/BS2BG.App/AppBootstrapper.cs, tests/BS2BG.Tests/MainWindowViewModelTests.cs</files>
  <behavior>
    - Project open/save uses only `ProjectFolder` as its start-folder channel and updates it after successful selection/save.
    - BodyGen export uses only `BodyGenExportFolder`; BoS export uses only `BosJsonExportFolder`.
    - Invalid or inaccessible remembered folders are ignored as picker hints and do not cancel workflows per D-03.
  </behavior>
  <action>Inject `IUserPreferencesService` into `WindowFileDialogService` and update DI. Resolve remembered folders with Avalonia `StorageProvider.TryGetFolderFromPathAsync` and pass valid results as `SuggestedStartLocation`; treat null/invalid results as no hint. After successful project path or export folder selection, save that channel's containing folder/path best-effort. Preserve cancellation checks after picker calls.</action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~MainWindowViewModelTests|FullyQualifiedName~UserPreferencesServiceTests"</automated>
  </verify>
  <done>Project and export picker channels remember and update their own folders without collapsing into a global folder or blocking if preference persistence fails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored local path → Avalonia picker hint | User-edited preference paths are used only as suggestions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | D | WindowFileDialogService | mitigate | Treat `TryGetFolderFromPathAsync` failures/null as no suggested start location; continue picker workflow per D-03. |
| T-02-02-02 | I | UserPreferences folder fields | mitigate | Store folder paths only in local AppData preferences; never write them into `.jbs2bg`, exports, logs, or shared artifacts. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~MainWindowViewModelTests|FullyQualifiedName~UserPreferencesServiceTests"`, then `dotnet test` after Wave 2.
</verification>

<success_criteria>
WORK-01 project/export folder persistence is complete for project open/save, BodyGen export, and BoS export with independent local channels.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-02-SUMMARY.md`
</output>
