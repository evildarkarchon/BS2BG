---
phase: 02-workflow-persistence-filtering-and-undo-hardening
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/BS2BG.App/Services/IBodySlideXmlFilePicker.cs
  - src/BS2BG.App/Services/INpcTextFilePicker.cs
  - src/BS2BG.App/Services/WindowBodySlideXmlFilePicker.cs
  - src/BS2BG.App/Services/WindowNpcTextFilePicker.cs
  - src/BS2BG.App/AppBootstrapper.cs
  - tests/BS2BG.Tests/TemplatesViewModelTests.cs
  - tests/BS2BG.Tests/MorphsViewModelTests.cs
autonomous: true
requirements: [WORK-01]
must_haves:
  truths:
    - "User can restart BS2BG and retain separate BodySlide XML import and NPC text import folders."
    - "Import picker preference failures do not block template or NPC imports."
    - "NPC filter/search state remains session-only and is not serialized to preferences."
  artifacts:
    - path: "src/BS2BG.App/Services/WindowBodySlideXmlFilePicker.cs"
      provides: "Remembered BodySlide XML import start folder"
      contains: "BodySlideXmlFolder"
    - path: "src/BS2BG.App/Services/WindowNpcTextFilePicker.cs"
      provides: "Remembered NPC text import start folder"
      contains: "NpcTextFolder"
  key_links:
    - from: "WindowBodySlideXmlFilePicker.cs"
      to: "UserPreferences.BodySlideXmlFolder"
      via: "SuggestedStartLocation and best-effort Save"
      pattern: "BodySlideXmlFolder"
    - from: "WindowNpcTextFilePicker.cs"
      to: "UserPreferences.NpcTextFolder"
      via: "SuggestedStartLocation and best-effort Save"
      pattern: "NpcTextFolder"
---

<objective>
Wire remembered start folders into import picker services.

Purpose: Users keep separate BodySlide XML and NPC text import locations across restarts, without persisting transient NPC filters per D-02 through D-04.
Output: Import picker preference wiring, DI registration updates, and ViewModel import tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-CONTEXT.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-02-SUMMARY.md

<interfaces>
From current import picker services:
```csharp
public interface IBodySlideXmlFilePicker
{
    Task<IReadOnlyList<string>> PickXmlPresetFilesAsync(CancellationToken cancellationToken);
}

public interface INpcTextFilePicker
{
    Task<IReadOnlyList<string>> PickNpcTextFilesAsync(CancellationToken cancellationToken);
}
```

Plan 02 provides `UserPreferences.BodySlideXmlFolder` and `UserPreferences.NpcTextFolder` fields through `IUserPreferencesService`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add remembered BodySlide XML import folder wiring</name>
  <files>src/BS2BG.App/Services/IBodySlideXmlFilePicker.cs, src/BS2BG.App/Services/WindowBodySlideXmlFilePicker.cs, src/BS2BG.App/AppBootstrapper.cs, tests/BS2BG.Tests/TemplatesViewModelTests.cs</files>
  <behavior>
    - XML picker uses `BodySlideXmlFolder` as an Avalonia `SuggestedStartLocation` when resolvable.
    - Successful XML file selection saves the containing directory back to `BodySlideXmlFolder` best-effort.
    - Save failures do not prevent `ImportPresetsAsync` from parsing selected files per D-03.
  </behavior>
  <action>Inject `IUserPreferencesService` into `WindowBodySlideXmlFilePicker`. Load the preference, resolve `BodySlideXmlFolder` using `TryGetFolderFromPathAsync`, set `SuggestedStartLocation`, and save the selected files' containing folder after a successful picker result. Preserve cancellation behavior and do not alter BodySlide XML parser semantics.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~TemplatesViewModelTests</automated>
  </verify>
  <done>BodySlide XML imports start in and update their own remembered folder channel without blocking on preference errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add remembered NPC text import folder wiring</name>
  <files>src/BS2BG.App/Services/INpcTextFilePicker.cs, src/BS2BG.App/Services/WindowNpcTextFilePicker.cs, src/BS2BG.App/AppBootstrapper.cs, tests/BS2BG.Tests/MorphsViewModelTests.cs</files>
  <behavior>
    - NPC text picker uses `NpcTextFolder` as an Avalonia `SuggestedStartLocation` when resolvable.
    - Successful NPC file selection saves the containing directory back to `NpcTextFolder` best-effort.
    - No NPC search/filter state appears in `UserPreferences` per D-04.
  </behavior>
  <action>Inject `IUserPreferencesService` into `WindowNpcTextFilePicker`. Use `NpcTextFolder` only for picker seeding and successful-selection updates. Add tests proving imports continue when saving preferences fails and that filter/search state remains absent from preference JSON per D-04.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~MorphsViewModelTests</automated>
  </verify>
  <done>NPC text imports start in and update their own remembered folder channel, and transient filter/search state is not persisted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local path preference → file picker | Stored user-editable paths are used as import picker hints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | D | WindowBodySlideXmlFilePicker / WindowNpcTextFilePicker | mitigate | Treat invalid/unresolvable paths as no start hint and keep import commands non-blocking per D-03. |
| T-02-03-02 | I | UserPreferences import folders | mitigate | Do not serialize import folder paths or filter state into `.jbs2bg`; keep them local only per D-02/D-04. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~TemplatesViewModelTests|FullyQualifiedName~MorphsViewModelTests|FullyQualifiedName~UserPreferencesServiceTests"`.
</verification>

<success_criteria>
WORK-01 import folder persistence is complete for BodySlide XML and NPC text files with independent channels and non-blocking preference behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-03-SUMMARY.md`
</output>
