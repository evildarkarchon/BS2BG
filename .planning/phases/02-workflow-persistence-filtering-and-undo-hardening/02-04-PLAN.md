---
phase: 02-workflow-persistence-filtering-and-undo-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Packages.props
  - src/BS2BG.App/BS2BG.App.csproj
  - src/BS2BG.App/ViewModels/Workflow/NpcRowViewModel.cs
  - src/BS2BG.App/ViewModels/Workflow/NpcFilterState.cs
  - tests/BS2BG.Tests/NpcFilterStateTests.cs
autonomous: true
requirements: [WORK-02, WORK-05]
must_haves:
  truths:
    - "Every NPC row has a generated stable UI identity independent of Mod, EditorID, FormID, and display name."
    - "NPC filter predicates cover mod, name, editor ID, form ID, race, assignment state, and preset-related values."
    - "Text filtering can be debounced by the ViewModel while checklist predicates apply immediately."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/Workflow/NpcRowViewModel.cs"
      provides: "Stable App-layer NPC row identity wrapper"
      exports: ["NpcRowViewModel"]
    - path: "src/BS2BG.App/ViewModels/Workflow/NpcFilterState.cs"
      provides: "Pure filter state/predicate/value-list contracts"
      exports: ["NpcFilterState", "NpcFilterColumn"]
    - path: "Directory.Packages.props"
      provides: "Central DynamicData package version"
      contains: "DynamicData"
  key_links:
    - from: "NpcFilterState.cs"
      to: "NpcRowViewModel.cs"
      via: "CreatePredicate over stable row wrappers"
      pattern: "Func<NpcRowViewModel, bool>"
---

<objective>
Create the stable NPC row identity and filtering contracts consumed by Morphs integration.

Purpose: Filtering, selection preservation, and undo targeting must not depend on mutable NPC display/export fields per D-05 through D-08.
Output: DynamicData dependency wiring, App-layer row/filter helper types, and focused unit tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-CONTEXT.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@openspec/specs/reactive-mvvm-conventions/spec.md

<interfaces>
From src/BS2BG.Core/Models/Npc.cs:
```csharp
public sealed class Npc(string name) : MorphTargetBase(name)
{
    public string Mod { get; set; }
    public string EditorId { get; set; }
    public string Race { get; set; }
    public string FormId { get; set; }
    public string SliderPresetsText => string.Join("|", SliderPresets.Select(sliderPreset => sliderPreset.Name));
    public override string ToMorphLine() => Mod + "|" + FormId + "=" + string.Join("|", SliderPresets.Select(sliderPreset => sliderPreset.Name));
}
```

Use an App-layer wrapper/identity map first per research recommendation. Do not serialize row IDs into `.jbs2bg`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DynamicData package and stable NPC row wrapper</name>
  <files>Directory.Packages.props, src/BS2BG.App/BS2BG.App.csproj, src/BS2BG.App/ViewModels/Workflow/NpcRowViewModel.cs, tests/BS2BG.Tests/NpcFilterStateTests.cs</files>
  <behavior>
    - Creating row wrappers for duplicate NPC field values produces distinct generated `Guid RowId` values.
    - Editing `Npc.Mod`, `Npc.EditorId`, `Npc.FormId`, or `Npc.Name` does not change `RowId`.
    - Row ID is App workflow state only and is not added to Core `Npc` or project serialization.
  </behavior>
  <action>Add `DynamicData` with version `9.4.31` to central package management and reference it from `BS2BG.App` per research. Create `src/BS2BG.App/ViewModels/Workflow/NpcRowViewModel.cs` as an App-layer wrapper around `BS2BG.Core.Models.Npc` with generated `Guid RowId`, exposed `Npc`, and convenience read-only properties used by filters. Add XML doc comments for public constructors/properties because this is a new workflow contract. Do not modify `src/BS2BG.Core/Models/Npc.cs` for row identity.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~NpcFilterStateTests</automated>
  </verify>
  <done>Stable row identity exists in App-layer workflow code and DynamicData is centrally referenced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define checklist/filter predicate contracts</name>
  <files>src/BS2BG.App/ViewModels/Workflow/NpcFilterState.cs, tests/BS2BG.Tests/NpcFilterStateTests.cs</files>
  <behavior>
    - `NpcFilterColumn` covers Mod, Name, EditorId, FormId, Race, AssignmentState, and Preset per D-06.
    - Checklist predicates include/exclude rows by selected values for each column.
    - Free-text global search matches mod, name, editor ID, form ID, race, and assigned preset names; empty search matches all rows.
    - Assignment state distinguishes assigned and empty rows.
  </behavior>
  <action>Create a pure `NpcFilterState` helper that owns checklist allowed values/search text by `NpcFilterColumn`, builds predicates over `NpcRowViewModel`, and produces distinct available values for filter popups. It must not mutate NPC rows. Include methods/properties that let `MorphsViewModel` apply debounced global search separately from immediate checklist updates per D-08.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~NpcFilterStateTests</automated>
  </verify>
  <done>Filter helpers can evaluate all required columns and preset/assignment values without relying on UI controls or mutable field identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported NPC data → filter predicate/value lists | Untrusted local text data becomes searchable/filterable UI values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | D | NpcFilterState | mitigate | Keep predicate generation pure and side-effect free; add large-list unit coverage so malformed/large values do not mutate data. |
| T-02-04-02 | T | NpcRowViewModel | mitigate | Generate row IDs independent of mutable display/export fields per D-05 and do not serialize them. |
</threat_model>

<verification>
Run `dotnet test --filter FullyQualifiedName~NpcFilterStateTests` and `dotnet build BS2BG.sln`.
</verification>

<success_criteria>
WORK-02 and WORK-05 have reusable row identity/filter contracts ready for Morphs ViewModel integration.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-04-SUMMARY.md`
</output>
