---
phase: 02-workflow-persistence-filtering-and-undo-hardening
plan: 05
type: execute
wave: 2
depends_on: [04]
files_modified:
  - src/BS2BG.App/ViewModels/MorphsViewModel.cs
  - src/BS2BG.App/ViewModels/Workflow/NpcFilterState.cs
  - tests/BS2BG.Tests/MorphsViewModelTests.cs
autonomous: true
requirements: [WORK-02, WORK-05]
must_haves:
  truths:
    - "User can filter NPC rows by all required columns while each NPC keeps stable identity."
    - "Hidden selected NPCs remain selected when filters change."
    - "Large NPC search input is debounced so visible rows are not rebuilt on every keystroke."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/MorphsViewModel.cs"
      provides: "DynamicData-backed visible NPC collections, stable selected row IDs, and debounced search"
      contains: "SourceCache"
    - path: "tests/BS2BG.Tests/MorphsViewModelTests.cs"
      provides: "Filtering, hidden selection, and large-dataset responsiveness tests"
  key_links:
    - from: "MorphsViewModel.cs"
      to: "NpcFilterState.cs"
      via: "filter predicate observable"
      pattern: "CreatePredicate"
    - from: "MorphsViewModel.cs"
      to: "VisibleNpcs/SelectedNpcs"
      via: "stable row ID projection"
      pattern: "RowId"
---

<objective>
Integrate stable NPC filtering into MorphsViewModel.

Purpose: Users can filter large NPC datasets without identity loss, hidden selection loss, or unbounded UI churn per D-05 through D-08.
Output: ViewModel filtering pipeline, selection preservation, debounced search, and regression tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-CONTEXT.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-RESEARCH.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-UI-SPEC.md
@.planning/codebase/ARCHITECTURE.md
@openspec/specs/reactive-mvvm-conventions/spec.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-04-SUMMARY.md

<interfaces>
Plan 04 provides:
```csharp
public sealed class NpcRowViewModel { public Guid RowId { get; } public Npc Npc { get; } }
public sealed class NpcFilterState { public Func<NpcRowViewModel, bool> CreatePredicate(); }
```

Current MorphsViewModel exposes:
```csharp
public ObservableCollection<Npc> NpcDatabase { get; } = new();
public ObservableCollection<Npc> VisibleNpcs { get; } = new();
public ObservableCollection<Npc> VisibleNpcDatabase { get; } = new();
public ObservableCollection<Npc> SelectedNpcs { get; } = new();
```
Preserve these public collection contracts unless tests are updated deliberately; add row wrappers as internal implementation state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace full rebuild filtering with keyed row pipeline</name>
  <files>src/BS2BG.App/ViewModels/MorphsViewModel.cs, src/BS2BG.App/ViewModels/Workflow/NpcFilterState.cs, tests/BS2BG.Tests/MorphsViewModelTests.cs</files>
  <behavior>
    - Imported and morphed NPC rows receive stable row IDs and feed DynamicData keyed sources.
    - Filters for mod, name, editor ID, form ID, race, assignment state, and preset values update `VisibleNpcs`/`VisibleNpcDatabase` correctly.
    - Existing public commands still see `Npc` models for assignment/generation semantics.
  </behavior>
  <action>Use DynamicData `SourceCache<NpcRowViewModel, Guid>` (or equivalent keyed cache from Plan 04) for NPC database and morphed NPC rows. Bind filtered outputs back to the existing public `ObservableCollection<Npc>` surfaces if preserving bindings is simpler. Replace per-keystroke full clear/repopulate paths with predicate updates. Keep Core generation/export models unchanged.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~MorphsViewModelTests</automated>
  </verify>
  <done>All required column filters affect only visible projections and preserve existing morph generation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve hidden selection and debounce free-text search</name>
  <files>src/BS2BG.App/ViewModels/MorphsViewModel.cs, tests/BS2BG.Tests/MorphsViewModelTests.cs</files>
  <behavior>
    - Selecting NPC A and NPC B, then filtering to hide B, keeps B selected by stable row ID per D-07.
    - Status text can report visible/selected/hidden-selected counts using the UI-spec copy shape.
    - Rapid text updates do not rebuild visible rows once per keystroke; checklist changes still apply immediately per D-08.
    - A large dataset smoke test with thousands of NPCs completes the focused test run without unbounded delay.
  </behavior>
  <action>Track selected stable row IDs separately from visible collections, then project to `SelectedNpcs` for command compatibility. Add debounced global/free-text filtering using ReactiveUI scheduler patterns (`Throttle` with `RxApp.MainThreadScheduler` or a testable scheduler path); do not call `Dispatcher.UIThread` from ViewModels. Checklist changes should invalidate predicates immediately. Surface hidden selection count in existing Morphs status text when non-zero per UI-SPEC.</action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~MorphsViewModelTests"</automated>
  </verify>
  <done>Hidden selected NPCs survive filter changes, and large free-text filtering follows a debounced path protected by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported NPC rows → ViewModel collection/filter pipeline | Local import data drives reactive filtering and selection state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | D | MorphsViewModel filtering | mitigate | Debounce free-text search and use keyed incremental filtering for large datasets per D-08/WORK-05. |
| T-02-05-02 | T | SelectedNpcs / stable IDs | mitigate | Preserve selection by generated row ID instead of mutable fields or visible collection references per D-05/D-07. |
</threat_model>

<verification>
Run `dotnet test --filter FullyQualifiedName~MorphsViewModelTests` and `dotnet test --filter "FullyQualifiedName~MorphsViewModelTests&FullyQualifiedName~Large"` if the large smoke tests use that naming convention.
</verification>

<success_criteria>
WORK-02 and WORK-05 are satisfied at the ViewModel level: filtering covers all required columns, selection identity survives hidden rows, and large search uses a debounced incremental path.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-05-SUMMARY.md`
</output>
