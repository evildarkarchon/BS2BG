---
phase: 02-workflow-persistence-filtering-and-undo-hardening
plan: 06
type: execute
wave: 3
depends_on: [05]
files_modified:
  - src/BS2BG.App/Views/MainWindow.axaml
  - src/BS2BG.App/Views/MainWindow.axaml.cs
  - src/BS2BG.App/Themes/ThemeResources.axaml
  - tests/BS2BG.Tests/M6UxAppShellTests.cs
autonomous: true
requirements: [WORK-02]
must_haves:
  truths:
    - "User can open checklist filters for mod, name, editor ID, form ID, race, assignment state, and preset-related values."
    - "Active filters are visually discoverable and empty filtered views look like filtered-empty state, not data loss."
    - "New filter controls are accessible and compile under Avalonia compiled bindings."
  artifacts:
    - path: "src/BS2BG.App/Views/MainWindow.axaml"
      provides: "Per-column NPC filter UI and empty filtered state"
      contains: "Search mods"
    - path: "tests/BS2BG.Tests/M6UxAppShellTests.cs"
      provides: "Headless UI coverage for filter controls and compiled bindings"
  key_links:
    - from: "MainWindow.axaml"
      to: "MorphsViewModel"
      via: "compiled bindings to filter state properties/commands"
      pattern: "Morphs\\..*Filter"
---

<objective>
Expose full NPC checklist filtering in the Avalonia Morphs UI.

Purpose: Users can discover and operate all required filters directly in the table/list workflow while preserving compiled-binding and accessibility contracts per D-06 through D-08.
Output: AXAML filter controls/popups, theme accents, code-behind glue only where unavoidable, and headless UI tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-CONTEXT.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-RESEARCH.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-UI-SPEC.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@openspec/specs/reactive-mvvm-conventions/spec.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-05-SUMMARY.md

<interfaces>
Plan 05 provides MorphsViewModel filter properties/commands for all `NpcFilterColumn` values. Bind to those using compiled bindings. Existing `MainWindow` is a plain `Avalonia.Controls.Window`; do not convert it to `ReactiveWindow`.

Required UI copy from UI-SPEC:
- Empty heading: `No NPCs match the current filters`
- Empty body: `Clear one or more filters to show hidden NPCs, or change the bulk scope before running an action.`
- Popup placeholders: `Search mods`, `Search names`, `Search editor IDs`, `Search form IDs`, `Search races`, `Search assignment states`, `Search presets`
- Clear button: `Clear`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add filter popup controls for every NPC column</name>
  <files>src/BS2BG.App/Views/MainWindow.axaml, src/BS2BG.App/Views/MainWindow.axaml.cs, tests/BS2BG.Tests/M6UxAppShellTests.cs</files>
  <behavior>
    - Headless UI can locate filter controls for mod, name, editor ID, form ID, race, assignment state, and preset filters.
    - Each new popup has a search textbox with the exact UI-SPEC placeholder and a `Clear` button.
    - New controls set `automation:AutomationProperties.Name` and preserve tab order around search → filters → list.
  </behavior>
  <action>Generalize the existing race filter popup pattern in `MainWindow.axaml` to all required columns per D-06. Keep code-behind limited to view-only popup/light-dismiss/selection glue that cannot be expressed as a binding; do not put business logic in code-behind. Every new `DataTemplate` must declare `x:DataType`; use `ReflectionBinding` only with a comment explaining why if compiled binding cannot express a dynamic column. Use UI-SPEC spacing (`Padding=8`, popup width 240, max height 280) and labels exactly.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~M6UxAppShellTests</automated>
  </verify>
  <done>All required filter controls are present, accessible, light-dismissable/Escape-dismissable where practical, and compile with bindings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add active filter badges and filtered-empty state</name>
  <files>src/BS2BG.App/Views/MainWindow.axaml, src/BS2BG.App/Themes/ThemeResources.axaml, tests/BS2BG.Tests/M6UxAppShellTests.cs</files>
  <behavior>
    - Active checklist filters show badge copy like `Race: 3 selected` and use accent styling per UI-SPEC.
    - Empty filtered views show the exact heading/body copy and do not clear selected hidden NPCs.
    - Hidden selection status copy can be displayed as `{visible} visible, {selected} selected ({hiddenSelected} hidden by filters)` when ViewModel exposes it.
  </behavior>
  <action>Add active-filter visual treatment using existing theme resources plus the UI-SPEC accent usage; do not introduce a third-party UI library or icons. Add the filtered-empty state inside the relevant list area bound to ViewModel empty/filter-active state. Reuse `BS2BGInfo*` or neutral resources for informational copy; destructive/warning colors are reserved for destructive all-scope confirmation and existing warnings.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~M6UxAppShellTests</automated>
  </verify>
  <done>Filter state is visually discoverable, empty filtered lists explain filters rather than implying data loss, and headless UI tests cover the controls/copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewModel filter values → AXAML controls | Imported local data is displayed in popup value lists and badges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | I | MainWindow.axaml filter values | mitigate | Bind values as text through Avalonia controls; do not log/export filter values or paths. |
| T-02-06-02 | D | MainWindow.axaml compiled bindings | mitigate | Add headless UI tests and run build to catch missing x:DataType/binding failures. |
</threat_model>

<verification>
Run `dotnet test --filter FullyQualifiedName~M6UxAppShellTests` and `dotnet build BS2BG.sln`.
</verification>

<success_criteria>
WORK-02 is visible in the UI: users can operate all required per-column checklist filters with accessible, compiled-bound controls and clear filtered-empty feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-06-SUMMARY.md`
</output>
