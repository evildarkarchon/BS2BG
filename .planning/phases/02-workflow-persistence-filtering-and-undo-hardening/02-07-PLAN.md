---
phase: 02-workflow-persistence-filtering-and-undo-hardening
plan: 07
type: execute
wave: 4
depends_on: [05, 06]
files_modified:
  - src/BS2BG.App/ViewModels/Workflow/NpcBulkScopeResolver.cs
  - src/BS2BG.App/ViewModels/MorphsViewModel.cs
  - src/BS2BG.App/Views/MainWindow.axaml
  - tests/BS2BG.Tests/MorphsViewModelTests.cs
  - tests/BS2BG.Tests/M6UxAppShellTests.cs
autonomous: true
requirements: [WORK-03, WORK-05]
must_haves:
  truths:
    - "User can choose All, Visible, Selected, and Visible Empty scopes before bulk NPC operations."
    - "Filtered hidden rows are not mutated by visible or visible-empty operations."
    - "Destructive all-scope operations require confirmation while routine visible/selected edits rely on labels and undo."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/Workflow/NpcBulkScopeResolver.cs"
      provides: "Materialized scope target ID resolver"
      exports: ["NpcBulkScopeResolver", "NpcBulkScope"]
    - path: "src/BS2BG.App/ViewModels/MorphsViewModel.cs"
      provides: "Scope-aware NPC bulk commands"
      contains: "VisibleEmpty"
    - path: "src/BS2BG.App/Views/MainWindow.axaml"
      provides: "Scope selector and Fill Visible Empty CTA"
      contains: "Fill Visible Empty"
  key_links:
    - from: "MorphsViewModel.cs"
      to: "NpcBulkScopeResolver.cs"
      via: "materialize target row IDs before mutation"
      pattern: "Resolve.*Scope"
---

<objective>
Make NPC bulk operations explicitly scope-aware.

Purpose: Users can safely bulk-edit filtered datasets without hidden-row mutations, while all/visible/selected/visible-empty semantics are centralized and testable per D-09 through D-12 and D-16.
Output: Scope resolver, ViewModel command wiring, scope selector UI, and tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-CONTEXT.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-RESEARCH.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-UI-SPEC.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-05-SUMMARY.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-06-SUMMARY.md

<interfaces>
From current MorphsViewModel commands:
```csharp
public ReactiveCommand<Unit, Unit> AddAllVisibleImportedNpcsCommand { get; }
public ReactiveCommand<Unit, Unit> ClearVisibleNpcsCommand { get; }
public ReactiveCommand<Unit, Unit> FillEmptyNpcsCommand { get; }
public ReactiveCommand<Unit, Unit> ClearAssignmentsCommand { get; }
public ReactiveCommand<Unit, Unit> AssignSelectedNpcsCommand { get; }
public ReactiveCommand<Unit, Unit> ClearSelectedNpcAssignmentsCommand { get; }
```

Required UI labels from UI-SPEC: selector label `Scope`; options `All`, `Visible`, `Selected`, `Visible Empty`; primary CTA `Fill Visible Empty`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create centralized bulk scope resolver</name>
  <files>src/BS2BG.App/ViewModels/Workflow/NpcBulkScopeResolver.cs, tests/BS2BG.Tests/MorphsViewModelTests.cs</files>
  <behavior>
    - `All` returns all row IDs.
    - `Visible` returns only currently visible row IDs.
    - `Selected` returns selected row IDs, including hidden selected rows per D-07.
    - `VisibleEmpty` returns only visible rows with no assigned presets and excludes hidden/assigned rows per D-11.
    - Resolver materializes the target row ID list before mutation.
  </behavior>
  <action>Create `NpcBulkScope` enum with exact options `All`, `Visible`, `Selected`, and `VisibleEmpty`, plus `NpcBulkScopeResolver` that accepts current all/visible/selected row projections and returns an immutable/materialized target ID array. Add XML doc comments explaining that snapshotting IDs before mutation prevents filter/selection changes from changing affected targets.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~MorphsViewModelTests</automated>
  </verify>
  <done>All scope semantics are centralized, tested, and row-ID based.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply scopes to bulk commands and confirmation behavior</name>
  <files>src/BS2BG.App/ViewModels/MorphsViewModel.cs, tests/BS2BG.Tests/MorphsViewModelTests.cs</files>
  <behavior>
    - When filters are active, routine bulk operations default to `Visible` per D-10.
    - Random fill defaults to `VisibleEmpty` and changes only visible empty rows per D-11.
    - Destructive `All` clear/remove operations require confirmation with the UI-SPEC copy per D-12.
    - Each bulk operation records one undoable action, not one per row, per D-16.
    - Status messages include affected count and scope, e.g. `Assigned presets to 42 visible empty NPCs.`
  </behavior>
  <action>Add selected scope state and scope-aware command paths to `MorphsViewModel`. Resolve target row IDs once before invoking `MorphAssignmentService` or collection mutations. Use existing dialog service for destructive all-scope confirmation only; do not prompt routine visible/selected operations. Keep command availability observable and ReactiveUI-based.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~MorphsViewModelTests</automated>
  </verify>
  <done>Bulk NPC operations mutate only the explicit scope and report affected count/scope, with destructive all-scope confirmation and one undo record per bulk action.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add scope selector UI and primary CTA copy</name>
  <files>src/BS2BG.App/Views/MainWindow.axaml, tests/BS2BG.Tests/M6UxAppShellTests.cs</files>
  <behavior>
    - Morphs tab contains a `Scope` selector with exactly `All`, `Visible`, `Selected`, and `Visible Empty` options.
    - Random-fill button label is `Fill Visible Empty` and automation name reflects the same primary action.
    - Scope selector is placed near bulk action buttons and sets `automation:AutomationProperties.Name="Scope"`.
  </behavior>
  <action>Bind a ComboBox or equivalent selector to the Morphs ViewModel scope state using compiled bindings. Place it before the primary bulk action cluster as specified in UI-SPEC. Update the random-fill CTA to `Fill Visible Empty`. Do not multiply buttons for each scope and do not add post-click scope prompts per D-09.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~M6UxAppShellTests</automated>
  </verify>
  <done>The UI exposes one explicit scope selector with required labels/copy and headless tests cover it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-selected scope/filter state → bulk mutation | UI state determines which NPC rows mutate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | T | NpcBulkScopeResolver | mitigate | Materialize row IDs before mutation and test all/visible/selected/visible-empty exclusions. |
| T-02-07-02 | R | destructive all-scope operations | mitigate | Require confirmation for destructive all-scope clear/remove operations and record one undoable operation. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~MorphsViewModelTests|FullyQualifiedName~M6UxAppShellTests"`.
</verification>

<success_criteria>
WORK-03 is satisfied: every bulk NPC operation has explicit scope semantics and filtered hidden rows are protected from accidental visible/visible-empty mutations.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-07-SUMMARY.md`
</output>
