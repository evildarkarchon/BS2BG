---
phase: 02-workflow-persistence-filtering-and-undo-hardening
plan: 08
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/BS2BG.App/Services/UndoRedoService.cs
  - src/BS2BG.App/ViewModels/Workflow/UndoSnapshots.cs
  - src/BS2BG.App/ViewModels/TemplatesViewModel.cs
  - src/BS2BG.App/ViewModels/MainWindowViewModel.cs
  - tests/BS2BG.Tests/TemplatesViewModelTests.cs
  - tests/BS2BG.Tests/M6UxViewModelTests.cs
autonomous: true
requirements: [WORK-04, WORK-05]
must_haves:
  truths:
    - "Undo/redo history is bounded so long sessions do not grow without limit."
    - "User receives a non-blocking status when old undo entries are pruned."
    - "Preset and profile operations undo/redo from value snapshots rather than mutable live references."
  artifacts:
    - path: "src/BS2BG.App/Services/UndoRedoService.cs"
      provides: "Bounded undo/redo history and pruning notification"
      contains: "HistoryPruned"
    - path: "src/BS2BG.App/ViewModels/Workflow/UndoSnapshots.cs"
      provides: "Value snapshot DTOs/helpers for risky undo operations"
      exports: ["PresetValueSnapshot"]
    - path: "src/BS2BG.App/ViewModels/TemplatesViewModel.cs"
      provides: "Snapshot-hardened preset/profile undo paths"
  key_links:
    - from: "TemplatesViewModel.cs"
      to: "UndoSnapshots.cs"
      via: "preset/profile value snapshots"
      pattern: "PresetValueSnapshot"
    - from: "MainWindowViewModel.cs"
      to: "UndoRedoService.cs"
      via: "prune status event"
      pattern: "Undo history trimmed"
---

<objective>
Bound undo history and harden template/profile undo snapshots.

Purpose: Users can safely undo high-risk preset/profile operations without live-reference corruption or unbounded history growth per D-13 through D-15.
Output: Bounded undo service, pruning status, snapshot helpers, and template/profile undo tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-CONTEXT.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-RESEARCH.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-UI-SPEC.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@openspec/specs/reactive-mvvm-conventions/spec.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-01-SUMMARY.md

<interfaces>
Current UndoRedoService:
```csharp
public sealed class UndoRedoService
{
    public bool IsReplaying { get; private set; }
    public bool CanUndo => undoStack.Count > 0;
    public bool CanRedo => redoStack.Count > 0;
    public event EventHandler? StateChanged;
    public void Record(string name, Action undo, Action redo);
    public bool Undo();
    public bool Redo();
    public void Clear();
}
```
Required UI-SPEC prune copy: `Undo history trimmed to keep large workflows responsive.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded undo history and pruning status event</name>
  <files>src/BS2BG.App/Services/UndoRedoService.cs, src/BS2BG.App/ViewModels/MainWindowViewModel.cs, tests/BS2BG.Tests/M6UxViewModelTests.cs</files>
  <behavior>
    - Recording more than the configured operation limit prunes the oldest undo entries deterministically.
    - Redo stack still clears on new record and replay guard still prevents nested recording.
    - Pruning raises a non-blocking notification consumed by shell status with exact UI-SPEC copy.
  </behavior>
  <action>Add an operation-count history limit to `UndoRedoService` (use a deterministic default such as 100 per research recommendation unless existing tests need constructor injection). Add an event or observable notification for pruning and route it to `MainWindowViewModel.StatusMessage` with `Undo history trimmed to keep large workflows responsive.` per D-15. Preserve current `StateChanged`, `CanUndo`, `CanRedo`, `Undo`, `Redo`, and `Clear` behavior.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~M6UxViewModelTests</automated>
  </verify>
  <done>Undo/redo history is bounded, redo semantics remain intact, and pruning is visible as non-blocking shell status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Snapshot-harden preset and profile undo paths</name>
  <files>src/BS2BG.App/ViewModels/Workflow/UndoSnapshots.cs, src/BS2BG.App/ViewModels/TemplatesViewModel.cs, tests/BS2BG.Tests/TemplatesViewModelTests.cs</files>
  <behavior>
    - Import, rename, duplicate/remove, clear presets, bulk slider percent changes, and profile selection changes restore values captured at operation time.
    - Interleaving rename → profile change → remove → undo restores the original preset values, not current mutated object references.
    - Project dirty state and preview refresh stay in sync after undo/redo.
  </behavior>
  <action>Create pure value snapshot helpers for preset/profile state (names, profile names, set slider min/max/value values, missing-default slider values, collection indexes). Replace risky template undo closures that capture mutable presets/set sliders with snapshot restore operations per D-13 and D-14. Do not edit sacred formatter/export files or golden expected fixtures.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~TemplatesViewModelTests</automated>
  </verify>
  <done>All high-risk preset/profile operations named by WORK-04 that live in Templates workflow use value snapshots and pass interleaving undo tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutable ViewModel project state → undo/redo replay | Stored undo callbacks replay after intervening mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | T | TemplatesViewModel undo records | mitigate | Capture values/indexes in snapshot DTOs instead of mutable live object graphs per D-13. |
| T-02-08-02 | D | UndoRedoService history | mitigate | Enforce bounded operation history and prune oldest entries with non-blocking status per D-15. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~TemplatesViewModelTests|FullyQualifiedName~M6UxViewModelTests"`.
</verification>

<success_criteria>
WORK-04 template/profile undo coverage is hardened with targeted value snapshots, and WORK-05 undo history remains bounded with visible pruning status.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-08-SUMMARY.md`
</output>
