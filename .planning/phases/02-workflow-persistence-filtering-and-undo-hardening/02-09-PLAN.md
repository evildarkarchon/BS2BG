---
phase: 02-workflow-persistence-filtering-and-undo-hardening
plan: 09
type: execute
wave: 5
depends_on: [07, 08]
files_modified:
  - src/BS2BG.App/ViewModels/Workflow/UndoSnapshots.cs
  - src/BS2BG.App/ViewModels/MorphsViewModel.cs
  - tests/BS2BG.Tests/MorphsViewModelTests.cs
  - tests/BS2BG.Tests/M6UxViewModelTests.cs
autonomous: true
requirements: [WORK-03, WORK-04, WORK-05]
must_haves:
  truths:
    - "NPC assignment, import, clear, target, and scoped bulk operations undo/redo from targeted value snapshots."
    - "Each bulk operation is one undoable user action even when it changes many rows."
    - "Hidden filtered rows remain untouched by scoped operations and remain correctly restored by undo/redo."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/MorphsViewModel.cs"
      provides: "Snapshot-hardened morph/NPC undo paths"
      contains: "NpcAssignmentSnapshot"
    - path: "src/BS2BG.App/ViewModels/Workflow/UndoSnapshots.cs"
      provides: "NPC/target assignment snapshot DTOs"
    - path: "tests/BS2BG.Tests/MorphsViewModelTests.cs"
      provides: "Interleaving and scoped bulk undo tests"
  key_links:
    - from: "MorphsViewModel.cs"
      to: "NpcBulkScopeResolver.cs"
      via: "snapshot scoped target IDs before mutation and undo"
      pattern: "VisibleEmpty"
    - from: "MorphsViewModel.cs"
      to: "UndoRedoService.cs"
      via: "one Record call per bulk operation"
      pattern: "undoRedo.Record"
---

<objective>
Harden morph/NPC undo and verify scoped bulk operations as single undoable actions.

Purpose: Users can undo/redo risky NPC, target, import, clear, assignment, and scoped bulk operations without mutable live-state corruption per D-13 through D-16.
Output: Morph snapshot helpers, ViewModel undo rewiring, and focused interleaving/scoped tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-CONTEXT.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-07-SUMMARY.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-08-SUMMARY.md

<interfaces>
Plan 07 provides scope state and `NpcBulkScopeResolver`; Plan 08 provides bounded `UndoRedoService` and shared snapshot helper file.

Current Core NPC output contract from `Npc.ToMorphLine()` must remain unchanged: `Mod|FormId=Preset1|Preset2`. Do not change morph generation/export semantics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Snapshot-harden target and NPC row operations</name>
  <files>src/BS2BG.App/ViewModels/Workflow/UndoSnapshots.cs, src/BS2BG.App/ViewModels/MorphsViewModel.cs, tests/BS2BG.Tests/MorphsViewModelTests.cs</files>
  <behavior>
    - Custom target add/remove/clear and target preset assignment/removal/clear undo by value snapshot and collection index.
    - NPC import/add/remove/clear undo by stable row ID plus value snapshot, not by mutable live references alone.
    - Interleavings such as import → assign → remove → undo restore the operation-time values.
  </behavior>
  <action>Add NPC and target snapshot DTOs/helpers for mod, name, editor ID, race, form ID, assigned preset names, target names, and collection indexes. Rework Morphs ViewModel undo records for target operations, NPC import/add/remove/clear, and assignment operations to restore from snapshots per D-13/D-14. Preserve existing Core `MorphAssignmentService` behavior and output formatting.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~MorphsViewModelTests</automated>
  </verify>
  <done>High-risk target/NPC row operations named by WORK-04 no longer depend on unsafe live-reference captures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Snapshot-harden scoped assignment and clear bulk operations</name>
  <files>src/BS2BG.App/ViewModels/MorphsViewModel.cs, tests/BS2BG.Tests/MorphsViewModelTests.cs, tests/BS2BG.Tests/M6UxViewModelTests.cs</files>
  <behavior>
    - Fill Visible Empty, clear assignments, assign selected, clear selected assignments, remove/clear scoped rows, and import-add operations record exactly one undo entry per user action per D-16.
    - Undo/redo restores only the materialized scoped target IDs and does not touch hidden rows excluded by scope.
    - Bounded history pruning still works after repeated large scoped bulk operations.
  </behavior>
  <action>Update all scoped bulk operations from Plan 07 to snapshot target row IDs and before/after assignment values before mutation. Call `undoRedo.Record` once per bulk command, with undo/redo restoring the scoped rows by stable ID and explicit missing-row behavior (skip rows that no longer exist, with status if appropriate). Add tests that count undo steps and assert hidden excluded rows are unchanged before and after undo/redo.</action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~MorphsViewModelTests|FullyQualifiedName~M6UxViewModelTests"</automated>
  </verify>
  <done>All morph/NPC bulk operations are one undoable action, scoped by stable IDs, and protected against live-state corruption and unbounded history growth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scoped row IDs/value snapshots → undo replay | Undo replay applies historical user actions to current mutable project state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09-01 | T | MorphsViewModel scoped undo | mitigate | Snapshot row IDs and before/after values before mutation; resolve current rows by ID at replay. |
| T-02-09-02 | D | Large bulk undo records | mitigate | Keep one undo entry per bulk operation and rely on bounded history from Plan 08. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~MorphsViewModelTests|FullyQualifiedName~M6UxViewModelTests"`, then `dotnet test` for the phase gate.
</verification>

<success_criteria>
WORK-03 and WORK-04 are jointly satisfied for morph/NPC workflows: scoped mutations affect only intended rows and undo/redo restores risky operations from targeted value snapshots with bounded history.
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-09-SUMMARY.md`
</output>
