---
phase: 03-validation-and-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BS2BG.Core/Diagnostics/DiagnosticSeverity.cs
  - src/BS2BG.Core/Diagnostics/DiagnosticFinding.cs
  - src/BS2BG.Core/Diagnostics/ProjectValidationReport.cs
  - src/BS2BG.Core/Diagnostics/ProjectValidationService.cs
  - src/BS2BG.Core/Diagnostics/ProfileDiagnosticsService.cs
  - tests/BS2BG.Tests/ProjectValidationServiceTests.cs
  - tests/BS2BG.Tests/ProfileDiagnosticsServiceTests.cs
autonomous: true
requirements: [DIAG-01, DIAG-02]
must_haves:
  truths:
    - "User can run a read-only project validation report grouped by Project, Profiles, Templates, Morphs/NPCs, Import, and Export."
    - "User can inspect profile diagnostics for coverage, unknown sliders, injected defaults, multipliers, inversions, and neutral fallback state."
    - "Unbundled saved profile diagnostics are informational and do not add mismatch scoring or normal-workflow warnings."
  artifacts:
    - path: "src/BS2BG.Core/Diagnostics/ProjectValidationService.cs"
      provides: "Read-only project health findings"
      exports: ["ProjectValidationService"]
    - path: "src/BS2BG.Core/Diagnostics/ProfileDiagnosticsService.cs"
      provides: "Whole-project and selected-preset profile diagnostic facts"
      exports: ["ProfileDiagnosticsService"]
    - path: "tests/BS2BG.Tests/ProjectValidationServiceTests.cs"
      provides: "DIAG-01 unit coverage"
    - path: "tests/BS2BG.Tests/ProfileDiagnosticsServiceTests.cs"
      provides: "DIAG-02 unit coverage"
  key_links:
    - from: "ProjectValidationService"
      to: "ProjectModel"
      via: "read-only validation of current collections"
      pattern: "Validate\(ProjectModel project"
    - from: "ProfileDiagnosticsService"
      to: "TemplateProfileCatalog.ContainsProfile/GetProfile"
      via: "neutral fallback and bundled table lookup"
      pattern: "ContainsProfile|GetProfile"
---

<objective>
Create the Core diagnostics foundation for project health and profile behavior.

Purpose: Phase 3 diagnostics must be reusable, UI-free, and read-only before App presentation work begins.
Output: Core diagnostic DTOs/services plus focused tests for DIAG-01 and DIAG-02.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-validation-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-validation-and-diagnostics/03-RESEARCH.md
@.planning/phases/03-validation-and-diagnostics/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@src/BS2BG.Core/Models/ProjectModel.cs
@src/BS2BG.Core/Models/SliderPreset.cs
@src/BS2BG.Core/Generation/TemplateProfileCatalog.cs
@src/BS2BG.Core/Generation/TemplateGenerationService.cs
@src/BS2BG.App/ViewModels/TemplatesViewModel.cs

<interfaces>
From TemplateProfileCatalog.cs:
```csharp
public bool ContainsProfile(string? name);
public TemplateProfile GetProfile(string? name);
public TemplateProfile DefaultProfile { get; }
public IReadOnlyList<string> ProfileNames { get; }
```
From TemplateGenerationService.cs:
```csharp
public string PreviewSetSlider(ModelSetSlider slider, TemplateProfile profile);
public string PreviewTemplate(ModelPreset preset, TemplateProfile profile, bool omitRedundantSliders);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add diagnostic contracts and read-only project validation</name>
  <files>src/BS2BG.Core/Diagnostics/DiagnosticSeverity.cs, src/BS2BG.Core/Diagnostics/DiagnosticFinding.cs, src/BS2BG.Core/Diagnostics/ProjectValidationReport.cs, src/BS2BG.Core/Diagnostics/ProjectValidationService.cs, tests/BS2BG.Tests/ProjectValidationServiceTests.cs</files>
  <read_first>src/BS2BG.Core/Models/ProjectModel.cs; src/BS2BG.Core/Models/SliderPreset.cs; src/BS2BG.Core/Models/CustomMorphTarget.cs; src/BS2BG.Core/Models/Npc.cs; tests/BS2BG.Tests/MorphCoreTests.cs</read_first>
  <behavior>
    - Empty project returns findings grouped under Project/Templates/Morphs/NPCs/Export without changing ProjectModel.ChangeVersion or IsDirty.
    - Findings use severity enum values exactly Blocker, Caution, Info per D-02.
    - Areas are exact strings: Project, Profiles, Templates, Morphs/NPCs, Import, Export per D-03.
  </behavior>
  <action>Create `BS2BG.Core.Diagnostics` with `DiagnosticSeverity { Blocker, Caution, Info }`, immutable `DiagnosticFinding` carrying `Severity`, `Area`, `Title`, `Detail`, `TargetKey`, and optional `ActionHint`, and `ProjectValidationReport` exposing `IReadOnlyList<DiagnosticFinding> Findings` plus severity counts. Add `ProjectValidationService.Validate(ProjectModel project, TemplateProfileCatalog profileCatalog)` that reads only project collections and emits readiness findings for: no presets (Templates Blocker), no morph targets/NPC assignments (Morphs/NPCs Info), empty preset names or missing preset references (Blocker), unbundled profile references as Profiles Info, and export readiness summary. Reference D-01 through D-04 and DIAG-01 in test names or comments where useful. Do not mutate project state, call MarkDirty, or add auto-fix actions per D-04.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~ProjectValidationServiceTests -x</automated>
  </verify>
  <acceptance_criteria>`ProjectValidationService.cs` contains `Validate(ProjectModel project, TemplateProfileCatalog profileCatalog)`; `DiagnosticSeverity.cs` contains `Blocker`, `Caution`, and `Info`; `ProjectValidationServiceTests.cs` asserts `project.IsDirty.Should().BeFalse()` after validation.</acceptance_criteria>
  <done>Project validation returns grouped, severity-coded, read-only findings for DIAG-01 without UI dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add profile diagnostic summary and drilldown facts</name>
  <files>src/BS2BG.Core/Diagnostics/ProfileDiagnosticsService.cs, src/BS2BG.Core/Diagnostics/ProjectValidationService.cs, tests/BS2BG.Tests/ProfileDiagnosticsServiceTests.cs, tests/BS2BG.Tests/ProjectValidationServiceTests.cs</files>
  <read_first>src/BS2BG.Core/Generation/TemplateProfileCatalog.cs; src/BS2BG.Core/Generation/TemplateProfile.cs; src/BS2BG.Core/Formatting/SliderProfile.cs; src/BS2BG.Core/Models/SetSlider.cs; tests/BS2BG.Tests/TemplatesViewModelTests.cs</read_first>
  <behavior>
    - Whole-project diagnostics include coverage counts, unknown slider counts, injected default counts, multiplier count, inversion count, fallback state, and affected preset count per D-07/D-09.
    - Unbundled saved profiles produce Info severity with saved profile name and calculation fallback profile per D-08.
    - No finding title/detail/action contains `mismatch`, `score`, `heuristic`, or `experimental` per D-06.
  </behavior>
  <action>Create immutable profile diagnostic DTOs in `ProfileDiagnosticsService.cs`: `ProfileDiagnosticsReport`, `ProfileDiagnosticsSummary`, and `ProfileSliderDiagnostic`. Implement `Analyze(ProjectModel project, TemplateProfileCatalog catalog, string? selectedPresetName = null)` with whole-project default scope and optional selected-preset filter. Compute known slider names from each preset's `SetSliders` and the calculation profile's defaults/multipliers/inverted tables; classify unknown sliders by absence from the active profile tables; count injected defaults by profile defaults absent from the preset; include multiplier/inversion counts from profile tables. Use `catalog.ContainsProfile` to identify unbundled saved profile names and `catalog.GetProfile` for neutral fallback profile details. Integrate concise profile findings into `ProjectValidationService` without adding normal Templates workflow warnings.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~ProfileDiagnosticsServiceTests -x</automated>
  </verify>
  <acceptance_criteria>`ProfileDiagnosticsServiceTests.cs` contains assertions for `Saved profile`, `calculation fallback`, `Info`, and negative assertions for `mismatch`; `ProfileDiagnosticsService.cs` contains `Analyze(ProjectModel project, TemplateProfileCatalog catalog`.</acceptance_criteria>
  <done>Profile diagnostics satisfy DIAG-02 as narrowed by D-06 while preserving Phase 1 neutral fallback semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProjectModel→Diagnostics | Local project data may contain malformed names, missing references, or unbundled profiles. |
| Profile catalog→Diagnostics | Bundled/fallback profile tables drive read-only calculations that must not alter generation semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T | ProjectValidationService | mitigate | Tests assert validation does not mutate `ProjectModel.IsDirty` or `ChangeVersion`; service reads collections only. |
| T-03-01-02 | I | ProfileDiagnosticsService | mitigate | Use neutral fallback Info details only; tests reject mismatch/scoring/experimental strings per D-06/D-08. |
| T-03-01-03 | D | ProfileDiagnosticsService | accept | Computation is in-memory over existing project collections; large-input stress is covered later through ViewModel/full-suite validation. |
</threat_model>

<verification>
Run focused tests after each task, then `dotnet test --filter "FullyQualifiedName~ProjectValidationServiceTests|FullyQualifiedName~ProfileDiagnosticsServiceTests" -x`.
</verification>

<success_criteria>
Core diagnostics services exist, are UI-free, have XML doc comments on public APIs, and tests prove read-only grouped project/profile diagnostics with no mismatch scoring.
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation-and-diagnostics/03-01-SUMMARY.md`.
</output>
