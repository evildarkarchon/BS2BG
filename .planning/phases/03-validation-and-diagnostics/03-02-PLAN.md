---
phase: 03-validation-and-diagnostics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BS2BG.Core/Import/NpcImportResult.cs
  - src/BS2BG.Core/Import/NpcTextParser.cs
  - src/BS2BG.Core/Import/NpcImportPreviewResult.cs
  - src/BS2BG.Core/Import/NpcImportPreviewService.cs
  - tests/BS2BG.Tests/NpcImportPreviewServiceTests.cs
autonomous: true
requirements: [DIAG-03]
must_haves:
  truths:
    - "User can preview NPC import rows, invalid lines, duplicates, fallback decoding, and rows that would be added before committing."
    - "NPC import preview does not mutate the project or NPC database."
    - "Preview distinguishes within-file duplicates from existing database/project duplicates where determinable."
  artifacts:
    - path: "src/BS2BG.Core/Import/NpcImportPreviewService.cs"
      provides: "No-mutation NPC import preview classification"
      exports: ["NpcImportPreviewService"]
    - path: "src/BS2BG.Core/Import/NpcImportPreviewResult.cs"
      provides: "Preview rows, summary, duplicate, invalid-line, and fallback facts"
    - path: "tests/BS2BG.Tests/NpcImportPreviewServiceTests.cs"
      provides: "DIAG-03 Core preview coverage"
  key_links:
    - from: "NpcImportPreviewService"
      to: "NpcTextParser.ParseFile/ParseText"
      via: "parser result wrapping"
      pattern: "Parse(File|Text)"
    - from: "NpcTextParser"
      to: "NpcImportDiagnostic"
      via: "within-file duplicate diagnostics"
      pattern: "Duplicate NPC row"
---

<objective>
Create Core NPC import preview facts without changing the direct import workflow.

Purpose: Phase 3 needs a temporary preview result that users can inspect before committing NPC rows to the database.
Output: Parser duplicate diagnostics, preview DTOs/service, and unit tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validation-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-validation-and-diagnostics/03-RESEARCH.md
@.planning/phases/03-validation-and-diagnostics/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/BS2BG.Core/Import/NpcTextParser.cs
@src/BS2BG.Core/Import/NpcImportResult.cs
@src/BS2BG.Core/Models/Npc.cs
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-04-SUMMARY.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-05-SUMMARY.md

<interfaces>
From NpcTextParser.cs:
```csharp
public NpcImportResult ParseFile(string path);
public NpcImportResult ParseText(string text);
```
From NpcImportResult.cs:
```csharp
public IReadOnlyList<Npc> Npcs { get; }
public IReadOnlyList<NpcImportDiagnostic> Diagnostics { get; }
public bool UsedFallbackEncoding { get; }
public string EncodingName { get; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve duplicate diagnostics in NPC parsing</name>
  <files>src/BS2BG.Core/Import/NpcImportResult.cs, src/BS2BG.Core/Import/NpcTextParser.cs, tests/BS2BG.Tests/NpcImportPreviewServiceTests.cs</files>
  <read_first>src/BS2BG.Core/Import/NpcTextParser.cs; src/BS2BG.Core/Import/NpcImportResult.cs; tests/BS2BG.Tests/MorphsViewModelTests.cs</read_first>
  <behavior>
    - Duplicate `Mod|EditorID` rows within the same file are skipped as before but now emit a row-level diagnostic explaining the duplicate per D-12.
    - Existing invalid-line diagnostics and fallback encoding fields remain unchanged.
    - Direct import policy remains skip duplicates by default.
  </behavior>
  <action>Extend `NpcImportResult` if needed with a duplicate-detail collection, or use existing `NpcImportDiagnostic` entries, so `NpcTextParser.ParseDecodedText` records within-file duplicate line numbers instead of silently continuing at the current `if (!seen.Add(key)) continue;` branch. Diagnostic message must include the exact phrase `Duplicate NPC row skipped` plus the duplicate key in `Mod|EditorID` form. Preserve the current parsed `Npcs` list behavior: the first unique row is kept, later duplicates are skipped. Add tests before implementation proving the diagnostic appears and parsed NPC count remains one for duplicate rows.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~NpcImportPreviewServiceTests -x</automated>
  </verify>
  <acceptance_criteria>`NpcTextParser.cs` contains `Duplicate NPC row skipped`; test asserts `result.Npcs.Should().ContainSingle()` and `result.Diagnostics.Should().Contain(d => d.Message.Contains("Duplicate NPC row skipped"))`.</acceptance_criteria>
  <done>Parser keeps default duplicate-skip policy while exposing duplicate reasons for preview.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add no-mutation NPC import preview service</name>
  <files>src/BS2BG.Core/Import/NpcImportPreviewResult.cs, src/BS2BG.Core/Import/NpcImportPreviewService.cs, tests/BS2BG.Tests/NpcImportPreviewServiceTests.cs</files>
  <read_first>src/BS2BG.Core/Models/Npc.cs; src/BS2BG.Core/Import/NpcTextParser.cs; src/BS2BG.Core/Import/NpcImportResult.cs; tests/BS2BG.Tests/MorphsViewModelTests.cs</read_first>
  <behavior>
    - Preview result includes parsed rows, invalid-line diagnostics, within-file duplicates, existing duplicates, fallback encoding name, rows that would be added, and summary counts per D-11/D-12/D-14.
    - Preview accepts existing NPCs and classifies duplicates against existing database/project rows by case-insensitive `(Mod, EditorId)`.
    - Preview never mutates the passed existing NPC collection.
  </behavior>
  <action>Create `NpcImportPreviewResult` and `NpcImportPreviewService`. Expose `PreviewText(string displayPath, string text, IReadOnlyCollection<Npc> existingNpcs)` and `PreviewFile(string path, IReadOnlyCollection<Npc> existingNpcs)`; both return a result with `SourcePath`, `ParsedRows`, `RowsToAdd`, `ExistingDuplicates`, `Diagnostics`, `UsedFallbackEncoding`, `EncodingName`, and summary count properties. For file preview, call `NpcTextParser.ParseFile(path)`; for text preview, call `ParseText(text)`. Existing duplicate classification must not alter parser duplicate policy and must not call any App-layer `AddNpcsToDatabase` path. Add XML doc comments explaining that preview is read-only and commit remains the caller's responsibility per D-10/D-13.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~NpcImportPreviewServiceTests -x</automated>
  </verify>
  <acceptance_criteria>`NpcImportPreviewService.cs` contains `PreviewFile(` and `PreviewText(`; `NpcImportPreviewResult.cs` contains `RowsToAdd`, `ExistingDuplicates`, `UsedFallbackEncoding`, and `EncodingName`; tests assert existing collection count is unchanged after preview.</acceptance_criteria>
  <done>Core preview service can describe NPC import effects without project/database mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NPC text file→parser | User-selected local text can be malformed, duplicate, huge, or non-UTF-8. |
| Parser result→preview | Preview classifies rows against existing state before commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | T | NpcImportPreviewService | mitigate | Tests assert preview does not mutate existing NPC collections and exposes rows-to-add separately. |
| T-03-02-02 | I | NpcTextParser | mitigate | Duplicate and fallback diagnostics include line/source facts so skipped rows are not silent. |
| T-03-02-03 | D | NpcTextParser | accept | Whole-file parsing remains existing project behavior; no new unbounded algorithm beyond duplicate classification. |
</threat_model>

<verification>
Run `dotnet test --filter FullyQualifiedName~NpcImportPreviewServiceTests -x` after both tasks and confirm existing `MorphsViewModelTests` still pass if parser diagnostics affect import status counts.
</verification>

<success_criteria>
NPC preview Core APIs exist, parse/classify rows read-only, explain duplicates and fallback decoding, and preserve direct import's default skip-duplicate policy.
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation-and-diagnostics/03-02-SUMMARY.md`.
</output>
