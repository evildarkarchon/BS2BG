---
phase: 03-validation-and-diagnostics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BS2BG.Core/Diagnostics/ExportPreviewService.cs
  - src/BS2BG.Core/Diagnostics/ExportPreviewResult.cs
  - src/BS2BG.Core/IO/WriteOutcomeLedger.cs
  - src/BS2BG.Core/IO/AtomicWriteException.cs
  - src/BS2BG.Core/IO/AtomicFileWriter.cs
  - tests/BS2BG.Tests/ExportPreviewServiceTests.cs
  - tests/BS2BG.Tests/AtomicFileWriterOutcomeTests.cs
autonomous: true
requirements: [DIAG-04, DIAG-05]
must_haves:
  truths:
    - "User can preview BodyGen and BoS export target paths, create/overwrite state, snippets, and batch risk before disk writes."
    - "Export preview uses existing generation output and writer path rules without changing byte-sensitive content semantics."
    - "File operation failures can carry written, restored, skipped, left-untouched, and incomplete states."
  artifacts:
    - path: "src/BS2BG.Core/Diagnostics/ExportPreviewService.cs"
      provides: "Read-only export preview facts"
      exports: ["ExportPreviewService"]
    - path: "src/BS2BG.Core/IO/WriteOutcomeLedger.cs"
      provides: "Per-file outcome states"
      exports: ["FileWriteOutcome", "FileWriteLedgerEntry", "WriteOutcomeLedger"]
    - path: "src/BS2BG.Core/IO/AtomicWriteException.cs"
      provides: "Exception carrying outcome ledger"
      exports: ["AtomicWriteException"]
  key_links:
    - from: "ExportPreviewService"
      to: "TemplateGenerationService/MorphGenerationService"
      via: "generated snippets from real services"
      pattern: "GenerateTemplates|GenerateMorphs|PreviewBosJson"
    - from: "AtomicFileWriter"
      to: "AtomicWriteException"
      via: "ledger on commit/rollback failure"
      pattern: "AtomicWriteException"
---

<objective>
Create export preview and file outcome ledger Core contracts.

Purpose: Users need exact export risk previews and actionable failure outcomes without changing parity-sensitive writer output.
Output: Export preview DTO/service, atomic outcome ledger, and focused tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-validation-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-validation-and-diagnostics/03-RESEARCH.md
@.planning/phases/03-validation-and-diagnostics/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/BS2BG.Core/Generation/TemplateGenerationService.cs
@src/BS2BG.Core/Generation/MorphGenerationService.cs
@src/BS2BG.Core/Export/BodyGenIniExportWriter.cs
@src/BS2BG.Core/Export/BosJsonExportWriter.cs
@src/BS2BG.Core/IO/AtomicFileWriter.cs

<interfaces>
From BodyGenIniExportWriter.cs:
```csharp
public BodyGenIniExportResult Write(string directoryPath, string templatesText, string morphsText);
```
From BosJsonExportWriter.cs:
```csharp
public virtual BosJsonExportResult Write(string directoryPath, IEnumerable<SliderPreset> presets, TemplateProfileCatalog profileCatalog);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add export preview service without writer formatting changes</name>
  <files>src/BS2BG.Core/Diagnostics/ExportPreviewService.cs, src/BS2BG.Core/Diagnostics/ExportPreviewResult.cs, tests/BS2BG.Tests/ExportPreviewServiceTests.cs</files>
  <read_first>src/BS2BG.Core/Generation/TemplateGenerationService.cs; src/BS2BG.Core/Generation/MorphGenerationService.cs; src/BS2BG.Core/Export/BodyGenIniExportWriter.cs; src/BS2BG.Core/Export/BosJsonExportWriter.cs; tests/BS2BG.Tests/ExportWriterTests.cs</read_first>
  <behavior>
    - BodyGen preview includes `templates.ini` and `morphs.ini` paths, create/overwrite flags, first 3 non-empty lines per file, and batch-risk flag per D-15/D-16.
    - BoS preview includes sanitized unique JSON paths matching writer naming behavior, create/overwrite flags, first 3 non-empty JSON lines per preset, and batch-risk flag.
    - Preview does not write files and does not modify `BodyGenIniExportWriter`, `BosJsonExportWriter`, `SliderMathFormatter`, or golden expected fixtures.
  </behavior>
  <action>Create `ExportPreviewResult`, `ExportPreviewFile`, and `ExportPreviewService` under `BS2BG.Core.Diagnostics`. Implement `PreviewBodyGen(string directoryPath, string templatesText, string morphsText)` and `PreviewBosJson(string directoryPath, IEnumerable<SliderPreset> presets, TemplateProfileCatalog profileCatalog)`. Use exact target filenames: `templates.ini`, `morphs.ini`, and BoS JSON sanitized/unique filenames equivalent to `BosJsonExportWriter` rules. Generate snippets by splitting existing generated text and `TemplateGenerationService.PreviewBosJson` output; default snippet length is first 3 non-empty lines per D-15 and UI-SPEC. Do not call `AtomicFileWriter`, `BodyGenIniExportWriter.Write`, or `BosJsonExportWriter.Write` in preview. Add XML doc comments noting the preview intentionally wraps generation/path rules and performs no writes.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~ExportPreviewServiceTests -x</automated>
  </verify>
  <acceptance_criteria>`ExportPreviewService.cs` contains `PreviewBodyGen(` and `PreviewBosJson(`; tests assert existing files set `WillOverwrite` true and new paths set `WillOverwrite` false; tests assert no output file is created by preview.</acceptance_criteria>
  <done>DIAG-04 Core preview facts are available without byte-sensitive writer output changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add atomic write outcome ledger for failures</name>
  <files>src/BS2BG.Core/IO/WriteOutcomeLedger.cs, src/BS2BG.Core/IO/AtomicWriteException.cs, src/BS2BG.Core/IO/AtomicFileWriter.cs, tests/BS2BG.Tests/AtomicFileWriterOutcomeTests.cs</files>
  <read_first>src/BS2BG.Core/IO/AtomicFileWriter.cs; tests/BS2BG.Tests/ExportWriterTests.cs; .planning/codebase/CONCERNS.md</read_first>
  <behavior>
    - Atomic batch failures expose entries with outcomes exactly Planned, Written, Restored, Skipped, LeftUntouched, Incomplete per D-17.
    - Rollback failures do not claim success; affected entries are Incomplete and exception details preserve original commit and rollback exceptions.
    - Existing `WriteAtomic`, `WriteAtomicPair`, and `WriteAtomicBatch` signatures remain callable for compatibility.
  </behavior>
  <action>Add `FileWriteOutcome` enum, immutable `FileWriteLedgerEntry`, `WriteOutcomeLedger`, and `AtomicWriteException : IOException` carrying `IReadOnlyList<FileWriteLedgerEntry> Entries`. Update `AtomicFileWriter.WriteAtomicBatch` internals to maintain ledger state: planned before temp writes, written after commit, restored after successful rollback of an overwritten file, left untouched for entries not committed when a later failure occurs, skipped for entries never attempted because an earlier setup/temp-write failure occurred, and incomplete when rollback fails or state is unknown. Keep public void write methods as wrappers; throw `AtomicWriteException` on commit/rollback failures where ledger can be built. Preserve existing temp/backup delete behavior; add comments explaining empty catch blocks if touched, per project comment policy.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~AtomicFileWriterOutcomeTests -x</automated>
  </verify>
  <acceptance_criteria>`WriteOutcomeLedger.cs` contains enum members `Written`, `Restored`, `Skipped`, `LeftUntouched`, and `Incomplete`; `AtomicWriteException.cs` exposes `IReadOnlyList<FileWriteLedgerEntry>`; existing `AtomicFileWriter.WriteAtomicPair(` remains present.</acceptance_criteria>
  <done>DIAG-05 has a Core outcome ledger for actionable save/export failure reporting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project/generated output→preview | Generated text and target paths are displayed before writes. |
| Filesystem→atomic writer | Locks, permissions, and missing directories can interrupt multi-file commits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T | ExportPreviewService | mitigate | Preview tests assert no files are written and snippets come from existing generation services. |
| T-03-03-02 | I | ExportPreviewService | mitigate | Exact target paths and create/overwrite flags are included before risky writes per D-15. |
| T-03-03-03 | T | AtomicFileWriter | mitigate | Outcome ledger states distinguish written/restored/skipped/left-untouched/incomplete and preserve exceptions. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~ExportPreviewServiceTests|FullyQualifiedName~AtomicFileWriterOutcomeTests" -x`, then `dotnet test --filter FullyQualifiedName~ExportWriterTests -x` to guard existing writer behavior.
</verification>

<success_criteria>
Export previews are read-only and output-ledger failures are testable without changing formatter/export byte semantics or sacred expected fixtures.
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation-and-diagnostics/03-03-SUMMARY.md`.
</output>
