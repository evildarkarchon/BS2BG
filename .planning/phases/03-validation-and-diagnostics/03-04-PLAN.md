---
phase: 03-validation-and-diagnostics
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/BS2BG.App/ViewModels/DiagnosticsViewModel.cs
  - src/BS2BG.App/ViewModels/DiagnosticFindingViewModel.cs
  - src/BS2BG.App/Services/DiagnosticsReportFormatter.cs
  - src/BS2BG.App/AppBootstrapper.cs
  - tests/BS2BG.Tests/DiagnosticsViewModelTests.cs
autonomous: true
requirements: [DIAG-01, DIAG-02]
must_haves:
  truths:
    - "User can refresh Diagnostics report state from the App layer without mutating project data."
    - "User can copy a plain-text grouped diagnostics report."
    - "Diagnostics ViewModel exposes severity counts, grouped findings, profile drilldown, and navigation intent surfaces."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/DiagnosticsViewModel.cs"
      provides: "ReactiveUI Diagnostics tab presentation state"
      exports: ["DiagnosticsViewModel"]
    - path: "src/BS2BG.App/Services/DiagnosticsReportFormatter.cs"
      provides: "Plain-text copyable report formatting"
      exports: ["DiagnosticsReportFormatter"]
    - path: "tests/BS2BG.Tests/DiagnosticsViewModelTests.cs"
      provides: "App diagnostics ViewModel coverage"
  key_links:
    - from: "DiagnosticsViewModel"
      to: "ProjectValidationService/ProfileDiagnosticsService"
      via: "RefreshDiagnosticsCommand"
      pattern: "RefreshDiagnosticsCommand"
    - from: "DiagnosticsViewModel"
      to: "IClipboardService"
      via: "CopyReportCommand"
      pattern: "SetTextAsync"
---

<objective>
Expose Core project/profile diagnostics through an App-layer ReactiveUI ViewModel and report formatter.

Purpose: The Diagnostics UI needs a binding-ready presentation model before AXAML wiring.
Output: Diagnostics ViewModel, row ViewModels, copyable report formatter, DI registration, and tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-validation-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-validation-and-diagnostics/03-RESEARCH.md
@.planning/phases/03-validation-and-diagnostics/03-UI-SPEC.md
@.planning/phases/03-validation-and-diagnostics/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@openspec/specs/reactive-mvvm-conventions/spec.md
@src/BS2BG.App/AppBootstrapper.cs
@src/BS2BG.App/Services/IClipboardService.cs
@src/BS2BG.App/ViewModels/MainWindowViewModel.cs
@src/BS2BG.Core/Diagnostics/ProjectValidationService.cs
@src/BS2BG.Core/Diagnostics/ProfileDiagnosticsService.cs

<interfaces>
Expected from Plan 03-01:
```csharp
public sealed class ProjectValidationService { public ProjectValidationReport Validate(ProjectModel project, TemplateProfileCatalog profileCatalog); }
public sealed class ProfileDiagnosticsService { public ProfileDiagnosticsReport Analyze(ProjectModel project, TemplateProfileCatalog catalog, string? selectedPresetName = null); }
```
Existing App service:
```csharp
public interface IClipboardService { Task SetTextAsync(string text, CancellationToken cancellationToken); }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Diagnostics ViewModel refresh and grouping state</name>
  <files>src/BS2BG.App/ViewModels/DiagnosticsViewModel.cs, src/BS2BG.App/ViewModels/DiagnosticFindingViewModel.cs, src/BS2BG.App/AppBootstrapper.cs, tests/BS2BG.Tests/DiagnosticsViewModelTests.cs</files>
  <read_first>src/BS2BG.App/ViewModels/MainWindowViewModel.cs; src/BS2BG.App/AppBootstrapper.cs; src/BS2BG.Core/Diagnostics/ProjectValidationService.cs; src/BS2BG.Core/Diagnostics/ProfileDiagnosticsService.cs; tests/BS2BG.Tests/MainWindowViewModelTests.cs</read_first>
  <behavior>
    - `Run Diagnostics` refresh populates grouped findings and severity counts using Blocker/Caution/Info labels per D-01/D-03.
    - Refresh does not change `ProjectModel.IsDirty` or `ChangeVersion`.
    - ViewModel exposes selected finding/detail state and navigation-intent properties but no auto-fix command per D-04.
  </behavior>
  <action>Create `DiagnosticsViewModel : ReactiveObject, IDisposable` with constructor dependencies `ProjectModel`, `TemplateProfileCatalog`, `ProjectValidationService`, `ProfileDiagnosticsService`, and optional `IClipboardService`/formatter in Task 2. Expose `ReactiveCommand<Unit, Unit> RefreshDiagnosticsCommand`, `ObservableCollection<DiagnosticFindingViewModel> Findings`, `ObservableCollection<string> Areas`, `SelectedFinding`, `BlockerCount`, `CautionCount`, `InfoCount`, `StatusMessage`, `IsBusy`, and read-only summary text using UI-SPEC copy: `No blockers found. Review cautions and info before exporting.` and `{N} blocker(s) need attention before output is ready.`. Map Core severity to exact labels `Blocker`, `Caution`, `Info`; sort/group by Project, Profiles, Templates, Morphs/NPCs, Import, Export. Register `ProjectValidationService`, `ProfileDiagnosticsService`, and `DiagnosticsViewModel` in `AppBootstrapper`.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~DiagnosticsViewModelTests -x</automated>
  </verify>
  <acceptance_criteria>`DiagnosticsViewModel.cs` contains `RefreshDiagnosticsCommand`; `DiagnosticFindingViewModel.cs` contains `SeverityLabel`; `AppBootstrapper.cs` registers `DiagnosticsViewModel`; tests assert severity labels are exactly `Blocker`, `Caution`, `Info`.</acceptance_criteria>
  <done>Diagnostics ViewModel can refresh and expose grouped read-only findings for the future tab.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add copyable diagnostics report formatting</name>
  <files>src/BS2BG.App/Services/DiagnosticsReportFormatter.cs, src/BS2BG.App/ViewModels/DiagnosticsViewModel.cs, tests/BS2BG.Tests/DiagnosticsViewModelTests.cs</files>
  <read_first>src/BS2BG.App/Services/IClipboardService.cs; src/BS2BG.App/ViewModels/TemplatesViewModel.cs; tests/BS2BG.Tests/TemplatesViewModelTests.cs; .planning/phases/03-validation-and-diagnostics/03-UI-SPEC.md</read_first>
  <behavior>
    - Copy report writes grouped plain text containing severity, area, title, detail, profile fallback details, and no auto-fix actions per D-04.
    - Successful copy sets status text exactly `Diagnostics report copied to clipboard.` per UI-SPEC.
    - Empty report copy includes `No diagnostics yet` and does not throw.
  </behavior>
  <action>Create `DiagnosticsReportFormatter` in App services with `Format(IEnumerable<DiagnosticFindingViewModel> findings, DateTimeOffset generatedAt)` returning plain text sections grouped by area. Add `CopyReportCommand` to `DiagnosticsViewModel` using `ReactiveCommand.CreateFromTask((CancellationToken ct) => CopyReportAsync(ct), canExecute)` and `IClipboardService.SetTextAsync`. Include profile fallback details as text `Saved profile: {name}; calculation fallback: {fallbackProfile}. This is informational and does not block generation or export.` when present. Do not include `Fix`, `Auto-fix`, or mutation verbs. Subscribe `ThrownExceptions` to set `StatusMessage = "Diagnostics report copy failed: " + exception.Message`.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~DiagnosticsViewModelTests -x</automated>
  </verify>
  <acceptance_criteria>`DiagnosticsReportFormatter.cs` contains `## Project`, `## Profiles`, and `Blocker`; `DiagnosticsViewModel.cs` contains `CopyReportCommand`; tests assert clipboard text contains `Saved profile:` and status equals `Diagnostics report copied to clipboard.`.</acceptance_criteria>
  <done>Diagnostics report can be copied as actionable, grouped, read-only text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Core diagnostics→App ViewModel | Domain findings become user-visible copy and navigation metadata. |
| App ViewModel→Clipboard | Copyable report can include local paths/profile names selected by the user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | T | DiagnosticsViewModel | mitigate | Refresh command calls read-only services only; tests assert ProjectModel dirty state remains unchanged. |
| T-03-04-02 | I | DiagnosticsReportFormatter | accept | Report copies user-visible local project facts; no new private data source is added. |
| T-03-04-03 | R | CopyReportCommand | mitigate | Status message records successful copy or failure text for user feedback. |
</threat_model>

<verification>
Run `dotnet test --filter FullyQualifiedName~DiagnosticsViewModelTests -x` and ensure `dotnet build BS2BG.sln` catches ReactiveUI/source-generator issues.
</verification>

<success_criteria>
A registered Diagnostics ViewModel exposes report refresh, grouped findings, severity counts, selected detail, and copy report command following ReactiveUI conventions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation-and-diagnostics/03-04-SUMMARY.md`.
</output>
