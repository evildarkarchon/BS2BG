---
phase: 03-validation-and-diagnostics
plan: 05
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - src/BS2BG.App/ViewModels/MorphsViewModel.cs
  - src/BS2BG.App/ViewModels/Workflow/NpcImportPreviewViewModel.cs
  - src/BS2BG.App/AppBootstrapper.cs
  - tests/BS2BG.Tests/MorphsViewModelTests.cs
autonomous: true
requirements: [DIAG-03]
must_haves:
  truths:
    - "User can preview NPC import files optionally while direct import remains available."
    - "Previewed NPC rows are not added to the NPC database until Import Previewed NPCs is invoked."
    - "Import effects and assignment effects stay distinct in status/preview copy."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/Workflow/NpcImportPreviewViewModel.cs"
      provides: "Binding-ready NPC import preview rows and summary"
    - path: "src/BS2BG.App/ViewModels/MorphsViewModel.cs"
      provides: "Preview/commit commands and assignment effect counts"
    - path: "tests/BS2BG.Tests/MorphsViewModelTests.cs"
      provides: "No-mutation preview and commit coverage"
  key_links:
    - from: "MorphsViewModel"
      to: "NpcImportPreviewService"
      via: "PreviewNpcImportCommand"
      pattern: "PreviewNpcImport"
    - from: "MorphsViewModel"
      to: "AddNpcsToDatabase"
      via: "ImportPreviewedNpcsCommand only"
      pattern: "ImportPreviewedNpcs"
---

<objective>
Integrate optional NPC import preview into the Morphs workflow.

Purpose: Users need to inspect parsed rows, invalid lines, duplicates, and fallback encoding before committing imports, while preserving the existing direct import path.
Output: Morphs ViewModel preview/commit commands, preview row ViewModel, DI wiring, and tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-validation-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-validation-and-diagnostics/03-RESEARCH.md
@.planning/phases/03-validation-and-diagnostics/03-UI-SPEC.md
@.planning/phases/03-validation-and-diagnostics/03-VALIDATION.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-05-SUMMARY.md
@.planning/phases/02-workflow-persistence-filtering-and-undo-hardening/02-07-SUMMARY.md
@src/BS2BG.App/ViewModels/MorphsViewModel.cs
@src/BS2BG.App/ViewModels/Workflow/NpcRowViewModel.cs
@src/BS2BG.App/ViewModels/Workflow/NpcBulkScopeResolver.cs
@src/BS2BG.Core/Import/NpcImportPreviewService.cs

<interfaces>
Expected from Plan 03-02:
```csharp
public sealed class NpcImportPreviewService { public NpcImportPreviewResult PreviewFile(string path, IReadOnlyCollection<Npc> existingNpcs); }
public sealed class NpcImportPreviewResult { public IReadOnlyList<Npc> RowsToAdd { get; } public bool UsedFallbackEncoding { get; } public string EncodingName { get; } }
```
Existing Morphs path:
```csharp
private int AddNpcsToDatabase(IEnumerable<Npc> npcs);
private async Task ImportNpcFilesCoreAsync(IReadOnlyList<string> files, string emptyStatus, CancellationToken cancellationToken);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional preview command without mutation</name>
  <files>src/BS2BG.App/ViewModels/Workflow/NpcImportPreviewViewModel.cs, src/BS2BG.App/ViewModels/MorphsViewModel.cs, src/BS2BG.App/AppBootstrapper.cs, tests/BS2BG.Tests/MorphsViewModelTests.cs</files>
  <read_first>src/BS2BG.App/ViewModels/MorphsViewModel.cs; src/BS2BG.App/Services/INpcTextFilePicker.cs; src/BS2BG.Core/Import/NpcImportPreviewService.cs; tests/BS2BG.Tests/MorphsViewModelTests.cs</read_first>
  <behavior>
    - `PreviewNpcImportCommand` parses selected files through `NpcImportPreviewService` and populates preview summaries/rows.
    - Running preview does not change `NpcDatabase.Count`, `VisibleNpcDatabase.Count`, `MorphedNpcs.Count`, `project.IsDirty`, or undo history.
    - Existing `ImportNpcsCommand` remains available and keeps direct import behavior per D-10.
  </behavior>
  <action>Inject `NpcImportPreviewService` into `MorphsViewModel` with design-time fallback. Add `ReactiveCommand<Unit, Unit> PreviewNpcImportCommand`, `ObservableCollection<NpcImportPreviewViewModel> NpcImportPreviewRows`, `NpcImportPreviewSummary`, `HasNpcImportPreview`, and status copy `Choose one or more NPC text files to preview rows, duplicates, invalid lines, and encoding fallback before importing.` for empty preview state. Use the existing `INpcTextFilePicker` to choose files, but do not call `AddNpcsToDatabase` or record undo during preview. Preserve direct `ImportNpcsCommand` and `ImportNpcFilesAsync` behavior. Register `NpcImportPreviewService` in `AppBootstrapper`.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~MorphsViewModelTests -x</automated>
  </verify>
  <acceptance_criteria>`MorphsViewModel.cs` contains `PreviewNpcImportCommand`; tests assert preview leaves `NpcDatabase.Count` unchanged; `AppBootstrapper.cs` registers `NpcImportPreviewService`.</acceptance_criteria>
  <done>NPC import preview can run with no project/database mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit commit and assignment-effect separation</name>
  <files>src/BS2BG.App/ViewModels/MorphsViewModel.cs, src/BS2BG.App/ViewModels/Workflow/NpcImportPreviewViewModel.cs, tests/BS2BG.Tests/MorphsViewModelTests.cs</files>
  <read_first>src/BS2BG.App/ViewModels/MorphsViewModel.cs; src/BS2BG.App/ViewModels/Workflow/NpcBulkScopeResolver.cs; src/BS2BG.Core/Import/NpcImportPreviewResult.cs; .planning/phases/03-validation-and-diagnostics/03-UI-SPEC.md</read_first>
  <behavior>
    - `ImportPreviewedNpcsCommand` commits only `RowsToAdd` and reports added/skipped counts.
    - Preview copy states file import adds NPCs to the database only and does not assign presets per D-13.
    - Assignment-changing commands expose before/after counts for selected scope when they commit rows to morphs.
  </behavior>
  <action>Add `ReactiveCommand<Unit, Unit> ImportPreviewedNpcsCommand` enabled only when preview rows include rows to add. Commit through existing `AddNpcsToDatabase` so duplicate policy remains shared. After commit, clear preview rows or mark them committed and set status `Imported {N} previewed NPC(s) to the NPC database. Import preview does not assign presets.` Add assignment effect summary properties to `MorphsViewModel`, such as `LastAssignmentEffectSummary`, updated by `FillEmptyNpcsForScope`, `ClearNpcAssignmentsForScope`, and `ClearNpcsForScope` with before/after target counts using existing `NpcBulkScopeResolver`. Do not alter assignment algorithms or random-provider behavior.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~MorphsViewModelTests -x</automated>
  </verify>
  <acceptance_criteria>`MorphsViewModel.cs` contains `ImportPreviewedNpcsCommand` and `LastAssignmentEffectSummary`; tests assert preview commit increases `NpcDatabase.Count` only after command execution and status contains `does not assign presets`.</acceptance_criteria>
  <done>Preview commit and assignment-effect reporting satisfy DIAG-03 without conflating import with assignment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-selected NPC files→Morphs ViewModel | Preview parses untrusted local text before commit. |
| Preview result→ProjectModel | Rows cross into mutable NPC database only after explicit commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | T | PreviewNpcImportCommand | mitigate | Tests assert preview does not mutate NPC database/project dirty state or undo history. |
| T-03-05-02 | R | ImportPreviewedNpcsCommand | mitigate | Commit status includes counts and states that import does not assign presets. |
| T-03-05-03 | I | Fallback encoding status | mitigate | Preview row/status includes `File decoded with {encodingName}` caution per D-14. |
</threat_model>

<verification>
Run `dotnet test --filter FullyQualifiedName~MorphsViewModelTests -x` after each task; run `dotnet test --filter FullyQualifiedName~NpcImportPreviewServiceTests -x` if Core preview behavior is adjusted.
</verification>

<success_criteria>
Morphs workflow offers optional no-mutation preview, explicit commit, duplicate/fallback summaries, and distinct assignment-effect counts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation-and-diagnostics/03-05-SUMMARY.md`.
</output>
