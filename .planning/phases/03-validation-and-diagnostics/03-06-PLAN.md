---
phase: 03-validation-and-diagnostics
plan: 06
type: execute
wave: 2
depends_on: [03-03]
files_modified:
  - src/BS2BG.App/ViewModels/MainWindowViewModel.cs
  - src/BS2BG.App/ViewModels/Workflow/ExportPreviewViewModel.cs
  - src/BS2BG.App/Services/IAppDialogService.cs
  - src/BS2BG.App/Services/WindowAppDialogService.cs
  - tests/BS2BG.Tests/MainWindowViewModelTests.cs
autonomous: true
requirements: [DIAG-04]
must_haves:
  truths:
    - "User can preview export paths, create/overwrite state, snippets, and partial-output risk before risky writes."
    - "Routine create-new exports avoid overwrite confirmation friction."
    - "Overwrite or multi-file batch risk requires confirmation with UI-SPEC copy before writing."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/Workflow/ExportPreviewViewModel.cs"
      provides: "Binding-ready export preview files and summary"
    - path: "src/BS2BG.App/ViewModels/MainWindowViewModel.cs"
      provides: "Preview export commands and risk-gated export execution"
    - path: "src/BS2BG.App/Services/IAppDialogService.cs"
      provides: "Risk confirmation contract"
  key_links:
    - from: "MainWindowViewModel"
      to: "ExportPreviewService"
      via: "PreviewExportCommand or export preflight"
      pattern: "ExportPreviewService"
    - from: "MainWindowViewModel"
      to: "IAppDialogService"
      via: "ConfirmExportOverwriteAsync"
      pattern: "Overwrite existing output files"
---

<objective>
Wire export preview and overwrite-risk confirmation into shell export workflows.

Purpose: Users must see exact paths/effects before risky writes without adding confirmation to routine create-new exports.
Output: MainWindow export preview state/commands, dialog confirmation contract, and tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-validation-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-validation-and-diagnostics/03-RESEARCH.md
@.planning/phases/03-validation-and-diagnostics/03-UI-SPEC.md
@.planning/phases/03-validation-and-diagnostics/03-VALIDATION.md
@src/BS2BG.App/ViewModels/MainWindowViewModel.cs
@src/BS2BG.App/Services/IAppDialogService.cs
@src/BS2BG.App/Services/WindowAppDialogService.cs
@src/BS2BG.Core/Diagnostics/ExportPreviewService.cs
@src/BS2BG.Core/Export/BodyGenIniExportWriter.cs
@src/BS2BG.Core/Export/BosJsonExportWriter.cs

<interfaces>
Expected from Plan 03-03:
```csharp
public sealed class ExportPreviewService { public ExportPreviewResult PreviewBodyGen(...); public ExportPreviewResult PreviewBosJson(...); }
public sealed class ExportPreviewResult { public IReadOnlyList<ExportPreviewFile> Files { get; } public bool RequiresConfirmation { get; } }
```
Existing commands:
```csharp
public ReactiveCommand<Unit, Unit> ExportBosJsonCommand { get; }
public ReactiveCommand<Unit, Unit> ExportBodyGenInisCommand { get; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shell export preview state and commands</name>
  <files>src/BS2BG.App/ViewModels/Workflow/ExportPreviewViewModel.cs, src/BS2BG.App/ViewModels/MainWindowViewModel.cs, tests/BS2BG.Tests/MainWindowViewModelTests.cs</files>
  <read_first>src/BS2BG.App/ViewModels/MainWindowViewModel.cs; src/BS2BG.Core/Diagnostics/ExportPreviewService.cs; tests/BS2BG.Tests/MainWindowViewModelTests.cs</read_first>
  <behavior>
    - Preview BodyGen and BoS commands populate exact paths, create/overwrite labels, snippets, and summary copy per D-15.
    - Preview command does not write files.
    - Create-new preview summary uses exact UI-SPEC copy `New files will be created at the paths below. No overwrite confirmation is required.`
  </behavior>
  <action>Inject `ExportPreviewService` into `MainWindowViewModel` with design-time fallback. Add `ExportPreviewViewModel` containing `TargetPath`, `EffectLabel` (`Create` or `Overwrite`), `SnippetLines`, `IsOverwrite`, and `Kind`. Expose `ObservableCollection<ExportPreviewViewModel> ExportPreviewFiles`, `ExportPreviewSummary`, `HasExportPreview`, `PreviewBodyGenExportCommand`, and `PreviewBosJsonExportCommand`. Reuse the existing folder pickers to choose target directories. Preview BodyGen after calling `Templates.GenerateTemplates()` and `Morphs.GenerateMorphs()`; preview BoS from a cloned preset snapshot. Do not call writer `Write` methods in preview.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~MainWindowViewModelTests -x</automated>
  </verify>
  <acceptance_criteria>`MainWindowViewModel.cs` contains `PreviewBodyGenExportCommand` and `PreviewBosJsonExportCommand`; tests assert preview creates no `templates.ini`, `morphs.ini`, or `.json` files.</acceptance_criteria>
  <done>Shell exposes read-only export preview state for DIAG-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate risky exports with confirmation only when needed</name>
  <files>src/BS2BG.App/ViewModels/MainWindowViewModel.cs, src/BS2BG.App/Services/IAppDialogService.cs, src/BS2BG.App/Services/WindowAppDialogService.cs, tests/BS2BG.Tests/MainWindowViewModelTests.cs</files>
  <read_first>src/BS2BG.App/ViewModels/MainWindowViewModel.cs; src/BS2BG.App/Services/IAppDialogService.cs; src/BS2BG.App/Services/WindowAppDialogService.cs; .planning/phases/03-validation-and-diagnostics/03-UI-SPEC.md</read_first>
  <behavior>
    - Existing target files or multi-file partial-output risk trigger confirmation per D-16.
    - Routine create-new exports do not call confirmation.
    - Dialog copy matches UI-SPEC: title `Overwrite existing output files?`, primary `Export Anyway`, secondary `Keep Existing Files` where Avalonia APIs allow labels.
  </behavior>
  <action>Add `ConfirmExportOverwriteAsync(ExportPreviewResult preview, CancellationToken cancellationToken)` or equivalent to `IAppDialogService`/`WindowAppDialogService` using UI-SPEC body: `BS2BG will write the files listed below. Existing targets may be replaced; if a write fails, the result ledger will show what was written, restored, skipped, or left untouched.` Update `ExportBodyGenInisAsync` and `ExportBosJsonAsync` to compute preview before writing, store it in `ExportPreviewFiles`, and call confirmation only when preview indicates an overwrite or multi-file batch risk. If user cancels, set status `Export cancelled; existing files kept.` and do not write. Preserve writer calls and output content unchanged.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~MainWindowViewModelTests -x</automated>
  </verify>
  <acceptance_criteria>`IAppDialogService.cs` contains `ConfirmExportOverwriteAsync`; `MainWindowViewModelTests.cs` asserts create-new export confirmation count is 0 and overwrite export confirmation count is 1; `WindowAppDialogService.cs` contains `Overwrite existing output files?`.</acceptance_criteria>
  <done>Risk confirmation is applied only to overwrite/batch-risk exports and never to routine create-new exports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App export preview→filesystem write | Preview facts decide whether user confirmation is required before writer calls. |
| Dialog service→user decision | User approves or cancels risky overwrites. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | T | MainWindowViewModel export preview | mitigate | Tests assert preview performs no writes and writer calls happen only after command path proceeds. |
| T-03-06-02 | R | ConfirmExportOverwriteAsync | mitigate | Cancel path sets explicit status and prevents write; confirm path is test-covered. |
| T-03-06-03 | I | ExportPreviewViewModel | mitigate | Exact target paths are exposed before risky writes per D-15. |
</threat_model>

<verification>
Run `dotnet test --filter FullyQualifiedName~MainWindowViewModelTests -x` and `dotnet test --filter FullyQualifiedName~ExportPreviewServiceTests -x`.
</verification>

<success_criteria>
MainWindow can preview exports, show exact effects, and require confirmation only for overwrite/risk cases before preserving existing writer behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation-and-diagnostics/03-06-SUMMARY.md`.
</output>
