---
phase: 03-validation-and-diagnostics
plan: 07
type: execute
wave: 3
depends_on: [03-03, 03-06]
files_modified:
  - src/BS2BG.Core/Export/BodyGenIniExportWriter.cs
  - src/BS2BG.Core/Export/BosJsonExportWriter.cs
  - src/BS2BG.Core/Serialization/ProjectFileService.cs
  - src/BS2BG.App/ViewModels/MainWindowViewModel.cs
  - src/BS2BG.App/ViewModels/Workflow/FileOperationLedgerViewModel.cs
  - tests/BS2BG.Tests/ExportWriterTests.cs
  - tests/BS2BG.Tests/MainWindowViewModelTests.cs
autonomous: true
requirements: [DIAG-05]
must_haves:
  truths:
    - "User receives save/export failure messages that identify written, restored, skipped, left-untouched, or incomplete files."
    - "Project save keeps normal no-preview flow while improving failure diagnostics."
    - "Existing atomic pair/batch write semantics and byte-sensitive writer output are preserved."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/Workflow/FileOperationLedgerViewModel.cs"
      provides: "Binding-ready file outcome rows"
    - path: "src/BS2BG.App/ViewModels/MainWindowViewModel.cs"
      provides: "Ledger-aware save/export failure status"
    - path: "tests/BS2BG.Tests/MainWindowViewModelTests.cs"
      provides: "DIAG-05 ViewModel coverage"
  key_links:
    - from: "AtomicWriteException"
      to: "MainWindowViewModel"
      via: "catch block formatting"
      pattern: "File operation incomplete"
    - from: "BodyGenIniExportWriter/BosJsonExportWriter"
      to: "AtomicFileWriter"
      via: "existing atomic write calls"
      pattern: "WriteAtomic"
---

<objective>
Surface atomic outcome ledgers in save/export failures without adding save preview friction.

Purpose: Users need actionable failure details after file operations while normal save/export content semantics remain unchanged.
Output: Ledger ViewModel rows, failure formatting, writer/project save propagation, and tests.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-validation-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-validation-and-diagnostics/03-RESEARCH.md
@.planning/phases/03-validation-and-diagnostics/03-UI-SPEC.md
@.planning/phases/03-validation-and-diagnostics/03-VALIDATION.md
@src/BS2BG.App/ViewModels/MainWindowViewModel.cs
@src/BS2BG.Core/IO/AtomicFileWriter.cs
@src/BS2BG.Core/IO/AtomicWriteException.cs
@src/BS2BG.Core/IO/WriteOutcomeLedger.cs
@src/BS2BG.Core/Export/BodyGenIniExportWriter.cs
@src/BS2BG.Core/Export/BosJsonExportWriter.cs
@src/BS2BG.Core/Serialization/ProjectFileService.cs

<interfaces>
Expected from Plan 03-03:
```csharp
public sealed class AtomicWriteException : IOException { public IReadOnlyList<FileWriteLedgerEntry> Entries { get; } }
public enum FileWriteOutcome { Planned, Written, Restored, Skipped, LeftUntouched, Incomplete }
```
Existing save/export methods:
```csharp
public async Task SaveProjectAsync(CancellationToken cancellationToken = default);
public async Task ExportBodyGenInisAsync(CancellationToken cancellationToken = default);
public async Task ExportBosJsonAsync(CancellationToken cancellationToken = default);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve writer APIs while propagating ledger failures</name>
  <files>src/BS2BG.Core/Export/BodyGenIniExportWriter.cs, src/BS2BG.Core/Export/BosJsonExportWriter.cs, src/BS2BG.Core/Serialization/ProjectFileService.cs, tests/BS2BG.Tests/ExportWriterTests.cs</files>
  <read_first>src/BS2BG.Core/IO/AtomicFileWriter.cs; src/BS2BG.Core/Export/BodyGenIniExportWriter.cs; src/BS2BG.Core/Export/BosJsonExportWriter.cs; src/BS2BG.Core/Serialization/ProjectFileService.cs; tests/BS2BG.Tests/ExportWriterTests.cs</read_first>
  <behavior>
    - Existing writer success outputs remain byte-identical: BodyGen INI CRLF, BoS JSON LF/no trailing newline.
    - Writer/project save failure paths allow `AtomicWriteException` ledgers to reach App catch blocks.
    - No golden expected fixtures are modified.
  </behavior>
  <action>Review writer and `ProjectFileService.WriteAtomic` catch behavior. If they catch and wrap exceptions, preserve `AtomicWriteException` as an inner or direct exception so App code can read ledger entries; if they do not wrap, leave success APIs unchanged. Add tests around failure propagation using temp directories/invalid paths where deterministic. Do not change line ending normalization, filename sanitization, JSON formatting, or writer success return DTOs. Because `BodyGenIniExportWriter.cs` and `BosJsonExportWriter.cs` are sacred files, limit edits to exception/result propagation only and add a comment if needed explaining no formatting behavior is changed.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~ExportWriterTests -x</automated>
  </verify>
  <acceptance_criteria>`BodyGenIniExportWriter.cs` still contains `NormalizeCrLf`; `BosJsonExportWriter.cs` still contains `PreviewBosJson`; `git diff -- tests/fixtures/expected` shows no changes; tests assert `AtomicWriteException` can be observed on deterministic failure if writer path wraps exceptions.</acceptance_criteria>
  <done>Core save/export failures retain ledger details while success output semantics remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Format save/export failure ledgers in MainWindow</name>
  <files>src/BS2BG.App/ViewModels/Workflow/FileOperationLedgerViewModel.cs, src/BS2BG.App/ViewModels/MainWindowViewModel.cs, tests/BS2BG.Tests/MainWindowViewModelTests.cs</files>
  <read_first>src/BS2BG.App/ViewModels/MainWindowViewModel.cs; src/BS2BG.Core/IO/AtomicWriteException.cs; .planning/phases/03-validation-and-diagnostics/03-UI-SPEC.md; tests/BS2BG.Tests/MainWindowViewModelTests.cs</read_first>
  <behavior>
    - Save/export failure status starts with `File operation incomplete` and includes UI-SPEC body text.
    - ViewModel exposes ledger rows with outcomes `Written`, `Restored`, `Skipped`, `Left untouched`, and `Incomplete/unknown`.
    - Save flow does not add preview or extra confirmation per D-19.
  </behavior>
  <action>Create `FileOperationLedgerViewModel` with `Path`, `OutcomeLabel`, and `Detail`. Add `ObservableCollection<FileOperationLedgerViewModel> LastFileOperationLedger`, `HasFileOperationLedger`, and a private `ReportFileOperationFailure(string operation, Exception exception)` helper to `MainWindowViewModel`. Catch `AtomicWriteException` directly or find it in aggregate/inner exceptions; map ledger outcomes to UI labels exactly: `Written`, `Restored`, `Skipped`, `Left untouched`, `Incomplete/unknown`. Update save/export catch blocks to use heading `File operation incomplete` and body `Review which files were written, restored, skipped, or left untouched, then retry after fixing the file access problem.` Include original exception message after the ledger summary. Do not add project save preview or overwrite confirmation.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~MainWindowViewModelTests -x</automated>
  </verify>
  <acceptance_criteria>`MainWindowViewModel.cs` contains `LastFileOperationLedger` and `File operation incomplete`; `FileOperationLedgerViewModel.cs` contains `Incomplete/unknown`; tests assert save failure does not call export confirmation and status includes `left untouched` or `Left untouched`.</acceptance_criteria>
  <done>DIAG-05 user-facing ledger reporting is available for save/export failures without save preview friction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Atomic writer exception→App status | Filesystem failure details become user-visible operational guidance. |
| Save/export catch blocks→Project state | Failed operations must not falsely mark project clean or imply rollback success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | R | MainWindowViewModel | mitigate | Status and ledger rows identify operation outcomes and original exception text. |
| T-03-07-02 | I | FileOperationLedgerViewModel | mitigate | Incomplete/unknown state prevents overclaiming successful rollback. |
| T-03-07-03 | T | Export writers | mitigate | Tests rerun writer success coverage and golden fixture diff remains empty. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~MainWindowViewModelTests|FullyQualifiedName~ExportWriterTests" -x` and `git diff -- tests/fixtures/expected` must show no changes.
</verification>

<success_criteria>
Save/export failures produce actionable outcome ledgers and do not alter normal save preview friction or byte-sensitive writer behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation-and-diagnostics/03-07-SUMMARY.md`.
</output>
