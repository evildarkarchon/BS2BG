---
phase: 03-validation-and-diagnostics
plan: 08
type: execute
wave: 4
depends_on: [03-04, 03-05, 03-06, 03-07]
files_modified:
  - src/BS2BG.App/ViewModels/MainWindowViewModel.cs
  - src/BS2BG.App/Views/MainWindow.axaml
  - src/BS2BG.App/Views/MainWindow.axaml.cs
  - tests/BS2BG.Tests/AppShellTests.cs
  - tests/BS2BG.Tests/DiagnosticsViewModelTests.cs
autonomous: false
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04, DIAG-05]
must_haves:
  truths:
    - "User can open a first-class Diagnostics tab beside Templates and Morphs."
    - "Diagnostics tab shows summary counts, grouped findings, selected detail/drilldown, copy/report controls, NPC preview, export preview, and ledger rows."
    - "All new AXAML bindings are compiled with x:DataType and actionable controls have automation names."
  artifacts:
    - path: "src/BS2BG.App/Views/MainWindow.axaml"
      provides: "Diagnostics tab UI"
      contains: "Header=\"Diagnostics\""
    - path: "src/BS2BG.App/ViewModels/MainWindowViewModel.cs"
      provides: "Diagnostics workspace wiring"
      contains: "AppWorkspace.Diagnostics"
    - path: "tests/BS2BG.Tests/AppShellTests.cs"
      provides: "Headless shell coverage for Diagnostics tab"
  key_links:
    - from: "MainWindow.axaml"
      to: "MainWindowViewModel.Diagnostics"
      via: "compiled binding in Diagnostics TabItem"
      pattern: "Diagnostics\.RefreshDiagnosticsCommand"
    - from: "MainWindowViewModel"
      to: "DiagnosticsViewModel"
      via: "constructor/DI property"
      pattern: "DiagnosticsViewModel"
---

<objective>
Add the first-class Diagnostics tab and bind all diagnostics/preview surfaces into the Avalonia shell.

Purpose: Phase 3 is user-visible only when the read-only report and previews are accessible from the existing app shell.
Output: MainWindow Diagnostics tab, workspace wiring, headless UI tests, and human visual verification checkpoint.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-validation-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-validation-and-diagnostics/03-RESEARCH.md
@.planning/phases/03-validation-and-diagnostics/03-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@openspec/specs/reactive-mvvm-conventions/spec.md
@src/BS2BG.App/Views/MainWindow.axaml
@src/BS2BG.App/Views/MainWindow.axaml.cs
@src/BS2BG.App/ViewModels/MainWindowViewModel.cs
@src/BS2BG.App/ViewModels/DiagnosticsViewModel.cs
@src/BS2BG.App/ViewModels/MorphsViewModel.cs

<interfaces>
Expected ViewModel surfaces:
```csharp
public DiagnosticsViewModel Diagnostics { get; }
public ObservableCollection<NpcImportPreviewViewModel> NpcImportPreviewRows { get; }
public ObservableCollection<ExportPreviewViewModel> ExportPreviewFiles { get; }
public ObservableCollection<FileOperationLedgerViewModel> LastFileOperationLedger { get; }
```
Existing shell pattern:
```xml
<TabControl x:Name="WorkspaceTabs" SelectedIndex="...">
  <TabItem Header="Templates">...</TabItem>
  <TabItem Header="Morphs">...</TabItem>
</TabControl>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire Diagnostics workspace into MainWindowViewModel and shell tests</name>
  <files>src/BS2BG.App/ViewModels/MainWindowViewModel.cs, tests/BS2BG.Tests/AppShellTests.cs, tests/BS2BG.Tests/DiagnosticsViewModelTests.cs</files>
  <read_first>src/BS2BG.App/ViewModels/MainWindowViewModel.cs; src/BS2BG.App/AppBootstrapper.cs; tests/BS2BG.Tests/AppShellTests.cs; tests/BS2BG.Tests/MainWindowViewModelTests.cs</read_first>
  <behavior>
    - `AppWorkspace` includes `Diagnostics` without breaking Templates/Morphs search routing.
    - `MainWindowViewModel` exposes `DiagnosticsViewModel Diagnostics` through constructor/DI.
    - Command palette/global search behavior remains valid for Templates and Morphs; Diagnostics search may be no-op unless explicitly implemented.
  </behavior>
  <action>Add `Diagnostics` to `AppWorkspace`. Update `MainWindowViewModel` constructors to accept and expose `DiagnosticsViewModel Diagnostics`; update design-time constructor and DI usage. Update busy aggregation to include `Diagnostics.IsBusy` if available. Ensure `ApplyGlobalSearchText` clears Templates/Morphs search or leaves them unchanged when `ActiveWorkspace == AppWorkspace.Diagnostics` rather than misrouting diagnostics text to Morphs. Add tests proving service provider resolves `MainWindowViewModel.Diagnostics`, active workspace can be set to Diagnostics, and existing Templates/Morphs search routing still passes.</action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~AppShellTests|FullyQualifiedName~DiagnosticsViewModelTests|FullyQualifiedName~MainWindowViewModelTests" -x</automated>
  </verify>
  <acceptance_criteria>`MainWindowViewModel.cs` contains `AppWorkspace.Diagnostics` and `public DiagnosticsViewModel Diagnostics`; tests assert DI resolution exposes non-null Diagnostics.</acceptance_criteria>
  <done>Shell ViewModel can host the Diagnostics workspace without regressing existing workspace behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add compiled-bound Diagnostics tab UI</name>
  <files>src/BS2BG.App/Views/MainWindow.axaml, src/BS2BG.App/Views/MainWindow.axaml.cs, tests/BS2BG.Tests/AppShellTests.cs</files>
  <read_first>src/BS2BG.App/Views/MainWindow.axaml; src/BS2BG.App/Views/MainWindow.axaml.cs; src/BS2BG.App/ViewModels/DiagnosticsViewModel.cs; src/BS2BG.App/ViewModels/Workflow/NpcImportPreviewViewModel.cs; src/BS2BG.App/ViewModels/Workflow/ExportPreviewViewModel.cs; src/BS2BG.App/ViewModels/Workflow/FileOperationLedgerViewModel.cs; .planning/phases/03-validation-and-diagnostics/03-UI-SPEC.md</read_first>
  <behavior>
    - MainWindow has top-level `Diagnostics` tab beside Templates and Morphs per D-01 and UI-SPEC.
    - Tab contains `Run Diagnostics`, `Copy Report`, grouped findings, selected detail, NPC preview controls, export preview controls, and file operation ledger rows.
    - Every new DataTemplate declares `x:DataType`; every new actionable control has `automation:AutomationProperties.Name`.
  </behavior>
  <action>Modify `MainWindow.axaml` under `WorkspaceTabs` to add `<TabItem Header="Diagnostics">`. Use UI-SPEC spacing values only (`4, 8, 16, 24, 32, 48, 64`) and exact copy: `Run Diagnostics`, `Copy Report`, `No diagnostics yet`, `Preview NPC Import`, `Import Previewed NPCs`, `Preview Export`, `File operation incomplete`. Bind Run/Copy to `Diagnostics.RefreshDiagnosticsCommand` and `Diagnostics.CopyReportCommand`; bind finding list to `Diagnostics.Findings` using `DataTemplate x:DataType="vm:DiagnosticFindingViewModel"`; bind NPC preview controls to `Morphs.PreviewNpcImportCommand`, `Morphs.ImportPreviewedNpcsCommand`, and `Morphs.NpcImportPreviewRows`; bind export preview controls to `PreviewBodyGenExportCommand`, `PreviewBosJsonExportCommand`, `ExportPreviewFiles`, and `LastFileOperationLedger`. Add code-behind only if needed for tab selection synchronization; keep it view-only. Avoid ambient profile warning banners in Templates.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~AppShellTests -x</automated>
  </verify>
  <acceptance_criteria>`MainWindow.axaml` contains `Header="Diagnostics"`, `Run Diagnostics`, `Copy Report`, `Preview NPC Import`, `Preview Export`, and `File operation incomplete`; grep for new Diagnostics `DataTemplate` blocks shows `x:DataType`; tests find a Diagnostics tab/control by name.</acceptance_criteria>
  <done>Diagnostics UI is visible, accessible, compiled-bound, and wired to the ViewModel surfaces.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification of Diagnostics tab</name>
  <files>src/BS2BG.App/Views/MainWindow.axaml, src/BS2BG.App/ViewModels/MainWindowViewModel.cs</files>
  <action>Pause execution after automated build and tests so the user can visually verify the Diagnostics tab in the running Avalonia app. This checkpoint verifies layout/readability and interaction affordances that automated headless tests cannot fully prove.</action>
  <what-built>Diagnostics tab, report list/detail, preview controls, export preview rows, and ledger rows were automated in AXAML and ViewModels.</what-built>
  <how-to-verify>
    1. Run `dotnet run --project src/BS2BG.App/BS2BG.App.csproj`.
    2. Confirm the top-level workspace tabs are `Templates`, `Morphs`, and `Diagnostics`.
    3. Open `Diagnostics`, click `Run Diagnostics`, and confirm severity labels are `Blocker`, `Caution`, `Info` with grouped areas Project, Profiles, Templates, Morphs/NPCs, Import, Export.
    4. Confirm profile fallback details, if present, use informational copy and there are no ambient Templates warning banners or mismatch/experimental labels.
    5. Confirm `Copy Report`, `Preview NPC Import`, `Import Previewed NPCs`, and `Preview Export` are visible and keyboard reachable.
  </how-to-verify>
  <verify>
    <automated>dotnet build BS2BG.sln</automated>
  </verify>
  <done>User approves the Diagnostics tab visual/interaction checkpoint or provides concrete issues for executor follow-up.</done>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewModel→AXAML | Binding errors or missing automation names can hide diagnostics or break accessibility. |
| User preview controls→mutation commands | Preview controls must remain distinct from commit/export commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | D | MainWindow.axaml compiled bindings | mitigate | Headless shell tests and `dotnet build` catch missing x:DataType/binding properties. |
| T-03-08-02 | I | Diagnostics tab copy | mitigate | UI includes text severity labels and automation names; does not rely on color alone. |
| T-03-08-03 | T | Preview/commit controls | mitigate | Buttons use distinct copy: preview vs import/export commit; human checkpoint verifies visual separation. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~AppShellTests|FullyQualifiedName~DiagnosticsViewModelTests|FullyQualifiedName~MainWindowViewModelTests|FullyQualifiedName~MorphsViewModelTests" -x`, then `dotnet build BS2BG.sln`, then complete the human verification checkpoint.
</verification>

<success_criteria>
Diagnostics is a first-class shell tab with compiled bindings, accessible controls, read-only report presentation, preview surfaces, and visible ledger details.
</success_criteria>

<output>
After completion, create `.planning/phases/03-validation-and-diagnostics/03-08-SUMMARY.md`.
</output>
