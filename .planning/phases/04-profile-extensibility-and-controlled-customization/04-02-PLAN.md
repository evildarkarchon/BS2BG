---
phase: 04-profile-extensibility-and-controlled-customization
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/BS2BG.Core/Generation/TemplateProfileCatalog.cs
  - src/BS2BG.App/Services/UserProfileStore.cs
  - src/BS2BG.App/Services/TemplateProfileCatalogService.cs
  - src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs
  - src/BS2BG.App/AppBootstrapper.cs
  - tests/BS2BG.Tests/UserProfileStoreTests.cs
  - tests/BS2BG.Tests/TemplateProfileCatalogFactoryTests.cs
autonomous: true
requirements: [EXT-01]
user_setup: []
must_haves:
  truths:
    - "D-01/D-04: Editable profiles are discovered from `%APPDATA%/jBS2BG/profiles/` and explicit import/copy actions can persist validated custom JSON there."
    - "D-02/D-03: Bundled profiles remain read-only and custom profiles cannot shadow bundled names case-insensitively."
    - "Addresses review concern: runtime catalog refresh has one owner after import/save/delete/conflict-resolution workflows change local profiles."
  artifacts:
    - path: "src/BS2BG.App/Services/UserProfileStore.cs"
      provides: "AppData custom-profile folder discovery and atomic local JSON writes"
      exports: ["IUserProfileStore", "UserProfileStore"]
    - path: "src/BS2BG.Core/Generation/TemplateProfileCatalog.cs"
      provides: "Catalog entries with source-kind/editability metadata while preserving existing lookup fallback"
    - path: "src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs"
      provides: "Bundled + custom catalog composition"
    - path: "src/BS2BG.App/Services/TemplateProfileCatalogService.cs"
      provides: "Mutable App-layer catalog holder with refresh and project-scope overlay hooks"
      exports: ["ITemplateProfileCatalogService", "TemplateProfileCatalogService"]
  key_links:
    - from: "TemplateProfileCatalogFactory.CreateDefault"
      to: "UserProfileStore.DiscoverProfiles"
      via: "validated local profile loading after bundled required profiles"
      pattern: "UserProfileStore"
    - from: "UserProfileStore.SaveProfile"
      to: "AtomicFileWriter.WriteAtomicBatch"
      via: "UTF-8 no-BOM custom profile JSON write"
      pattern: "WriteAtomic"
    - from: "ProfileManagerViewModel/Project open conflict flows"
      to: "ITemplateProfileCatalogService.Refresh"
      via: "single refresh contract after local profile store changes"
      pattern: "Refresh"
---

<objective>
Add user-local custom profile storage and source-tagged catalog composition without allowing custom data to mutate or shadow bundled profiles.

Purpose: Make reusable local custom profiles available at runtime while preserving bundled profile trust.
Output: App profile store, catalog metadata, factory wiring, DI registration, and tests.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-CONTEXT.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-RESEARCH.md
@src/BS2BG.Core/Generation/TemplateProfileCatalog.cs
@src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs
@src/BS2BG.Core/IO/AtomicFileWriter.cs
@src/BS2BG.App/Services/UserPreferencesService.cs
@src/BS2BG.App/AppBootstrapper.cs
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-01-SUMMARY.md

<interfaces>
From Plan 01:
```csharp
public sealed record CustomProfileDefinition(string Name, string Game, SliderProfile SliderProfile, ProfileSourceKind SourceKind, string? FilePath);
public sealed class ProfileDefinitionService { public ProfileValidationResult ValidateProfileJson(string json, ProfileValidationContext context); public string ExportProfileJson(CustomProfileDefinition profile); }
```
Existing catalog:
```csharp
public sealed class TemplateProfileCatalog
{
    public IReadOnlyList<TemplateProfile> Profiles { get; }
    public IReadOnlyList<string> ProfileNames { get; }
    public TemplateProfile DefaultProfile { get; }
    public bool ContainsProfile(string? name);
    public TemplateProfile GetProfile(string? name);
}
```
Required App catalog service contract:
```csharp
public interface ITemplateProfileCatalogService
{
    TemplateProfileCatalog Current { get; }
    IReadOnlyList<ProfileValidationDiagnostic> LastDiscoveryDiagnostics { get; }
    TemplateProfileCatalog Refresh();
    TemplateProfileCatalog WithProjectProfiles(IEnumerable<CustomProfileDefinition> projectProfiles);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source-tagged catalog entries</name>
  <files>src/BS2BG.Core/Generation/TemplateProfileCatalog.cs, tests/BS2BG.Tests/TemplateProfileCatalogFactoryTests.cs</files>
  <read_first>
    - src/BS2BG.Core/Generation/TemplateProfileCatalog.cs
    - src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs
    - tests/BS2BG.Tests/TemplateProfileCatalogFactoryTests.cs
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-CONTEXT.md
  </read_first>
  <behavior>
    - Existing `ProfileNames`, `ContainsProfile`, `GetProfile`, and `DefaultProfile` behaviors remain unchanged for bundled profiles.
    - Each catalog row exposes `SourceKind`, `FilePath`, and `IsEditable`; bundled rows are not editable per D-02.
    - Constructing a catalog with case-insensitive duplicate names throws `ArgumentException` per D-03.
  </behavior>
  <action>Extend `TemplateProfileCatalog` with a `ProfileCatalogEntry` record (Name, TemplateProfile, SourceKind, FilePath, IsEditable) and an `Entries` property. Preserve the existing `TemplateProfileCatalog(IEnumerable<TemplateProfile>)` constructor by wrapping all inputs as `ProfileSourceKind.Bundled`. Add a second constructor accepting `IEnumerable<ProfileCatalogEntry>`. Make duplicate display names case-insensitive blockers at construction. Keep `Profiles`, `ProfileNames`, `DefaultProfile`, `ContainsProfile`, and `GetProfile` compatible so existing generation code continues working.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~TemplateProfileCatalogFactoryTests</automated>
  </verify>
  <acceptance_criteria>
    - `TemplateProfileCatalog.cs` contains `public sealed record ProfileCatalogEntry`.
    - `TemplateProfileCatalog.cs` contains `public IReadOnlyList<ProfileCatalogEntry> Entries`.
    - A test asserts duplicate names differing only by case throw `ArgumentException`.
  </acceptance_criteria>
  <done>Catalog can represent bundled/custom source metadata without changing lookup semantics or allowing shadowing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AppData custom profile store</name>
  <files>src/BS2BG.App/Services/UserProfileStore.cs, tests/BS2BG.Tests/UserProfileStoreTests.cs</files>
  <read_first>
    - src/BS2BG.App/Services/UserPreferencesService.cs
    - src/BS2BG.Core/IO/AtomicFileWriter.cs
    - src/BS2BG.Core/Generation/ProfileDefinitionService.cs
    - tests/BS2BG.Tests/UserProfileStoreTests.cs
  </read_first>
  <behavior>
    - `GetDefaultProfileDirectory()` returns `%APPDATA%/jBS2BG/profiles` per research discretion and D-01.
    - Discovery validates every `*.json` file and returns valid profiles plus diagnostics for rejected files without throwing for malformed user JSON.
    - Saving a profile writes atomically and suggests a sanitized filename; profile identity still comes from internal `Name`, not filename per D-12.
  </behavior>
  <action>Create `IUserProfileStore` and `UserProfileStore` in App services. The concrete store owns folder creation under `Environment.SpecialFolder.ApplicationData` + `jBS2BG` + `profiles`, discovers `*.json`, uses `ProfileDefinitionService` for validation, writes JSON via Core atomic writer using UTF-8 without BOM, and returns result objects with diagnostics/status instead of throwing for normal user-data failures. Include methods `DiscoverProfiles()`, `SaveProfile(CustomProfileDefinition profile)`, `DeleteProfile(CustomProfileDefinition profile)`, and `GetDefaultProfileDirectory()`. Filename suggestions must strip `Path.GetInvalidFileNameChars()`, normalize to a lower-case 64-character base, append `.json`, and resolve sanitized-name collisions by appending `-{hash8}` from a stable hash of the profile name. Profile identity still comes only from internal `Name`; filenames never resolve missing references. Keep all Avalonia UI/file-picker code out of this store. Addresses review concern: filename sanitization and collisions are deterministic.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~UserProfileStoreTests</automated>
  </verify>
  <acceptance_criteria>
    - `UserProfileStore.cs` contains `Path.Combine(appData, "jBS2BG", "profiles")` or equivalent path segments.
    - `UserProfileStore.cs` contains `Path.GetInvalidFileNameChars` and `hash8`/8-character hash collision handling or equivalent deterministic suffix logic.
    - `UserProfileStore.cs` contains `ProfileDefinitionService` usage for discovery/import validation.
    - `UserProfileStore.cs` contains `AtomicFileWriter` usage for saves.
    - Focused test command exits 0.
  </acceptance_criteria>
  <done>User-local profile storage discovers and writes validated custom profiles without touching bundled install files.</done>
</task>

<task type="auto">
  <name>Task 3: Compose custom profiles into DI catalog</name>
  <files>src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs, src/BS2BG.App/AppBootstrapper.cs, tests/BS2BG.Tests/TemplateProfileCatalogFactoryTests.cs</files>
  <read_first>
    - src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs
    - src/BS2BG.App/AppBootstrapper.cs
    - src/BS2BG.Core/Generation/TemplateProfileCatalog.cs
    - src/BS2BG.App/Services/UserProfileStore.cs
  </read_first>
  <action>Convert `TemplateProfileCatalogFactory` from a static-only factory into an injectable instance factory that accepts `IUserProfileStore` and bundled search directories, while preserving static `CreateDefault(...)` shim methods for existing tests and design-time callers. Keep required bundled profile loading from `AppContext.BaseDirectory`; populate bundled `ProfileCatalogEntry` metadata with `Game = "Skyrim"` for Skyrim CBBE/UUNP and `Game = "Fallout4"` for Fallout 4 CBBE when wrapping bundled definitions. Append valid local custom profiles from `IUserProfileStore` as `ProfileSourceKind.LocalCustom`. If local profile folder discovery fails or a file is invalid, preserve a bundled-only catalog and expose rejected-file diagnostics through a catalog service result for later UI; do not fail app startup for malformed custom user files. Create `ITemplateProfileCatalogService`/`TemplateProfileCatalogService` as the sole runtime holder of the current catalog, with `Refresh()` rebuilding bundled + local entries after import/save/delete/replace operations and `WithProjectProfiles(...)` returning a catalog overlay containing `ProfileSourceKind.EmbeddedProject` entries for project-scoped `Use Project Copy` flows without writing local files. Register `ProfileDefinitionService`, `IUserProfileStore/UserProfileStore`, injectable `TemplateProfileCatalogFactory`, and `ITemplateProfileCatalogService` in `AppBootstrapper`; existing consumers can receive `TemplateProfileCatalog` from `catalogService.Current` until later plans migrate to refresh-aware service injection. Addresses review concerns: static factory migration, catalog refresh ownership, bundled Game defaults, and project-scope overlay are specified.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~TemplateProfileCatalogFactoryTests</automated>
  </verify>
  <acceptance_criteria>
    - `AppBootstrapper.cs` contains `AddSingleton<ProfileDefinitionService>`.
    - `AppBootstrapper.cs` contains `AddSingleton<IUserProfileStore, UserProfileStore>`.
    - `AppBootstrapper.cs` contains `AddSingleton<ITemplateProfileCatalogService, TemplateProfileCatalogService>`.
    - `TemplateProfileCatalogFactory.cs` contains a non-static constructor accepting `IUserProfileStore` and retains a static `CreateDefault` shim.
    - `TemplateProfileCatalogService.cs` contains `Refresh(` and `WithProjectProfiles(`.
    - `TemplateProfileCatalogFactory.cs` creates bundled entries before custom entries.
    - Tests prove a local custom profile appears in `ProfileNames` and a bundled-name duplicate is rejected/skipped without replacing bundled math.
  </acceptance_criteria>
  <done>Runtime catalog includes valid local custom profiles, rejects duplicates, and remains usable when user profile files are broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user profile folder → App store | Local editable files may be malformed or intentionally confusing. |
| App store → Core catalog | Only validated custom profiles may enter lookup/generation paths. |
| custom profile identity → bundled identity | Custom display names must not spoof bundled profiles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | UserProfileStore.DiscoverProfiles | mitigate | Validate entire JSON with ProfileDefinitionService before returning a catalog candidate. |
| T-04-02-02 | Spoofing | TemplateProfileCatalog constructor | mitigate | Reject case-insensitive duplicate names across bundled and custom entries per D-03. |
| T-04-02-03 | Tampering/DoS | UserProfileStore.SaveProfile | mitigate | Use sanitized filenames and AtomicFileWriter so partial local profile writes do not corrupt existing files. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~UserProfileStoreTests|FullyQualifiedName~TemplateProfileCatalogFactoryTests"` and `dotnet build BS2BG.sln`.
</verification>

<success_criteria>
Bundled profiles are source-tagged read-only, user profiles live under AppData, custom discovery is automatic and explicit-action ready, duplicates cannot shadow bundled names, and app startup remains resilient to broken local JSON.
</success_criteria>

<output>
After completion, create `.planning/phases/04-profile-extensibility-and-controlled-customization/04-02-SUMMARY.md`
</output>
