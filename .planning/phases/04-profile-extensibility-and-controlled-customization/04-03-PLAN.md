---
phase: 04-profile-extensibility-and-controlled-customization
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/BS2BG.Core/Models/ProjectModel.cs
  - src/BS2BG.Core/Serialization/ProjectFileService.cs
  - tests/BS2BG.Tests/ProjectFileServiceCustomProfileTests.cs
autonomous: true
requirements: [EXT-03, EXT-05]
user_setup: []
must_haves:
  truths:
    - "D-13/D-15: Saving a project embeds only custom profile definitions referenced by project presets."
    - "D-14: Existing `.jbs2bg` legacy root fields (`SliderPresets`, `CustomMorphTargets`, `MorphedNPCs`, `isUUNP`, `Profile`) remain intact and older-reader-compatible."
  artifacts:
    - path: "src/BS2BG.Core/Models/ProjectModel.cs"
      provides: "Project-owned embedded custom profile collection"
    - path: "src/BS2BG.Core/Serialization/ProjectFileService.cs"
      provides: "Optional top-level CustomProfiles project section"
    - path: "tests/BS2BG.Tests/ProjectFileServiceCustomProfileTests.cs"
      provides: "Round-trip and legacy-field compatibility coverage"
  key_links:
    - from: "ProjectFileService.SaveToString"
      to: "ProjectModel.CustomProfiles"
      via: "filters referenced custom profile names from SliderPresets"
      pattern: "CustomProfiles"
    - from: "ProjectFileService.LoadFromString"
      to: "ProjectModel.CustomProfiles"
      via: "hydrates optional embedded profiles without changing preset Profile fields"
      pattern: "LoadFromString"
---

<objective>
Extend `.jbs2bg` project serialization with optional embedded custom profile definitions while preserving all legacy fields and named-profile compatibility.

Purpose: Enable project sharing with referenced custom profiles without requiring sidecar files or breaking existing consumers.
Output: Project model storage, serializer DTO extension, and project round-trip tests.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-CONTEXT.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-RESEARCH.md
@openspec/specs/project-roundtrip/spec.md
@src/BS2BG.Core/Serialization/ProjectFileService.cs
@src/BS2BG.Core/Models/ProjectModel.cs
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-01-SUMMARY.md

<interfaces>
Existing serializer fields:
```csharp
[JsonPropertyName("SliderPresets")][JsonPropertyOrder(0)] public Dictionary<string, SliderPresetDto>? SliderPresets { get; set; }
[JsonPropertyName("CustomMorphTargets")][JsonPropertyOrder(1)] public Dictionary<string, MorphTargetDto>? CustomMorphTargets { get; set; }
[JsonPropertyName("MorphedNPCs")][JsonPropertyOrder(2)] public NamedNpcObjectList? MorphedNpCs { get; set; }
```
Required extension:
```csharp
[JsonPropertyName("CustomProfiles")]
[JsonPropertyOrder(3)]
public List<EmbeddedProfileDto>? CustomProfiles { get; set; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add project model support for embedded custom profiles</name>
  <files>src/BS2BG.Core/Models/ProjectModel.cs, tests/BS2BG.Tests/ProjectFileServiceCustomProfileTests.cs</files>
  <read_first>
    - src/BS2BG.Core/Models/ProjectModel.cs
    - src/BS2BG.Core/Models/SliderPreset.cs
    - src/BS2BG.Core/Serialization/ProjectFileService.cs
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-CONTEXT.md
  </read_first>
  <behavior>
    - ProjectModel can hold embedded `CustomProfileDefinition` objects and clone/replace them when loading a project.
    - Adding/removing embedded profiles affects dirty state like other project data.
    - Existing project data remains unchanged when no embedded profiles are present.
  </behavior>
  <action>Add an observable or read-only-mutable `CustomProfiles` collection to `ProjectModel` for embedded project profile definitions. It must participate in `ReplaceWith`, `MarkClean`, dirty tracking, and project snapshot semantics in the same style as existing `SliderPresets`, `CustomMorphTargets`, and `MorphedNpcs`. Add tests that prove loading a project without `CustomProfiles` leaves the collection empty and existing legacy fields still round-trip.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~ProjectFileServiceCustomProfileTests</automated>
  </verify>
  <acceptance_criteria>
    - `ProjectModel.cs` contains `CustomProfiles`.
    - Tests include a no-custom-profiles project JSON case that preserves `isUUNP` and `Profile` fields.
  </acceptance_criteria>
  <done>Project aggregate can carry embedded custom profile definitions without disturbing legacy project behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serialize optional CustomProfiles section</name>
  <files>src/BS2BG.Core/Serialization/ProjectFileService.cs, tests/BS2BG.Tests/ProjectFileServiceCustomProfileTests.cs</files>
  <read_first>
    - src/BS2BG.Core/Serialization/ProjectFileService.cs
    - src/BS2BG.Core/Generation/ProfileDefinitionService.cs
    - tests/BS2BG.Tests/ProjectFileServiceTests.cs
  </read_first>
  <behavior>
    - Save emits `CustomProfiles` only when at least one project preset references a custom profile present in `ProjectModel.CustomProfiles`.
    - Save excludes unrelated local/embedded profiles not referenced by any preset per D-15.
    - Load hydrates `CustomProfiles` when present but keeps every preset `Profile` string unchanged.
    - Property order keeps `SliderPresets`, `CustomMorphTargets`, `MorphedNPCs`, then `CustomProfiles` per D-14.
  </behavior>
  <action>Extend `ProjectFileService` DTOs with a top-level `[JsonPropertyName("CustomProfiles")][JsonPropertyOrder(3)]` nullable list. Use DTO fields `Name`, `Game`, `Defaults`, `Multipliers`, and `Inverted`, matching profile JSON shape from Plan 01. In `ToDto(ProjectModel)`, include only custom profiles whose `Name` matches at least one `SliderPreset.ProfileName` case-insensitively and whose source is not `Bundled`. In `LoadFromString`, validate/hydrate embedded definitions into `ProjectModel.CustomProfiles` without mutating preset `Profile` values. Do not reorder or rename existing legacy fields.</action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ProjectFileServiceCustomProfileTests|FullyQualifiedName~ProjectFileServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `ProjectFileService.cs` contains `[JsonPropertyName("CustomProfiles")]`.
    - `ProjectFileService.cs` still contains `[JsonPropertyName("SliderPresets")]`, `[JsonPropertyName("CustomMorphTargets")]`, `[JsonPropertyName("MorphedNPCs")]`, `[JsonPropertyName("isUUNP")]`, and `[JsonPropertyName("Profile")]`.
    - Tests assert unrelated custom profiles are not serialized.
    - Focused test command exits 0.
  </acceptance_criteria>
  <done>Project files embed exactly referenced custom profiles while preserving legacy-compatible fields and ordering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project JSON → ProjectFileService | Shared `.jbs2bg` may contain malformed or conflicting embedded profile data. |
| ProjectModel → saved `.jbs2bg` | Serializer decides which local/custom profile definitions become shared data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | ProjectFileService.LoadFromString | mitigate | Validate embedded profile DTOs before hydrating them; malformed profiles do not mutate legacy preset profile strings. |
| T-04-03-02 | Information Disclosure | ProjectFileService.SaveToString | mitigate | Serialize only custom profiles referenced by project presets; do not include unrelated local profiles per D-15. |
| T-04-03-03 | Tampering | ProjectFileDto field order/names | mitigate | Preserve legacy property names/order and add only optional `CustomProfiles` section after existing root fields per D-14. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~ProjectFileServiceCustomProfileTests|FullyQualifiedName~ProjectFileServiceTests"` and `dotnet build BS2BG.sln`.
</verification>

<success_criteria>
`.jbs2bg` save/load supports optional embedded custom profiles, includes only referenced definitions, keeps legacy fields intact, and remains compatible when the optional section is absent.
</success_criteria>

<output>
After completion, create `.planning/phases/04-profile-extensibility-and-controlled-customization/04-03-SUMMARY.md`
</output>
