---
phase: 04-profile-extensibility-and-controlled-customization
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/BS2BG.Core/Diagnostics/ProfileRecoveryDiagnosticsService.cs
  - src/BS2BG.Core/Diagnostics/ProfileDiagnosticsService.cs
  - tests/BS2BG.Tests/ProfileRecoveryDiagnosticsServiceTests.cs
autonomous: true
requirements: [EXT-04]
user_setup: []
must_haves:
  truths:
    - "D-09/D-10: Projects with missing custom profiles remain openable and show visible neutral fallback/recovery diagnostics rather than silent fallback."
    - "D-11/D-12: Recovery diagnostics describe exact actions and imported-profile resolution uses internal display-name exact match ignoring case, not filenames."
  artifacts:
    - path: "src/BS2BG.Core/Diagnostics/ProfileRecoveryDiagnosticsService.cs"
      provides: "Read-only recovery findings and exact-match resolution checks for missing custom profiles"
      exports: ["ProfileRecoveryDiagnosticsService", "ProfileRecoveryDiagnostic", "ProfileRecoveryActionKind"]
    - path: "tests/BS2BG.Tests/ProfileRecoveryDiagnosticsServiceTests.cs"
      provides: "Missing-profile and exact-match recovery tests"
  key_links:
    - from: "ProfileRecoveryDiagnosticsService.Analyze"
      to: "ProjectModel.SliderPresets"
      via: "detect profile names absent from TemplateProfileCatalog"
      pattern: "Missing profile diagnostic"
    - from: "ProfileRecoveryDiagnosticsService.CanResolveMissingReference"
      to: "CustomProfileDefinition.Name"
      via: "OrdinalIgnoreCase internal display-name match only"
      pattern: "StringComparison.OrdinalIgnoreCase"
---

<objective>
Create read-only Core recovery diagnostics for missing custom profile references and exact-match import resolution.

Purpose: Preserve non-blocking project open/generation while ensuring unresolved custom profile fallback is visible and actionable.
Output: Recovery diagnostics service and tests, plus neutral diagnostics integration.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-CONTEXT.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-RESEARCH.md
@src/BS2BG.Core/Diagnostics/ProfileDiagnosticsService.cs
@src/BS2BG.Core/Diagnostics/ProjectValidationService.cs
@src/BS2BG.Core/Generation/TemplateProfileCatalog.cs
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-01-SUMMARY.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-03-SUMMARY.md

<interfaces>
Existing neutral fallback finding pattern:
```csharp
findings.Add(new DiagnosticFinding(
    DiagnosticSeverity.Info,
    "Profiles",
    "Profile fallback detail",
    "Saved profile: " + savedProfileName + "; calculation fallback: " + calculationFallbackProfileName + ". This is informational and does not block generation or export.",
    savedProfileName));
```
Required recovery actions:
```csharp
public enum ProfileRecoveryActionKind { ImportMatchingProfile, RemapToInstalledProfile, KeepUnresolvedForNow }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add profile recovery diagnostics service</name>
  <files>src/BS2BG.Core/Diagnostics/ProfileRecoveryDiagnosticsService.cs, tests/BS2BG.Tests/ProfileRecoveryDiagnosticsServiceTests.cs</files>
  <read_first>
    - src/BS2BG.Core/Diagnostics/ProfileDiagnosticsService.cs
    - src/BS2BG.Core/Generation/TemplateProfileCatalog.cs
    - src/BS2BG.Core/Models/ProjectModel.cs
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-CONTEXT.md
  </read_first>
  <behavior>
    - A preset profile name absent from the catalog produces an Info recovery diagnostic with copy containing `Project references custom profile` and `visible fallback calculation`.
    - The diagnostic exposes exactly `ImportMatchingProfile`, `RemapToInstalledProfile`, and `KeepUnresolvedForNow` actions per D-11 and UI-SPEC.
    - A project with no missing profile names produces no recovery diagnostics.
  </behavior>
  <action>Create `ProfileRecoveryDiagnosticsService` in Core. It must be read-only: accept `ProjectModel` and `TemplateProfileCatalog`, inspect preset `ProfileName` values, and return immutable `ProfileRecoveryDiagnostic` records. Each missing reference diagnostic must include affected preset names, missing profile display name, fallback calculation profile name, neutral message text, and the three recovery action kinds. Do not mutate `ProjectModel`, do not block generation/export, and do not use wording `mismatch`, `error`, or `experimental` for normal missing-profile recovery.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~ProfileRecoveryDiagnosticsServiceTests</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileRecoveryDiagnosticsService.cs` contains `ProfileRecoveryActionKind` with `ImportMatchingProfile`, `RemapToInstalledProfile`, and `KeepUnresolvedForNow`.
    - `ProfileRecoveryDiagnosticsService.cs` contains neutral message text including `BS2BG can continue with visible fallback calculation until you resolve it`.
    - Tests assert the diagnostic severity is informational/neutral and does not block generation.
  </acceptance_criteria>
  <done>Missing custom profile references produce visible read-only recovery diagnostics with exact allowed actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add exact-match recovery resolution helpers</name>
  <files>src/BS2BG.Core/Diagnostics/ProfileRecoveryDiagnosticsService.cs, tests/BS2BG.Tests/ProfileRecoveryDiagnosticsServiceTests.cs</files>
  <read_first>
    - src/BS2BG.Core/Diagnostics/ProfileRecoveryDiagnosticsService.cs
    - src/BS2BG.Core/Models/CustomProfileDefinition.cs
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-CONTEXT.md
  </read_first>
  <behavior>
    - Imported profile with internal `Name` equal to missing reference ignoring case resolves the diagnostic.
    - Imported profile whose filename/path resembles the missing reference but internal `Name` differs does not resolve it per D-12.
    - Resolution helpers do not install or mutate profiles; they only report whether an explicit import can resolve a missing reference.
  </behavior>
  <action>Add helper methods such as `CanResolveMissingReference(string missingProfileName, CustomProfileDefinition importedProfile)` or a richer result type. The comparison must use only `CustomProfileDefinition.Name` and `StringComparison.OrdinalIgnoreCase`; ignore `FilePath` entirely for identity. Add tests for exact-case, different-case, and filename/internal-name mismatch. Keep this service Core-only so App recovery commands can call it before mutating project/local store state.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~ProfileRecoveryDiagnosticsServiceTests</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileRecoveryDiagnosticsService.cs` contains `CanResolveMissingReference` or equivalent exact-match helper.
    - Tests contain a case where `FilePath` suggests a match but `Name` differs and resolution is false.
    - Focused test command exits 0.
  </acceptance_criteria>
  <done>Recovery matching is exact internal-display-name only and cannot silently resolve from filenames or fuzzy matching.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project profile reference → diagnostics | Saved profile names may point to absent or user-specific data. |
| imported profile → recovery resolution | Imported local files may try to masquerade as missing references through filenames. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure/Integrity | ProfileRecoveryDiagnosticsService.Analyze | mitigate | Always surface neutral missing-profile diagnostics when fallback is used; unresolved state must not be silent. |
| T-04-04-02 | Spoofing | CanResolveMissingReference | mitigate | Use internal profile display name exact match ignoring case; ignore filename/path for identity per D-12. |
| T-04-04-03 | Tampering | Recovery diagnostics | mitigate | Keep diagnostics read-only; mutation happens only through explicit App commands in later plans. |
</threat_model>

<verification>
Run `dotnet test --filter FullyQualifiedName~ProfileRecoveryDiagnosticsServiceTests` and `dotnet build BS2BG.sln`.
</verification>

<success_criteria>
Missing custom profile references are reported neutrally and actionably, generation remains non-blocking, and imported profile matching is exact internal-display-name only.
</success_criteria>

<output>
After completion, create `.planning/phases/04-profile-extensibility-and-controlled-customization/04-04-SUMMARY.md`
</output>
