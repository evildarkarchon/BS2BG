---
phase: 04-profile-extensibility-and-controlled-customization
plan: 05
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/BS2BG.App/Services/ProfileManagementDialogService.cs
  - src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs
  - src/BS2BG.App/ViewModels/ProfileEditorViewModel.cs
  - src/BS2BG.App/AppBootstrapper.cs
  - tests/BS2BG.Tests/ProfileManagerViewModelTests.cs
  - tests/BS2BG.Tests/ProfileEditorViewModelTests.cs
autonomous: true
requirements: [EXT-01, EXT-02]
user_setup: []
must_haves:
  truths:
    - "D-02/D-05: Bundled profiles can be inspected and copied as custom before metadata or slider tables are edited."
    - "D-06/D-07/D-08/D-17: Users can import, create blank, validate, save, edit, delete, and export selected custom profile JSON through App workflows gated by Core validation."
    - "Addresses review concern: dialog service signatures, editor lifetime, catalog refresh, validation cadence, referenced-profile delete behavior, and busy state are explicit App contracts."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs"
      provides: "Profile list/source rail commands and custom profile import/copy/export/delete orchestration"
    - path: "src/BS2BG.App/ViewModels/ProfileEditorViewModel.cs"
      provides: "Metadata/Defaults/Multipliers/Inverted editing with validation blocker state"
    - path: "src/BS2BG.App/Services/ProfileManagementDialogService.cs"
      provides: "StorageProvider-facing import/export destination service interfaces and window implementation"
  key_links:
    - from: "ProfileEditorViewModel.SaveProfileCommand"
      to: "UserProfileStore.SaveProfile"
      via: "validated CustomProfileDefinition"
      pattern: "SaveProfileCommand"
    - from: "ProfileManagerViewModel.CopyBundledProfileCommand"
      to: "TemplateProfileCatalog.Entries"
      via: "pre-populate editor from read-only bundled source with new non-conflicting name required"
      pattern: "CopyBundledProfileCommand"
---

<objective>
Add ReactiveUI App-layer profile manager/editor workflows over the Core validator and user profile store.

Purpose: Provide controlled custom profile authoring without ViewModels writing files directly or editing bundled profile definitions.
Output: Profile manager/editor ViewModels, dialog/file service boundary, DI wiring, and tests.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-CONTEXT.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-RESEARCH.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-UI-SPEC.md
@openspec/specs/reactive-mvvm-conventions/spec.md
@src/BS2BG.App/ViewModels/TemplatesViewModel.cs
@src/BS2BG.App/AppBootstrapper.cs
@src/BS2BG.App/Services/WindowFileDialogService.cs
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-01-SUMMARY.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-02-SUMMARY.md

<interfaces>
ReactiveUI conventions:
```csharp
public sealed partial class SomeViewModel : ReactiveObject, IDisposable
{
    [Reactive] private string _someValue = string.Empty;
    public ReactiveCommand<Unit, Unit> SaveCommand { get; }
}
```
Required manager commands:
```csharp
ImportProfileCommand, CreateBlankProfileCommand, CopyBundledProfileCommand, ValidateProfileCommand, SaveProfileCommand, ExportProfileCommand, DeleteCustomProfileCommand
```
Required dialog service methods:
```csharp
Task<IReadOnlyList<string>> PickProfileImportFilesAsync(CancellationToken cancellationToken);
Task<string?> PickProfileExportPathAsync(string suggestedFileName, CancellationToken cancellationToken);
Task<bool> ConfirmDeleteProfileAsync(string profileName, CancellationToken cancellationToken);
Task<bool> ConfirmDeleteReferencedProfileAsync(string profileName, int affectedPresetCount, CancellationToken cancellationToken);
Task<bool> ConfirmDiscardUnsavedEditsAsync(CancellationToken cancellationToken);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create profile manager ViewModel and service boundary</name>
  <files>src/BS2BG.App/Services/ProfileManagementDialogService.cs, src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs, tests/BS2BG.Tests/ProfileManagerViewModelTests.cs</files>
  <read_first>
    - src/BS2BG.App/Services/WindowFileDialogService.cs
    - src/BS2BG.App/ViewModels/TemplatesViewModel.cs
    - src/BS2BG.App/Services/UserProfileStore.cs
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-UI-SPEC.md
  </read_first>
  <behavior>
    - Bundled catalog entries are selectable and expose `Copy as Custom Profile`, but not edit/delete/save actions per D-02.
    - Custom entries expose edit, validate, export, save, and delete commands per UI-SPEC.
    - Import uses the service boundary to pick JSON, validates before store write/catalog inclusion, and reports duplicate-name blockers per D-03/D-06.
  </behavior>
  <action>Create `IProfileManagementDialogService`/`ProfileManagementDialogService` with the exact method signatures in the interfaces block for profile JSON import/export file picker paths and destructive confirmations, following the existing Window service + null test implementation pattern. Create `ProfileManagerViewModel` using ReactiveUI `[Reactive]` properties and `ReactiveCommand` commands. It must own a single `ProfileEditorViewModel` instance rebound on selection changes; if the editor has unsaved changes, selection change must call `ConfirmDiscardUnsavedEditsAsync` before discarding. It must load catalog entries from `ITemplateProfileCatalogService.Current`, subscribe to the Plan 02 catalog changed signal so `ProfileEntries` updates after refresh within the same session, expose an `[ObservableAsProperty] bool IsBusy` derived from command `IsExecuting`, group/source-label rows as `Bundled — read-only`, `Custom — editable`, `Embedded in project`, and `Missing — using fallback`, and orchestrate import/create/copy/export/delete through `ProfileDefinitionService`, `IUserProfileStore`, and catalog metadata. Delete behavior must check the open project for presets referencing the selected custom profile: if referenced, call `ConfirmDeleteReferencedProfileAsync(profileName, affectedPresetCount, cancellationToken)` and, after confirmed deletion, keep project references unresolved so Phase 4 recovery diagnostics reappear rather than silently remapping. After successful import/save/delete, call `ITemplateProfileCatalogService.Refresh()` so Templates/Diagnostics/Profile UI see runtime catalog updates. Do not write files directly from the ViewModel; do not use RelayCommand or direct Dispatcher calls. Document the current single-shell lifetime assumption on `ProfileManagerViewModel` in an XML doc comment or class comment. Addresses review concerns: dialog interface, editor lifecycle, catalog refresh/list rebinding, referenced-profile deletion, single-shell lifetime, and busy aggregation are specified.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~ProfileManagerViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileManagerViewModel.cs` contains `ReactiveCommand` and does not contain `RelayCommand`.
    - `ProfileManagerViewModel.cs` contains `IsBusy` and calls `Refresh()` after import/save/delete success.
    - `ProfileManagementDialogService.cs` contains `IProfileManagementDialogService`.
    - `ProfileManagementDialogService.cs` contains `PickProfileImportFilesAsync`, `PickProfileExportPathAsync`, `ConfirmDeleteProfileAsync`, `ConfirmDeleteReferencedProfileAsync`, and `ConfirmDiscardUnsavedEditsAsync`.
    - Tests assert bundled profile edit/delete commands cannot execute and copy-as-custom can execute.
    - Tests assert changing selection with unsaved edits invokes discard confirmation and preserves the prior editor when declined.
    - Tests assert deleting a custom profile referenced by the open project requires referenced-delete confirmation and leaves references unresolved after deletion.
  </acceptance_criteria>
  <done>Profile manager exposes safe source-aware profile operations through App service boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create profile editor ViewModel with validation-gated save</name>
  <files>src/BS2BG.App/ViewModels/ProfileEditorViewModel.cs, tests/BS2BG.Tests/ProfileEditorViewModelTests.cs</files>
  <read_first>
    - src/BS2BG.Core/Generation/ProfileDefinitionService.cs
    - src/BS2BG.Core/Models/CustomProfileDefinition.cs
    - openspec/specs/reactive-mvvm-conventions/spec.md
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-UI-SPEC.md
  </read_first>
  <behavior>
    - Editor sections expose metadata, Defaults, Multipliers, Inverted Sliders, and Validation per UI-SPEC and D-05.
    - Save is disabled when validation has blockers; Validate remains enabled when unsaved edits exist.
    - Blank profile with unique name and empty tables shows `Blank profiles are allowed. Add defaults, multipliers, and inverted sliders when you are ready.` and can be saved per D-08.
    - Broad finite numeric values are accepted; malformed/nonnumeric text produces validation blockers per D-06/D-07.
  </behavior>
  <action>Create `ProfileEditorViewModel` with editable row models for defaults (`slider`, `valueSmall`, `valueBig`), multipliers (`slider`, `value`), and inverted sliders (`slider`, `isInverted` or equivalent). Use `[Reactive]`, `ObservableAsProperty`, and `ReactiveCommand` with observable canExecute. Validation cadence: build an in-memory `CustomProfileDefinition` candidate directly from row state and validate it through a `ProfileDefinitionService` candidate-validation overload or shared validator, not by serializing to JSON and parsing on every keystroke; JSON import/export still uses `ValidateProfileJson`/`ExportProfileJson`. Trigger validation when the user invokes `Validate Profile`, when Save canExecute is evaluated after throttled row edits, and immediately on metadata/table changes in tests through the immediate scheduler. `IsValid` and command gating must reflect the current unsaved editor buffer, not only the last-saved profile. Add `SearchText`/filter support for slider rows so large tables can be filtered by slider name without changing the underlying profile data. Surface validation row text exactly from UI-SPEC for duplicate-name, invalid JSON import, nonnumeric value, duplicate slider, extreme numeric caution, and blank-profile info; distinguish validation blockers from store/write failures in separate status rows. Save failure from the store must keep the editor open with unsaved changes intact and show `Profile could not be saved. Review the validation messages below; malformed or ambiguous profile data is not added to the catalog.` Do not clamp finite values; do not block empty tables when name is unique. Addresses review concern: validation cadence avoids JSON round-trips for every keystroke, large slider tables are searchable, and save failure behavior is testable.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~ProfileEditorViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileEditorViewModel.cs` contains `SaveProfileCommand` and `ValidateProfileCommand`.
    - `ProfileEditorViewModel.cs` contains the exact copy `Blank profiles are allowed. Add defaults, multipliers, and inverted sliders when you are ready.`
    - Tests assert save canExecute false with blocker and true for valid blank profile.
    - Tests assert validation transitions Valid → Blocker → Valid as the current buffer changes.
    - Tests assert `SearchText` filters editor rows without removing them from the saved candidate.
    - Tests assert save IO failure preserves unsaved editor rows and leaves the profile uncommitted.
    - Focused test command exits 0.
  </acceptance_criteria>
  <done>Profile editor enforces Core validation and Phase 4 authoring rules while following ReactiveUI conventions.</done>
</task>

<task type="auto">
  <name>Task 3: Wire profile manager into dependency injection</name>
  <files>src/BS2BG.App/AppBootstrapper.cs, src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs, tests/BS2BG.Tests/ProfileManagerViewModelTests.cs</files>
  <read_first>
    - src/BS2BG.App/AppBootstrapper.cs
    - src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs
    - src/BS2BG.App/ViewModels/MainWindowViewModel.cs
  </read_first>
  <action>Register `IProfileManagementDialogService`, `ProfileManagementDialogService`, `ProfileManagerViewModel`, and any editor factory dependencies in `AppBootstrapper`. Use singleton or transient lifetimes consistent with existing App services: window-bound service singleton with interface mapping, manager singleton because it represents workspace state. Inject `ITemplateProfileCatalogService` rather than a stale `TemplateProfileCatalog` where refresh-aware behavior is required. Ensure design-time/test constructors provide null/empty service implementations.</action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~ProfileManagerViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `AppBootstrapper.cs` contains `AddSingleton<ProfileManagerViewModel>`.
    - `AppBootstrapper.cs` contains `IProfileManagementDialogService` registration.
    - Focused test command exits 0.
  </acceptance_criteria>
  <done>Profile manager/editor services are resolvable for shell/UI integration plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user action/file picker → App ViewModel | User-selected JSON and save destinations enter App orchestration. |
| App ViewModel → profile store | Only validated definitions may be written to local profile storage. |
| bundled catalog entry → editor | Read-only bundled data must be copied before edit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | ProfileManagerViewModel.ImportProfileCommand | mitigate | Validate imported JSON with Core service before store write or catalog refresh. |
| T-04-05-02 | Elevation of Privilege | ProfileManagerViewModel bundled rows | mitigate | Disable edit/delete/save for `ProfileSourceKind.Bundled`; only copy-as-custom is allowed. |
| T-04-05-03 | Tampering | ProfileEditorViewModel.SaveProfileCommand | mitigate | Gate save on validation blockers and route writes through UserProfileStore, not direct ViewModel file I/O. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~ProfileManagerViewModelTests|FullyQualifiedName~ProfileEditorViewModelTests"` and `dotnet build BS2BG.sln`.
</verification>

<success_criteria>
Users have test-covered App workflows for importing, copying, creating blank, editing metadata/tables, validating, saving, exporting, and deleting custom profiles, while bundled profiles stay read-only.
</success_criteria>

<output>
After completion, create `.planning/phases/04-profile-extensibility-and-controlled-customization/04-05-SUMMARY.md`
</output>
