---
phase: 04-profile-extensibility-and-controlled-customization
plan: 07
type: execute
wave: 5
depends_on: [04-04, 04-05, 04-06]
files_modified:
  - src/BS2BG.App/ViewModels/TemplatesViewModel.cs
  - src/BS2BG.App/ViewModels/DiagnosticsViewModel.cs
  - src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs
  - openspec/specs/template-generation-flow/spec.md
  - openspec/specs/project-roundtrip/spec.md
  - openspec/specs/profile-extensibility/spec.md
  - tests/BS2BG.Tests/TemplatesViewModelTests.cs
  - tests/BS2BG.Tests/DiagnosticsViewModelTests.cs
  - tests/BS2BG.Tests/ProfileManagerViewModelTests.cs
autonomous: true
requirements: [EXT-01, EXT-03, EXT-04, EXT-05]
user_setup: []
must_haves:
  truths:
    - "D-10/D-11: Missing profile recovery is visible in Diagnostics/Profiles and explicit actions import matching, remap to installed, use project copy, or keep unresolved."
    - "D-12: Import Matching Profile resolves only by internal display-name exact match ignoring case, never filename."
    - "D-17: Selected custom or embedded profiles can be exported as standalone JSON."
    - "Addresses review concern: diagnostics recovery commands, cross-VM coordination, referenced delete behavior, and OpenSpec capability deltas are explicit."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/DiagnosticsViewModel.cs"
      provides: "Binding-ready missing-profile recovery action rows"
    - path: "src/BS2BG.App/ViewModels/TemplatesViewModel.cs"
      provides: "Undo-aware project preset profile remap/adoption commands"
    - path: "openspec/specs/profile-extensibility/spec.md"
      provides: "Capability spec for custom profile management behavior"
  key_links:
    - from: "DiagnosticsViewModel recovery action"
      to: "TemplatesViewModel remap/adopt command"
      via: "MainWindowViewModel or recovery coordinator command routing"
      pattern: "Remap to Installed Profile"
    - from: "Import Matching Profile"
      to: "ProfileRecoveryDiagnosticsService.CanResolveMissingReference"
      via: "internal display-name exact-match validation"
      pattern: "CanResolveMissingReference"
---

<objective>
Expose explicit missing-profile recovery actions and profile-sharing commands after project-open conflicts are transactional.

Purpose: Let users resolve or intentionally keep unresolved custom-profile references without diagnostics mutating data by default.
Output: Recovery commands, undo-aware remap/adoption behavior, selected-profile export integration, tests, and OpenSpec deltas.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-CONTEXT.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-RESEARCH.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-REVIEWS.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-UI-SPEC.md
@src/BS2BG.App/ViewModels/TemplatesViewModel.cs
@src/BS2BG.App/ViewModels/DiagnosticsViewModel.cs
@src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs
@openspec/specs/template-generation-flow/spec.md
@openspec/specs/project-roundtrip/spec.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-04-SUMMARY.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-05-SUMMARY.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-06-SUMMARY.md

<interfaces>
Required recovery action labels:
```text
Import Matching Profile
Remap to Installed Profile
Use Project Copy
Keep Unresolved for Now
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add diagnostics/profile recovery action routing</name>
  <files>src/BS2BG.App/ViewModels/DiagnosticsViewModel.cs, src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs, tests/BS2BG.Tests/DiagnosticsViewModelTests.cs, tests/BS2BG.Tests/ProfileManagerViewModelTests.cs</files>
  <read_first>
    - src/BS2BG.App/ViewModels/DiagnosticsViewModel.cs
    - src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs
    - src/BS2BG.Core/Diagnostics/ProfileRecoveryDiagnosticsService.cs
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-UI-SPEC.md
  </read_first>
  <behavior>
    - Diagnostics exposes action labels `Import Matching Profile`, `Remap to Installed Profile`, `Use Project Copy` when available, and `Keep Unresolved for Now`.
    - Diagnostics remains read-only until an explicit recovery action command is invoked.
    - `Import Matching Profile` validates internal display-name exact match before saving a profile and leaves unresolved state visible when names differ.
    - `Keep Unresolved for Now` preserves visible fallback information per D-10.
  </behavior>
  <action>Add binding-ready recovery action rows to `DiagnosticsViewModel` using `ProfileRecoveryDiagnostic.Code`/action kinds from Plan 04, with exact labels from the interfaces block. Route actions through `MainWindowViewModel` or a small profile recovery coordinator service so Diagnostics does not directly own file-pickers, local-store writes, or Templates mutations. Implement `Import Matching Profile` by using `IProfileManagementDialogService.PickProfileImportFilesAsync`, validating JSON with `ProfileDefinitionService`, checking `ProfileRecoveryDiagnosticsService.CanResolveMissingReference`, saving only exact internal display-name matches through `IUserProfileStore`, then calling `ITemplateProfileCatalogService.Refresh()`. If the imported internal `Name` differs from the missing reference, show the UI-SPEC exact-match note and keep the reference unresolved. Implement `Use Project Copy` by activating the Plan 06 project-scoped overlay from `ProjectModel.CustomProfiles` without writing the embedded definition to the local store. Implement `Keep Unresolved for Now` as an explicit acknowledgement/status update only; it must not suppress future fallback/recovery diagnostics. Addresses review concern: cross-VM coordination and explicit diagnostics mutation boundary are specified.</action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~DiagnosticsViewModelTests|FullyQualifiedName~ProfileManagerViewModelTests"</automated>
  </verify>
  <acceptance_criteria>
    - `DiagnosticsViewModel.cs` contains `Import Matching Profile`, `Remap to Installed Profile`, and `Keep Unresolved for Now` labels or row data.
    - Tests assert filename/internal-name mismatch does not resolve missing reference.
    - Tests assert Use Project Copy enables catalog lookup through project-scoped overlay without local store writes.
    - Tests assert keep-unresolved leaves fallback information visible.
  </acceptance_criteria>
  <done>Missing-profile recovery actions are visible, exact-match based, and explicit rather than automatic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add undo-aware profile remap/adoption and selected JSON export</name>
  <files>src/BS2BG.App/ViewModels/TemplatesViewModel.cs, src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs, tests/BS2BG.Tests/TemplatesViewModelTests.cs, tests/BS2BG.Tests/ProfileManagerViewModelTests.cs</files>
  <read_first>
    - src/BS2BG.App/ViewModels/TemplatesViewModel.cs
    - src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs
    - src/BS2BG.Core/Generation/ProfileDefinitionService.cs
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-UI-SPEC.md
  </read_first>
  <behavior>
    - `Remap to Installed Profile` changes affected preset `ProfileName`, marks project dirty, and records undo/redo.
    - Undo after remap restores the previous unresolved profile reference and recovery diagnostics reappear.
    - `Export Profile JSON` is available only for selected custom or embedded profiles and writes standalone JSON through `ProfileDefinitionService.ExportProfileJson`.
  </behavior>
  <action>Add a Templates/profile workflow method or coordinator command for remapping affected project preset profile references to an installed catalog profile. Record `UndoRedoService.Record` around `SliderPreset.ProfileName` changes for explicit recovery remaps after a project is open; refresh missing-default rows, preview, selector state, and neutral fallback information after remap/undo/redo. Undoing the remap must restore the previous unresolved profile reference and cause the recovery diagnostic/fallback information to reappear. Add selected-profile export to `ProfileManagerViewModel`: enabled for `ProfileSourceKind.LocalCustom` and `ProfileSourceKind.EmbeddedProject`, disabled for bundled and missing rows, uses `IProfileManagementDialogService.PickProfileExportPathAsync`, writes `ProfileDefinitionService.ExportProfileJson` output, and reports `Profile JSON exported.` on success. Do not expand export into Phase 5 portable bundle behavior. Addresses review concerns: undo/dirty behavior for recovery remap and D-17 standalone export are concrete.</action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~TemplatesViewModelTests|FullyQualifiedName~ProfileManagerViewModelTests"</automated>
  </verify>
  <acceptance_criteria>
    - `TemplatesViewModel.cs` contains a remap/adopt method that calls `undoRedo.Record` or the existing undo-recording API.
    - Tests assert undo after remap restores the missing profile name and recovery diagnostic visibility.
    - `ProfileManagerViewModel.cs` contains `ExportProfileCommand` using `ExportProfileJson` and export success copy `Profile JSON exported.`
    - Tests assert export disabled for bundled rows and enabled for custom/embedded rows.
  </acceptance_criteria>
  <done>Recovery remaps are reversible project mutations, and selected profile JSON export satisfies D-17 without becoming a portable bundle feature.</done>
</task>

<task type="auto">
  <name>Task 3: Update OpenSpec capability specs for profile extensibility</name>
  <files>openspec/specs/profile-extensibility/spec.md, openspec/specs/project-roundtrip/spec.md, openspec/specs/template-generation-flow/spec.md</files>
  <read_first>
    - openspec/specs/project-roundtrip/spec.md
    - openspec/specs/template-generation-flow/spec.md
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-CONTEXT.md
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-REVIEWS.md
  </read_first>
  <action>Create or update `openspec/specs/profile-extensibility/spec.md` with Phase 4 behavior: bundled profiles are read-only; custom profiles live under user-local storage; duplicate display names are rejected case-insensitively; import/save/catalog inclusion is strict validation-gated; projects embed only referenced custom profiles; missing profile recovery is neutral/non-blocking; embedded/local conflicts require explicit decisions; selected custom/embedded profiles can be exported as JSON. Update `project-roundtrip/spec.md` with optional `CustomProfiles` section plus legacy field preservation, and update `template-generation-flow/spec.md` with project-scoped overlays and visible fallback behavior. Addresses review concern: OpenSpec capability deltas are captured before UI verification.</action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~TemplatesViewModelTests|FullyQualifiedName~DiagnosticsViewModelTests|FullyQualifiedName~ProfileManagerViewModelTests"</automated>
  </verify>
  <acceptance_criteria>
    - `openspec/specs/profile-extensibility/spec.md` contains `CustomProfiles`, `Profile conflict found`, and `Import Matching Profile`.
    - `openspec/specs/project-roundtrip/spec.md` contains `CustomProfiles` and `legacy` field preservation wording.
    - `openspec/specs/template-generation-flow/spec.md` contains project-scoped profile overlay or equivalent wording.
  </acceptance_criteria>
  <done>Capability specs document the new profile extensibility behaviors and cross-capability effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recovery UI action → project/local profile state | Explicit buttons can import files, activate overlays, or remap preset references. |
| selected profile → export path | User chooses a destination for standalone custom profile JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Spoofing | Import Matching Profile | mitigate | Use internal display-name exact match ignoring case; filenames do not resolve references. |
| T-04-07-02 | Repudiation | Remap to Installed Profile | mitigate | Record undo/redo and dirty state for profile reference changes. |
| T-04-07-03 | Information Disclosure | Export Profile JSON | mitigate | Enable export only for selected custom/embedded profile data, not unrelated local profiles or full project bundles. |
</threat_model>

<verification>
Run `dotnet test --filter "FullyQualifiedName~DiagnosticsViewModelTests|FullyQualifiedName~TemplatesViewModelTests|FullyQualifiedName~ProfileManagerViewModelTests"` and `dotnet build BS2BG.sln`.
</verification>

<success_criteria>
Missing custom profile recovery is visible and exact-match based; remap/adoption is undo-aware; selected custom/embedded profile export works; OpenSpec specs reflect Phase 4 behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-profile-extensibility-and-controlled-customization/04-07-SUMMARY.md`
</output>
