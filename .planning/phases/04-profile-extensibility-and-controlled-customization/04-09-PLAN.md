---
phase: 04-profile-extensibility-and-controlled-customization
plan: 09
type: execute
wave: 7
depends_on: [04-08]
files_modified:
  - src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs
  - src/BS2BG.App/ViewModels/ProfileEditorViewModel.cs
  - src/BS2BG.App/Views/MainWindow.axaml
  - tests/BS2BG.Tests/ProfileManagerViewModelTests.cs
  - tests/BS2BG.Tests/MainWindowHeadlessTests.cs
autonomous: true
gap_closure: true
requirements: [EXT-01, EXT-02, EXT-05]
must_haves:
  truths:
    - "User can select any rendered bundled, local custom, embedded project, or missing profile row before invoking row-scoped profile actions."
    - "Copy-as-custom uses the selected bundled profile data instead of clearing selection before the copy source is read."
    - "Selected custom or embedded profile JSON export preserves the profile Name and Game metadata."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs"
      provides: "Selection-safe profile manager row actions and metadata-preserving export/copy paths"
    - path: "src/BS2BG.App/ViewModels/ProfileEditorViewModel.cs"
      provides: "Editor construction that carries Game metadata from source entries"
    - path: "src/BS2BG.App/Views/MainWindow.axaml"
      provides: "Selectable Profiles workspace rows wired to ProfileManagerViewModel.SelectedProfile"
  key_links:
    - from: "src/BS2BG.App/Views/MainWindow.axaml"
      to: "ProfileManagerViewModel.SelectedProfile"
      via: "Selectable row binding or explicit row command"
      pattern: "SelectedProfile|SelectProfileCommand"
    - from: "ProfileManagerViewModel.CopyBundledProfile"
      to: "ProfileEditorViewModel.FromEntry"
      via: "captured source row before selection clear"
      pattern: "var .*selected.*= SelectedProfile"
---

<objective>
Close Phase 4 verification gaps around profile-row targeting, copy-as-custom, and metadata-preserving selected export.

Purpose: The Profiles workspace currently renders real profile rows but user-visible copy/edit/export actions cannot reliably target them, and copy/export can lose the source profile's Game metadata. This plan turns the rendered rows into actionable selection sources and fixes the copy/export data path without changing Core profile math or bundled profile files.
Output: Selection-safe Profile Manager workflows, selectable Profiles UI rows, and focused regression coverage for EXT-01, EXT-02, and EXT-05 gaps from `04-VERIFICATION.md`.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-CONTEXT.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-VERIFICATION.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-05-SUMMARY.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-07-SUMMARY.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-08-SUMMARY.md

<interfaces>
Existing contracts to preserve:
- `ProfileManagerViewModel` owns source-aware profile row projection and commands for import, create blank, copy bundled, validate, save, export, and delete.
- `ProfileEditorViewModel` builds editable candidates from metadata plus `Defaults`, `Multipliers`, and `Inverted` row state.
- The Profiles workspace in `MainWindow.axaml` must keep Avalonia compiled bindings with `x:DataType` on templates and must not use WPF-only constructs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make profile row selection drive manager actions</name>
  <files>src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs, src/BS2BG.App/Views/MainWindow.axaml, tests/BS2BG.Tests/ProfileManagerViewModelTests.cs, tests/BS2BG.Tests/MainWindowHeadlessTests.cs</files>
  <read_first>
    - src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs
    - src/BS2BG.App/Views/MainWindow.axaml
    - tests/BS2BG.Tests/ProfileManagerViewModelTests.cs
    - tests/BS2BG.Tests/MainWindowHeadlessTests.cs
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-VERIFICATION.md
  </read_first>
  <behavior>
    - Selecting a bundled/local/embedded/missing profile row in the Profiles workspace updates `ProfileManagerViewModel.SelectedProfile` to that row.
    - Row selection remains explicit and accessible: either use a selectable control with `SelectedItem` binding or add a row command/button that assigns `SelectedProfile`; every actionable row target has an automation name.
    - Source groups remain visible and keep existing source/recovery copy; no color-only state is introduced.
  </behavior>
  <action>
    Add a selection mechanism for every rendered Profiles source row per verification gap lines 14-18 and D-04/D-17: convert the current non-selecting `ItemsControl` row surfaces to a selectable Avalonia control with `SelectedItem="{Binding SelectedProfile}"` where practical, or add an explicit row-level `Select`/row command that assigns the clicked `ProfileManagerEntryViewModel` to `SelectedProfile`. Keep compiled binding requirements (`x:DataType` on `DataTemplate`s), existing source groups (`BundledProfiles`, `CustomProfiles`, `EmbeddedProjectProfiles`, `MissingProfiles`), and neutral missing-profile copy. If adding a command, implement it as `ReactiveCommand<ProfileManagerEntryViewModel, Unit>` with observable gating where needed; do not introduce `RelayCommand` or direct dispatcher calls.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ProfileManagerViewModelTests|FullyQualifiedName~MainWindowHeadlessTests"</automated>
  </verify>
  <acceptance_criteria>
    - `src/BS2BG.App/Views/MainWindow.axaml` contains either `SelectedItem="{Binding SelectedProfile` or a binding to a `SelectProfileCommand` for profile rows.
    - `tests/BS2BG.Tests/ProfileManagerViewModelTests.cs` or `tests/BS2BG.Tests/MainWindowHeadlessTests.cs` contains an assertion that selecting a non-initial profile row changes `SelectedProfile`.
    - `src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs` contains no `RelayCommand` or `AsyncRelayCommand` references.
  </acceptance_criteria>
  <done>Users can target arbitrary displayed profile rows before invoking edit/export/delete/copy actions; EXT-01 and EXT-05 row-targeting blockers are closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve selected source data for copy and export</name>
  <files>src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs, src/BS2BG.App/ViewModels/ProfileEditorViewModel.cs, tests/BS2BG.Tests/ProfileManagerViewModelTests.cs</files>
  <read_first>
    - src/BS2BG.App/ViewModels/ProfileManagerViewModel.cs
    - src/BS2BG.App/ViewModels/ProfileEditorViewModel.cs
    - tests/BS2BG.Tests/ProfileManagerViewModelTests.cs
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-VERIFICATION.md
  </read_first>
  <behavior>
    - `CopyBundledProfile` captures the selected bundled row before any selection clearing or editor replacement, then seeds the editor from that captured row.
    - `ProfileEditorViewModel.FromEntry` and `ProfileManagerEntryViewModel.ToCustomProfileDefinition` preserve `Game` metadata from local custom and embedded project profiles.
    - Selected JSON export writes profile JSON with the same `Name` and `Game` metadata as the selected local custom or embedded project row.
  </behavior>
  <action>
    Fix the copy/export metadata path per verification gaps lines 12-18, 23-31, and 52-56. In `CopyBundledProfile`, store `var selected = SelectedProfile;` before clearing or replacing selection/editor state, and derive the copy name/table data from `selected` rather than from `SelectedProfile` after it may be null. Add a `Game` metadata property to `ProfileManagerEntryViewModel` if it is missing, populate it from bundled/local/embedded definitions, pass it into `ProfileEditorViewModel.FromEntry`, and use it in `ProfileManagerEntryViewModel.ToCustomProfileDefinition` and selected export/save paths. Do not infer identity from filenames; continue using internal display name per D-12.
  </action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~ProfileManagerViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileManagerViewModel.cs` contains a captured selected-row local in `CopyBundledProfile` before any `SelectedProfile = null` assignment.
    - `ProfileEditorViewModel.cs` no longer hardcodes `string.Empty` for `Game` when creating an editor from an entry.
    - `ProfileManagerViewModelTests.cs` asserts selected profile export or save preserves a non-empty `Game` value.
  </acceptance_criteria>
  <done>Copy-as-custom opens a real bundled-profile copy, and selected JSON export/save no longer drops Game metadata; EXT-01, EXT-02, and EXT-05 metadata blockers are closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profiles UI row -> ViewModel command | User selection determines which profile definition is copied, edited, deleted, or exported. |
| Selected profile -> JSON export | Local/embedded profile metadata crosses from app state to a user-selected file path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Tampering | `CopyBundledProfile` | mitigate | Capture the selected row before clearing selection so copy data cannot be replaced by an empty/default editor state. |
| T-04-09-02 | Repudiation | Profile row actions | mitigate | Bind row selection/action state explicitly and cover selection changes in tests so user-targeted operations have deterministic source rows. |
| T-04-09-03 | Information Disclosure | Profile JSON export | accept | Export writes only the user-selected local/embedded profile through existing picker paths; no private unrelated profiles are exported by this plan. |
</threat_model>

<verification>
Run focused regression: `dotnet test --filter "FullyQualifiedName~ProfileManagerViewModelTests|FullyQualifiedName~MainWindowHeadlessTests"`.
</verification>

<success_criteria>
- Verification gap items for copy-as-custom, non-selectable profile rows, and Game metadata loss are covered by tests and code changes.
- No bundled profile JSON files, sacred formatter/export files, or golden expected fixtures are modified.
- Avalonia compiled bindings remain valid and `dotnet build BS2BG.sln` succeeds when run after focused tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-profile-extensibility-and-controlled-customization/04-09-SUMMARY.md`.
</output>
