---
phase: 04-profile-extensibility-and-controlled-customization
plan: 10
type: execute
wave: 8
depends_on: [04-09]
files_modified:
  - src/BS2BG.App/ViewModels/ProfileEditorViewModel.cs
  - src/BS2BG.App/Views/MainWindow.axaml
  - tests/BS2BG.Tests/ProfileEditorViewModelTests.cs
  - tests/BS2BG.Tests/MainWindowHeadlessTests.cs
autonomous: true
gap_closure: true
requirements: [EXT-02]
must_haves:
  truths:
    - "User can author blank or existing custom profiles by adding and removing Defaults, Multipliers, and Inverted rows in the Profiles workspace."
    - "Editing any metadata or slider-table row refreshes validation state and Save command availability before the user saves."
    - "Validation remains strict about malformed/ambiguous data while preserving broad finite numeric values and blank custom-profile starts."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/ProfileEditorViewModel.cs"
      provides: "Add/remove row commands and live validation subscriptions for all editable profile tables"
    - path: "src/BS2BG.App/Views/MainWindow.axaml"
      provides: "Compiled-bound add/remove controls for Defaults, Multipliers, and Inverted editor sections"
    - path: "tests/BS2BG.Tests/ProfileEditorViewModelTests.cs"
      provides: "Regression coverage for table authoring and row-edit validation refresh"
  key_links:
    - from: "ProfileEditorViewModel row properties"
      to: "ProfileEditorViewModel validation state"
      via: "property-change subscription and ValidateProfileCommand/SaveProfileCommand gating"
      pattern: "PropertyChanged.*Validate|WhenAnyValue.*IsValid"
    - from: "MainWindow.axaml editor buttons"
      to: "ProfileEditorViewModel add/remove commands"
      via: "compiled-bound command bindings"
      pattern: "AddDefault|RemoveDefault|AddMultiplier|RemoveMultiplier|AddInverted|RemoveInverted"
---

<objective>
Close Phase 4 verification gaps around editable profile table authoring and live validation refresh.

Purpose: Blank custom profiles are allowed by D-08, but the current UI can only display existing rows and row value edits can leave validation/save gating stale. This plan completes the supported editor workflow for metadata plus `Defaults`, `Multipliers`, and `Inverted` entries without clamping modder-provided numeric values.
Output: Validated add/remove row commands, compiled-bound editor controls, and regression tests for EXT-02.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-CONTEXT.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-RESEARCH.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-VERIFICATION.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-05-SUMMARY.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-09-SUMMARY.md

<interfaces>
Required editor semantics from locked decisions:
- D-05: editable metadata plus all current slider tables: `Defaults`, `Multipliers`, and `Inverted`.
- D-06: reject malformed schema, blank/duplicate slider names, nonnumeric values, ambiguous profile names, and invalid profile data before save/catalog inclusion.
- D-07: allow broad finite numeric values; do not clamp defaults to 0-1 or multipliers to positive values.
- D-08: allow a blank custom profile, so add-row controls must be sufficient to build a profile from an empty starting point.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add editor table add/remove commands and validation tests</name>
  <files>src/BS2BG.App/ViewModels/ProfileEditorViewModel.cs, tests/BS2BG.Tests/ProfileEditorViewModelTests.cs</files>
  <read_first>
    - src/BS2BG.App/ViewModels/ProfileEditorViewModel.cs
    - tests/BS2BG.Tests/ProfileEditorViewModelTests.cs
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-CONTEXT.md
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-VERIFICATION.md
  </read_first>
  <behavior>
    - `AddDefaultCommand`, `RemoveDefaultCommand`, `AddMultiplierCommand`, `RemoveMultiplierCommand`, `AddInvertedCommand`, and `RemoveInvertedCommand` exist or equivalent clearly named ReactiveCommands exist.
    - Add commands insert editable row models into the correct table collections using non-conflicting initial slider-name text where needed.
    - Remove commands remove only the targeted row and immediately refresh validation/status rows.
    - Tests cover blank profile -> add rows -> valid candidate, duplicate/blank slider rejection, and broad finite numeric values not being clamped/rejected solely for range.
  </behavior>
  <action>
    Implement ReactiveUI command surfaces for authoring every editable profile table per D-05/D-08 and verification gaps lines 27-32. Use `ReactiveCommand.Create*` or `ReactiveCommand.CreateFromTask`; do not introduce `RelayCommand`. Add/remove commands must update the underlying row collections and call the existing validation refresh path so `IsValid`, `ValidationRows`, and `SaveProfileCommand` availability reflect the current table state. Preserve strict malformed/duplicate validation from D-06 and broad finite float acceptance from D-07.
  </action>
  <verify>
    <automated>dotnet test --filter FullyQualifiedName~ProfileEditorViewModelTests</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileEditorViewModel.cs` contains add and remove command properties for Defaults, Multipliers, and Inverted table rows.
    - `ProfileEditorViewModelTests.cs` contains tests for adding and removing at least one row in each table type.
    - `ProfileEditorViewModelTests.cs` contains an assertion that a broad finite numeric value outside common 0-1 defaults remains accepted when otherwise well-formed.
  </acceptance_criteria>
  <done>Profile editor ViewModel can build and revise all supported table sections from blank or existing profile state, with validation updated after each table mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh validation from row property edits and wire editor controls</name>
  <files>src/BS2BG.App/ViewModels/ProfileEditorViewModel.cs, src/BS2BG.App/Views/MainWindow.axaml, tests/BS2BG.Tests/ProfileEditorViewModelTests.cs, tests/BS2BG.Tests/MainWindowHeadlessTests.cs</files>
  <read_first>
    - src/BS2BG.App/ViewModels/ProfileEditorViewModel.cs
    - src/BS2BG.App/Views/MainWindow.axaml
    - tests/BS2BG.Tests/ProfileEditorViewModelTests.cs
    - tests/BS2BG.Tests/MainWindowHeadlessTests.cs
    - .planning/phases/04-profile-extensibility-and-controlled-customization/04-VERIFICATION.md
  </read_first>
  <behavior>
    - Changing a row slider name/value immediately refreshes `IsValid`, `ValidationRows`, and `SaveProfileCommand.CanExecute` state.
    - Existing rows loaded during editor construction and rows added later are both subscribed; removed rows do not keep affecting validation after removal.
    - Profiles UI exposes add/remove controls for Defaults, Multipliers, and Inverted sections with compiled bindings and automation names.
  </behavior>
  <action>
    Subscribe to row model property changes for all initial and newly added Defaults, Multipliers, and Inverted rows. On row property changes, run the same in-memory candidate validation used by save gating. Dispose or detach subscriptions when rows are removed or the editor is replaced so stale row edits do not affect current validation. In `MainWindow.axaml`, add visible add/remove controls for each table section and bind them to the ViewModel commands from Task 1. Keep `x:DataType` declarations on all roots/templates and use explicit text labels/automation names such as `Add default slider`, `Remove default slider`, `Add multiplier slider`, `Remove multiplier slider`, `Add inverted slider`, and `Remove inverted slider`.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~ProfileEditorViewModelTests|FullyQualifiedName~MainWindowHeadlessTests"</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileEditorViewModelTests.cs` contains a test where changing an existing row value changes validation or save availability without calling a manual validate command.
    - `MainWindow.axaml` contains visible command bindings or buttons for adding and removing Defaults, Multipliers, and Inverted rows.
    - Focused tests pass without modifying `tests/fixtures/expected/**` or sacred formatter/export files.
  </acceptance_criteria>
  <done>Profile table edits are fully authorable through the UI and validation/save state tracks current row edits; EXT-02 editor blockers are closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editable row text -> Core validation | User-entered slider names and numeric text become candidate profile definitions only after validation. |
| Editor UI -> local profile save | Save command availability controls whether edited profile data can enter the local catalog. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10-01 | Tampering | `ProfileEditorViewModel` | mitigate | Re-run validation on every metadata/table row mutation before enabling save. |
| T-04-10-02 | Denial of Service | Row subscriptions | mitigate | Detach subscriptions for removed rows/current editor replacement to avoid stale validation loops or leaks. |
| T-04-10-03 | Tampering | Numeric validation | mitigate | Reject malformed/nonnumeric values but preserve D-07 broad finite numeric acceptance; tests must prevent accidental range clamping. |
</threat_model>

<verification>
Run focused editor/UI regression: `dotnet test --filter "FullyQualifiedName~ProfileEditorViewModelTests|FullyQualifiedName~MainWindowHeadlessTests"`.
</verification>

<success_criteria>
- Defaults, Multipliers, and Inverted rows can be added and removed from blank/custom profiles.
- Row property edits update validation and save gating automatically.
- Strict validation and broad finite numeric acceptance remain consistent with D-06 through D-08.
</success_criteria>

<output>
After completion, create `.planning/phases/04-profile-extensibility-and-controlled-customization/04-10-SUMMARY.md`.
</output>
