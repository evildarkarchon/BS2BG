---
phase: 05-automation-sharing-and-release-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Directory.Packages.props
  - BS2BG.sln
  - src/BS2BG.Cli/BS2BG.Cli.csproj
  - src/BS2BG.Cli/Program.cs
  - src/BS2BG.Core/Automation/HeadlessGenerationContracts.cs
  - tests/BS2BG.Tests/CliGenerationTests.cs
autonomous: true
requirements: [AUTO-01]
user_setup: []
must_haves:
  truths:
    - "User has a dedicated BS2BG.Cli executable separate from BS2BG.App.exe."
    - "CLI command contracts require explicit project, output directory, output intent, and overwrite choice."
  artifacts:
    - path: "src/BS2BG.Cli/BS2BG.Cli.csproj"
      provides: "Dedicated net10.0 console executable with System.CommandLine"
    - path: "src/BS2BG.Core/Automation/HeadlessGenerationContracts.cs"
      provides: "Typed headless generation request/result/intent/exit-code contracts"
    - path: "tests/BS2BG.Tests/CliGenerationTests.cs"
      provides: "Executable CLI contract tests"
  key_links:
    - from: "src/BS2BG.Cli/Program.cs"
      to: "BS2BG.Core.Automation.HeadlessGenerationRequest"
      via: "System.CommandLine parse action creates typed request"
      pattern: "new HeadlessGenerationRequest"
---

<objective>
Create the dedicated CLI executable foundation and typed Core automation contracts for validation-first, explicit-output headless generation.

Purpose: Satisfies D-01 through D-05 and AUTO-01 without adding a headless flag to the Avalonia app or hand-rolling argument parsing.
Output: New CLI project, central package pin, solution entry, Core contracts, and first CLI contract tests.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md
@.planning/phases/05-automation-sharing-and-release-trust/05-RESEARCH.md
@.planning/phases/05-automation-sharing-and-release-trust/05-UI-SPEC.md
@Directory.Packages.props
@BS2BG.sln
@src/BS2BG.Core/BS2BG.Core.csproj
@src/BS2BG.App/AppBootstrapper.cs

<interfaces>
From research: use System.CommandLine 2.0.7 stable, not manual args parsing. Exit-code recommendation: 0=success, 1=parse/usage, 2=validation blocked, 3=overwrite refused, 4=I/O failure.

Existing Core services to be consumed in later plans: ProjectFileService, TemplateGenerationService, MorphGenerationService, ProjectValidationService, BodyGenIniExportWriter, BosJsonExportWriter.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI project and parser package contract</name>
  <files>Directory.Packages.props, BS2BG.sln, src/BS2BG.Cli/BS2BG.Cli.csproj, src/BS2BG.Cli/Program.cs, tests/BS2BG.Tests/CliGenerationTests.cs</files>
  <read_first>Directory.Packages.props; BS2BG.sln; src/BS2BG.App/BS2BG.App.csproj; tests/BS2BG.Tests/BS2BG.Tests.csproj; .planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md D-01/D-03; .planning/phases/05-automation-sharing-and-release-trust/05-RESEARCH.md Standard Stack and Pattern 1</read_first>
  <behavior>
    - Test 1: BS2BG.Cli project exists and is referenced by the solution as a separate executable from BS2BG.App.exe per D-01.
    - Test 2: Running CLI with missing required options returns parse/usage exit code 1 and writes help/usage text.
    - Test 3: CLI command metadata includes explicit output intent values: bodygen, bos, all per D-03.
  </behavior>
  <action>Add PackageVersion Include="System.CommandLine" Version="2.0.7" to Directory.Packages.props. Create src/BS2BG.Cli/BS2BG.Cli.csproj as Microsoft.NET.Sdk, OutputType Exe, TargetFramework net10.0, RootNamespace BS2BG.Cli, LangVersion 14, PackageReference System.CommandLine, ProjectReference ..\BS2BG.Core\BS2BG.Core.csproj. Add the project to BS2BG.sln under the src solution folder. Create Program.cs with a root command named bs2bg and a generate command using System.CommandLine, not manual args parsing. Required options: --project FileInfo, --output DirectoryInfo, --intent with allowed values bodygen|bos|all, and --overwrite bool default false. Per D-01, do not modify BS2BG.App.exe startup.</action>
  <verify>
    <automated>dotnet test BS2BG.sln --filter FullyQualifiedName~CliGeneration</automated>
  </verify>
  <acceptance_criteria>Directory.Packages.props contains System.CommandLine Version="2.0.7"; src/BS2BG.Cli/BS2BG.Cli.csproj contains OutputType&gt;Exe&lt;, TargetFramework&gt;net10.0&lt;, and ProjectReference to BS2BG.Core; Program.cs contains Command("generate"), --project, --output, --intent, --overwrite, and no reference to Avalonia or BS2BG.App; CliGenerationTests.cs contains assertions for missing option exit code 1 and bodygen/bos/all intent names.</acceptance_criteria>
  <done>Dedicated CLI project builds as part of the solution and parser tests prove explicit output selection and script-friendly usage failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define headless generation contracts</name>
  <files>src/BS2BG.Core/Automation/HeadlessGenerationContracts.cs, tests/BS2BG.Tests/CliGenerationTests.cs, src/BS2BG.Cli/Program.cs</files>
  <read_first>src/BS2BG.Core/Diagnostics/ProjectValidationReport.cs; src/BS2BG.Core/Export/BodyGenIniExportWriter.cs; src/BS2BG.Core/Export/BosJsonExportWriter.cs; .planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md D-02 through D-05</read_first>
  <behavior>
    - Test 1: OutputIntent enum exposes BodyGen, BosJson, All.
    - Test 2: HeadlessGenerationExitCode enum exposes Success=0, UsageError=1, ValidationBlocked=2, OverwriteRefused=3, IoFailure=4.
    - Test 3: Program.cs maps parsed intent text to OutputIntent instead of passing strings into Core.</behavior>
  <action>Create namespace BS2BG.Core.Automation. Add public enum OutputIntent { BodyGen, BosJson, All }, public enum HeadlessGenerationExitCode { Success = 0, UsageError = 1, ValidationBlocked = 2, OverwriteRefused = 3, IoFailure = 4 }, public sealed record HeadlessGenerationRequest(string ProjectPath, string OutputDirectory, OutputIntent Intent, bool Overwrite), and public sealed record HeadlessGenerationResult(HeadlessGenerationExitCode ExitCode, string Message, IReadOnlyList&lt;string&gt; WrittenFiles, ProjectValidationReport? ValidationReport). Add XML doc comments for public types. Wire Program.cs parse action to create a HeadlessGenerationRequest and return UsageError for parser failures until Plan 02 supplies the service. Use these exact enum names so downstream plans can reference them.</action>
  <verify>
    <automated>dotnet test BS2BG.sln --filter FullyQualifiedName~CliGeneration</automated>
  </verify>
  <acceptance_criteria>HeadlessGenerationContracts.cs contains OutputIntent, HeadlessGenerationExitCode, HeadlessGenerationRequest, HeadlessGenerationResult; HeadlessGenerationExitCode values are exactly 0,1,2,3,4; Program.cs contains HeadlessGenerationRequest and no File.WriteAllText for templates.ini, morphs.ini, or BoS JSON.</acceptance_criteria>
  <done>Core automation contracts are concrete and ready for the generation service without output-writer duplication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args→Core request | Untrusted command-line paths and intent values become filesystem write requests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | Program.cs parser | mitigate | Use System.CommandLine typed FileInfo/DirectoryInfo/options and reject missing/invalid intent before creating HeadlessGenerationRequest. |
| T-05-01-02 | Elevation of Privilege | BS2BG.Cli project references | mitigate | CLI references BS2BG.Core only; it must not reference BS2BG.App, Avalonia, or desktop window services. |
</threat_model>

<verification>
Run dotnet build BS2BG.sln and dotnet test BS2BG.sln --filter FullyQualifiedName~CliGeneration.
</verification>

<success_criteria>
A separate BS2BG.Cli executable exists, uses System.CommandLine 2.0.7, exposes explicit generation intent, and compiles without Avalonia/App dependencies.
</success_criteria>

<output>
After completion, create .planning/phases/05-automation-sharing-and-release-trust/05-01-SUMMARY.md
</output>
