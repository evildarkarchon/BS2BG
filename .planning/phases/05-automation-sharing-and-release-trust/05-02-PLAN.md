---
phase: 05-automation-sharing-and-release-trust
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/BS2BG.Core/Automation/HeadlessGenerationService.cs
  - src/BS2BG.Core/Automation/HeadlessGenerationContracts.cs
  - src/BS2BG.Core/Generation/TemplateProfileCatalogFactory.cs
  - src/BS2BG.Core/Export/BosJsonExportPlanner.cs
  - src/BS2BG.Core/Export/BosJsonExportWriter.cs
  - src/BS2BG.Cli/Program.cs
  - tests/BS2BG.Tests/CliGenerationTests.cs
autonomous: true
requirements: [AUTO-01]
user_setup: []
must_haves:
  truths:
    - "CLI generation loads .jbs2bg projects and validates before writing."
    - "Blocker diagnostics produce a nonzero exit and no output writes."
    - "Existing BodyGen and BoS targets are refused unless --overwrite is supplied."
    - "D-02: CLI generation reuses Core loading, catalog, validation, generation, and export services without a second formatter/writer path."
    - "D-04: CLI validation runs before writes; Blocker diagnostics fail nonzero while Caution/Info report without blocking."
    - "D-05: Existing BodyGen and BoS targets require explicit overwrite before replacement."
  artifacts:
    - path: "src/BS2BG.Core/Automation/HeadlessGenerationService.cs"
      provides: "Validation-first headless generation orchestration over existing Core services"
    - path: "src/BS2BG.Cli/Program.cs"
      provides: "Generate command wired to Core service exit codes"
  key_links:
    - from: "HeadlessGenerationService"
      to: "ProjectValidationService.Validate"
      via: "pre-write validation"
      pattern: "ProjectValidationService\.Validate"
    - from: "HeadlessGenerationService"
      to: "BodyGenIniExportWriter/BosJsonExportWriter"
      via: "existing writer calls only"
      pattern: "bodyGenIniExportWriter\.Write|bosJsonExportWriter\.Write"
---

<objective>
Implement the headless generation service and wire the CLI generate command to the same Core validation, generation, and export writer paths used by the GUI.

Purpose: Delivers AUTO-01 while preserving D-02 validation/output parity, D-04 validation-first behavior, and D-05 overwrite safety.
Output: Tested Core service plus CLI command execution.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md
@.planning/phases/05-automation-sharing-and-release-trust/05-RESEARCH.md
@.planning/phases/05-automation-sharing-and-release-trust/05-01-SUMMARY.md
@src/BS2BG.Core/Automation/HeadlessGenerationContracts.cs
@src/BS2BG.Core/Serialization/ProjectFileService.cs
@src/BS2BG.Core/Generation/TemplateGenerationService.cs
@src/BS2BG.Core/Generation/MorphGenerationService.cs
@src/BS2BG.Core/Diagnostics/ProjectValidationService.cs
@src/BS2BG.Core/Export/BodyGenIniExportWriter.cs
@src/BS2BG.Core/Export/BosJsonExportWriter.cs

<interfaces>
Review constraints to address before implementation: catalog composition must be a single Core recipe loaded install-relative from `AppContext.BaseDirectory`, not copied from App-only bootstrap code; BoS JSON overwrite preflight must use a writer-owned/sibling `BosJsonExportPlanner` so filename sanitization is not duplicated in headless generation; CLI tests should invoke `BS2BG.Cli.Program.Main(string[])` in-process with captured stdout/stderr under a non-parallel console-capture collection rather than shelling out to `dotnet run`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement validation-first generation service</name>
  <files>src/BS2BG.Core/Automation/HeadlessGenerationService.cs, src/BS2BG.Core/Export/BosJsonExportPlanner.cs, src/BS2BG.Core/Export/BosJsonExportWriter.cs, tests/BS2BG.Tests/CliGenerationTests.cs</files>
  <read_first>src/BS2BG.Core/Automation/HeadlessGenerationContracts.cs; src/BS2BG.Core/Serialization/ProjectFileService.cs; src/BS2BG.Core/Diagnostics/ProjectValidationService.cs; src/BS2BG.Core/Generation/TemplateGenerationService.cs; src/BS2BG.Core/Generation/MorphGenerationService.cs; src/BS2BG.Core/Export/BodyGenIniExportWriter.cs; src/BS2BG.Core/Export/BosJsonExportWriter.cs; existing GUI callers of TemplateGenerationService and MorphGenerationService; .planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md D-02/D-04/D-05</read_first>
  <behavior>
    - Test 1: A project with no presets returns ValidationBlocked (2), includes the validation report, and creates no output directory files.
    - Test 2: BodyGen intent writes templates.ini and morphs.ini through BodyGenIniExportWriter when validation has no blockers.
    - Test 3: BosJson intent writes per-preset JSON through BosJsonExportWriter when validation has no blockers.
    - Test 4: Existing templates.ini/morphs.ini or BoS JSON files return OverwriteRefused (3) when Overwrite is false, and `--intent all` checks every selected BodyGen and BoS target before any write so overwrite refusal creates no files and mutates no existing files.
    - Test 5: BoS overwrite preflight obtains filenames from writer-owned planning, so sanitized/duplicate JSON names match BosJsonExportWriter exactly.
    - Test 6: `--intent all` reports a Phase-3-style outcome ledger when BodyGen succeeds but BoS fails; the service returns IoFailure (4), states BodyGen artifacts remain present, and does not claim full success.
    - Test 7: Malformed `.jbs2bg` JSON maps to UsageError (1), missing project file maps to UsageError (1), output directory denied/unwritable maps to IoFailure (4), and existing target maps to OverwriteRefused (3).</behavior>
  <action>Create HeadlessGenerationService in BS2BG.Core.Automation with constructor dependencies ProjectFileService, TemplateGenerationService, MorphGenerationService, ProjectValidationService or the existing static validation entry point, BodyGenIniExportWriter, BosJsonExportWriter, TemplateProfileCatalog, and request.OmitRedundantSliders. First verify actual TemplateGenerationService/MorphGenerationService signatures from read_first and call the existing API shapes exactly. Add Run(HeadlessGenerationRequest request): load request.ProjectPath with ProjectFileService.Load, call ProjectValidationService.Validate(project, catalog), return ValidationBlocked when BlockerCount &gt; 0 before any writer call, preflight existing files for selected intent when request.Overwrite is false, generate template/morph content through existing generation services using request.OmitRedundantSliders, then call only BodyGenIniExportWriter.Write and/or BosJsonExportWriter.Write. Address review concern: add a new sibling `BosJsonExportPlanner` in `src/BS2BG.Core/Export/` that produces BoS JSON output plans with the same sanitized/deduplicated filenames the writer uses, and have overwrite preflight consume that planner instead of duplicating filename logic in HeadlessGenerationService. `BosJsonExportWriter.cs` is a sacred file: if delegation from the writer to the planner is required, keep the change mechanical and run the existing BoS golden/export tests plus CliGeneration tests; do not alter JSON content, ordering, newline, or filename semantics. For `OutputIntent.All`, collect and return a write outcome ledger consistent with Phase 3 atomic outcome reporting; do not silently roll back BodyGen unless an existing writer API already supports rollback, but report written/restored/skipped/failed files accurately and explicitly state BodyGen artifacts remain present when BodyGen succeeds and BoS later fails. Do not use File.WriteAllText for output artifacts. Use this deterministic exit-code table: missing required CLI option or parse error=1; project path missing=1; malformed project JSON=1; missing profile asset/config=1; validation Blocker=2; existing selected target without overwrite=3; output directory creation/write denied, IOException during writer call, UnauthorizedAccessException during writer call=4.</action>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~CliGeneration</automated></verify>
  <acceptance_criteria>HeadlessGenerationService.cs contains ProjectValidationService.Validate before any .Write call; contains BodyGenIniExportWriter and BosJsonExportWriter calls; contains OverwriteRefused; contains all-selected-output overwrite preflight before writer calls for OutputIntent.All; contains no direct templates.ini/morphs.ini/BoS JSON File.WriteAllText; BosJson overwrite preflight uses BosJsonExportPlanner; tests assert exit codes 0, 1, 2, 3, and 4, omit-redundant behavior, overwrite-refusal no-write behavior for intent all, partial-failure ledger behavior after non-overwrite preflight passes, and existing BoS golden/export tests still pass if BosJsonExportWriter.cs is touched.</acceptance_criteria>
  <done>Headless generation preserves Core validation/export semantics and overwrite refusal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire generate command to service and script-friendly output</name>
  <files>src/BS2BG.Cli/Program.cs, src/BS2BG.Core/Generation/TemplateProfileCatalogFactory.cs, tests/BS2BG.Tests/CliGenerationTests.cs</files>
  <read_first>src/BS2BG.Cli/Program.cs; src/BS2BG.App/AppBootstrapper.cs; src/BS2BG.App/Services/TemplateProfileCatalogFactory.cs; .planning/phases/05-automation-sharing-and-release-trust/05-RESEARCH.md Pattern 1</read_first>
  <behavior>
    - Test 1: Program.Main invoked in-process with generate --project valid.jbs2bg --output out --intent all returns 0 and prints written file paths.
    - Test 2: Validation blockers print Blocker finding text and return 2.
    - Test 3: Overwrite refusal prints Target files already exist. Enable overwrite to replace them. and returns 3.
    - Test 4: CLI works when current directory is a temp folder and profile JSON files exist only beside the executable/test assembly, proving install-relative catalog lookup.
    - Test 5: `--omit-redundant-sliders` is parsed and forwarded so CLI output can match the GUI preference intentionally.
    - Test 6: In-process CLI tests that redirect Console.Out/Error run in a serialized test collection so parallel xUnit execution cannot race console state.</behavior>
  <action>In Program.cs compose Core services directly without referencing BS2BG.App. Address review concern: move or create a UI-free `TemplateProfileCatalogFactory` in BS2BG.Core.Generation that uses SliderProfileJsonService and TemplateProfileCatalog to load `settings.json`, `settings_UUNP.json`, and `settings_FO4_CBBE.json` from `AppContext.BaseDirectory` first, with an explicit optional override for tests; do not probe arbitrary parent directories or rely on repository cwd. If the App currently has an App-only catalog factory, leave a thin App wrapper or migrate AppBootstrapper later only when needed; this plan only requires CLI to use the Core factory. Instantiate ProjectFileService, TemplateGenerationService, MorphGenerationService, ProjectValidationService/static validator adapter, BodyGenIniExportWriter, BosJsonExportWriter, BosJsonExportPlanner, and HeadlessGenerationService. Invoke service.Run(request), write Message/WrittenFiles/outcome ledger to stdout for success/partial writes, validation/overwrite/I/O messages to stderr for nonzero results, and return (int)ExitCode. Keep System.CommandLine responsible for parse failures. CLI tests must call Program.Main(string[]) in-process with Console.SetOut/SetError capture inside a named xUnit collection or equivalent lock so Console redirection is serialized; do not invoke dotnet run from xUnit.</action>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~CliGeneration</automated></verify>
  <acceptance_criteria>Program.cs references BS2BG.Core.Automation and Microsoft.Extensions.DependencyInjection only if needed; Program.cs contains no using Avalonia and no using BS2BG.App; TemplateProfileCatalogFactory lives in Core and uses AppContext.BaseDirectory/install-relative profile lookup; CLI tests execute Program.Main in-process under a serialized console-capture collection and assert stdout/stderr snippets plus exit codes.</acceptance_criteria>
  <done>CLI generate command is executable and delegates all generation semantics to Core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Project file→Core model | User-controlled .jbs2bg JSON drives validation and output content. |
| CLI output path→filesystem | User-selected directory receives generated artifacts. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | HeadlessGenerationService | mitigate | Run ProjectValidationService before writes and refuse Blocker reports with exit code 2. |
| T-05-02-02 | Tampering | Output preflight | mitigate | Check selected target paths and require request.Overwrite true before replacing existing BodyGen/BoS files. |
| T-05-02-03 | Information Disclosure | CLI messages | accept | CLI prints selected user paths for automation feedback; bundle/report path scrubbing is handled in bundle plans. |
| T-05-02-04 | Tampering | BoS filename preflight | mitigate | Reuse BosJsonExportWriter-owned output planning so overwrite checks cannot drift from actual sanitized JSON filenames. |
</threat_model>

<verification>
dotnet build BS2BG.sln; dotnet test BS2BG.sln --filter FullyQualifiedName~CliGeneration
</verification>

<success_criteria>
CLI generation returns 0/2/3/4 deterministically, writes only requested outputs, and never bypasses existing Core writers.
</success_criteria>

<output>
After completion, create .planning/phases/05-automation-sharing-and-release-trust/05-02-SUMMARY.md
</output>
