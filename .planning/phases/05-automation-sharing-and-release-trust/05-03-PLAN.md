---
phase: 05-automation-sharing-and-release-trust
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BS2BG.Core/Models/ProjectModel.cs
  - src/BS2BG.Core/Serialization/ProjectFileService.cs
  - src/BS2BG.Core/Morphs/AssignmentStrategyContracts.cs
  - tests/BS2BG.Tests/AssignmentStrategyServiceTests.cs
autonomous: true
requirements: [AUTO-03]
user_setup: []
must_haves:
  truths:
    - "Assignment strategy configuration is project data that round-trips through .jbs2bg."
    - "Race-aware rules use imported Npc.Race text case-insensitively and do not look up game/plugin data."
    - "D-13: Assignment strategy configuration is saved in .jbs2bg for GUI, CLI, bundle generation, undo/redo, and collaborators."
    - "D-14: Race-aware rules match imported Npc.Race OrdinalIgnoreCase with no plugin/game-data lookup."
  artifacts:
    - path: "src/BS2BG.Core/Morphs/AssignmentStrategyContracts.cs"
      provides: "Persistable seeded random, round-robin, weighted, race-filter, and group/bucket configuration model"
    - path: "src/BS2BG.Core/Serialization/ProjectFileService.cs"
      provides: "Backward-compatible Strategy JSON section"
  key_links:
    - from: "ProjectModel"
      to: "ProjectFileService"
      via: "AssignmentStrategy property serialized in optional project JSON field"
      pattern: "AssignmentStrategy"
---

<objective>
Define the persisted assignment strategy data contract and add backward-compatible project serialization for the full Phase 5 strategy menu.

Purpose: Implements D-11 through D-14 at the data boundary before algorithms or UI consume the configuration.
Output: Core strategy model, ProjectModel property, .jbs2bg round-trip tests.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md
@.planning/phases/05-automation-sharing-and-release-trust/05-RESEARCH.md
@.planning/phases/04-profile-extensibility-and-controlled-customization/04-03-SUMMARY.md
@src/BS2BG.Core/Models/ProjectModel.cs
@src/BS2BG.Core/Serialization/ProjectFileService.cs
@src/BS2BG.Core/Models/Npc.cs

<interfaces>
Review constraints to address before execution: define rule-shape validation in the persisted model, include `SchemaVersion = 1` in the persisted strategy data, inspect existing ProjectFileService DTO order before choosing JsonPropertyOrder, and ensure ProjectModel dirty/clone/ReplaceWith paths preserve AssignmentStrategy. Review concern resolved: Phase 5 strategy rules are composable data. `AssignmentStrategyKind` selects the primary selection algorithm, while each rule can also carry optional RaceFilters, Weight, and BucketName so weighted race-filtered bucket rules are representable without introducing a v2 schema. GroupsBuckets rules with empty RaceFilters match any imported race; non-empty RaceFilters restrict the bucket rule with OrdinalIgnoreCase matching.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create strategy configuration contracts</name>
  <files>src/BS2BG.Core/Morphs/AssignmentStrategyContracts.cs, tests/BS2BG.Tests/AssignmentStrategyServiceTests.cs</files>
  <read_first>src/BS2BG.Core/Models/Npc.cs; src/BS2BG.Core/Models/SliderPreset.cs; .planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md D-11 through D-14</read_first>
  <behavior>
    - Test 1: Default strategy is disabled/null and does not change legacy projects.
    - Test 2: Strategy types include SeededRandom, RoundRobin, Weighted, RaceFilters, GroupsBuckets.
    - Test 3: A Weighted strategy rule can also carry RaceFilters and BucketName, proving the schema supports composable full-menu rules rather than mutually exclusive data islands.
    - Test 4: Race rule matching is OrdinalIgnoreCase against Npc.Race and has no plugin/game lookup dependency.
    - Test 5: Persisted strategy JSON includes schemaVersion: 1 and version-tolerant loading rejects unsupported future schema versions with a recoverable project-load diagnostic.</behavior>
  <action>Create BS2BG.Core.Morphs AssignmentStrategyContracts.cs with XML-docced public types: enum AssignmentStrategyKind { SeededRandom, RoundRobin, Weighted, RaceFilters, GroupsBuckets }; sealed record AssignmentStrategyDefinition(int SchemaVersion, AssignmentStrategyKind Kind, int? Seed, IReadOnlyList&lt;AssignmentStrategyRule&gt; Rules); sealed record AssignmentStrategyRule(string Name, IReadOnlyList&lt;string&gt; PresetNames, IReadOnlyList&lt;string&gt; RaceFilters, double Weight, string? BucketName). `SchemaVersion` defaults to 1 for new strategies; loader accepts missing schemaVersion as 1 for compatibility within Phase 5 development and reports unsupported future versions as invalid strategy diagnostics. Treat Kind as the primary selection algorithm and the fields on AssignmentStrategyRule as composable filters/weights/bucket metadata: RaceFilters may restrict any rule kind, Weight applies when Kind is Weighted, and BucketName applies when Kind is GroupsBuckets. GroupsBuckets with empty RaceFilters matches any imported race; non-empty RaceFilters restrict matching by imported `Npc.Race`. Include static StringComparer RaceComparer = StringComparer.OrdinalIgnoreCase or a helper MatchesRace(Npc npc). Keep all types UI-free and serialization-friendly. Do not use game-data lookup, plugin parsing, or implicit race resolution per D-14. Addresses review concern: persisted assignment strategy has an explicit schema/version field and GroupsBuckets empty-race behavior is defined.</action>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~AssignmentStrategy</automated></verify>
  <acceptance_criteria>AssignmentStrategyContracts.cs contains SchemaVersion, AssignmentStrategyKind values SeededRandom/RoundRobin/Weighted/RaceFilters/GroupsBuckets, StringComparer.OrdinalIgnoreCase for race matching, and no references to Avalonia, BS2BG.App, plugin, esp, esm lookup services, or xEdit.</acceptance_criteria>
  <done>Strategy data contract covers the full menu and encodes race matching as imported-text-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist strategy configuration in project files</name>
  <files>src/BS2BG.Core/Models/ProjectModel.cs, src/BS2BG.Core/Serialization/ProjectFileService.cs, tests/BS2BG.Tests/AssignmentStrategyServiceTests.cs</files>
  <read_first>src/BS2BG.Core/Models/ProjectModel.cs; src/BS2BG.Core/Serialization/ProjectFileService.cs; .planning/phases/04-profile-extensibility-and-controlled-customization/04-03-SUMMARY.md; .planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md D-13</read_first>
  <behavior>
    - Test 1: Loading a legacy project with no AssignmentStrategy succeeds and leaves ProjectModel.AssignmentStrategy null.
    - Test 2: Saving a project with a strategy appends an optional AssignmentStrategy JSON field after legacy fields without changing existing SliderPresets/CustomMorphTargets/MorphedNPCs/CustomProfiles behavior.
    - Test 3: Loading the saved JSON restores schemaVersion=1, Kind, Seed, Rules, PresetNames, RaceFilters, Weight, and BucketName.
    - Test 4: Invalid strategy hydration reports diagnostics or rejects negative, NaN, Infinity, or all-zero weights; duplicate non-empty rule names; empty PresetNames for rules that must assign; and empty RaceFilters for RaceFilters rules with clear messages while legacy project fields still load where possible.
    - Test 5: ProjectModel.ReplaceWith/clone/open flows preserve AssignmentStrategy and strategy setter marks project dirty.</behavior>
  <action>Add AssignmentStrategyDefinition? AssignmentStrategy to ProjectModel with dirty tracking when changed and include it in clone/ReplaceWith/project replacement paths. Before assigning JsonPropertyOrder, inspect existing ProjectFileService DTO ordering including CustomProfiles and choose the next free order value; do not hardcode 4 if it collides. Extend ProjectFileService DTO with [JsonPropertyName("AssignmentStrategy")], the selected non-conflicting [JsonPropertyOrder(N)], and [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]. Persist strategy JSON with `schemaVersion: 1`; map DTO to/from AssignmentStrategyDefinition using nullable-safe defaults: missing schemaVersion becomes 1, missing Rules becomes empty array, missing Weight defaults to 1.0, and missing RaceFilters/PresetNames becomes empty arrays. Add load-time validation with recovery posture: if the existing ProjectFileService has a ProjectLoadResult diagnostics path, load the project and attach an invalid-strategy diagnostic while setting AssignmentStrategy to a salvageable invalid snapshot if the App repair UI can hydrate it, otherwise null; only throw JsonException for structurally unreadable JSON that the current service already treats as fatal. Validation rules: unsupported schemaVersion is invalid; negative/NaN/Infinity weights rejected; Weighted requires at least one rule with Weight &gt; 0 and non-empty PresetNames at configuration level; GroupsBuckets requires non-empty BucketName and PresetNames but RaceFilters may be empty to match any imported race; RaceFilters strategy kind requires non-empty RaceFilters and non-empty PresetNames; duplicate non-empty rule Name values are rejected OrdinalIgnoreCase, unnamed/default rules are allowed only when they are unique by original order and do not require name-based lookup. Keep CustomProfiles order and omission behavior unchanged. Add tests that assert legacy JSON still loads, saving with null AssignmentStrategy omits the property byte-for-byte for strategy absence, and new strategy JSON round-trips per D-13. Addresses review concern: invalid strategy data remains available for salvageable UI repair where structurally possible.</action>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~AssignmentStrategy</automated></verify>
  <acceptance_criteria>ProjectModel.cs contains public AssignmentStrategyDefinition? AssignmentStrategy and preserves it through ReplaceWith/clone paths; ProjectFileService.cs contains JsonPropertyName("AssignmentStrategy"), schemaVersion handling, and a non-conflicting JsonPropertyOrder; tests assert absence of AssignmentStrategy in legacy save when null, presence of schemaVersion: 1 when configured, dirty tracking, composable rule fields, legacy no-strategy JSON save stability, unsupported schemaVersion diagnostics, and invalid strategy diagnostic/rejection behavior.</acceptance_criteria>
  <done>Assignment strategy configuration is persisted in .jbs2bg for GUI, CLI, bundle, undo/redo, and collaborator reproduction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Shared .jbs2bg→strategy model | Collaborator-provided JSON controls assignment eligibility. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | ProjectFileService strategy DTO | mitigate | Nullable-safe hydration and bounded defaults prevent malformed optional fields from corrupting legacy project data. |
| T-05-03-02 | Information Disclosure | Race rules | mitigate | Store only imported race strings and preset names; do not add game-data lookup paths. |
</threat_model>

<verification>
dotnet test BS2BG.sln --filter FullyQualifiedName~AssignmentStrategy; dotnet build BS2BG.sln
</verification>

<success_criteria>
The full strategy menu has a Core data model and optional project serialization with legacy compatibility preserved.
</success_criteria>

<output>
After completion, create .planning/phases/05-automation-sharing-and-release-trust/05-03-SUMMARY.md
</output>
