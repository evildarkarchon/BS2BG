---
phase: 05-automation-sharing-and-release-trust
plan: 05
type: execute
wave: 3
depends_on: [05-04]
files_modified:
  - src/BS2BG.App/ViewModels/MorphsViewModel.cs
  - src/BS2BG.App/ViewModels/AssignmentStrategyRuleRowViewModel.cs
  - src/BS2BG.App/AppBootstrapper.cs
  - src/BS2BG.App/Views/MainWindow.axaml
  - tests/BS2BG.Tests/MorphsViewModelStrategyTests.cs
autonomous: false
requirements: [AUTO-03]
user_setup: []
must_haves:
  truths:
    - "User can configure seeded random, round-robin, weighted, race filter, and groups/buckets strategies in Morphs."
    - "Applying a strategy is undoable, dirty-tracked, and reports rule gaps with visible text."
  artifacts:
    - path: "src/BS2BG.App/ViewModels/MorphsViewModel.cs"
      provides: "ReactiveUI strategy editor/apply state"
    - path: "src/BS2BG.App/Views/MainWindow.axaml"
      provides: "Compiled-bound accessible strategy UI in Morphs workspace"
  key_links:
    - from: "MorphsViewModel"
      to: "AssignmentStrategyService"
      via: "ApplyStrategyCommand"
      pattern: "ApplyStrategyCommand"
---

<objective>
Expose assignment strategy configuration and application in the existing Morphs workspace using ReactiveUI and compiled Avalonia bindings.

Purpose: Completes AUTO-03 user workflow while preserving UI-SPEC placement, accessibility, undo, dirty tracking, and D-12 full menu scope.
Output: Strategy editor UI, ViewModel commands, tests, and visual checkpoint.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-automation-sharing-and-release-trust/05-UI-SPEC.md
@.planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md
@.planning/phases/05-automation-sharing-and-release-trust/05-04-SUMMARY.md
@src/BS2BG.App/ViewModels/MorphsViewModel.cs
@src/BS2BG.App/Views/MainWindow.axaml
@src/BS2BG.App/AppBootstrapper.cs
@src/BS2BG.App/Services/UndoRedoService.cs

<interfaces>
Review decisions for UI execution: strategy apply operates on the full `MorphedNpcs` collection, not the current filter/visible scope, so deterministic replay holds across GUI, CLI, and collaborators; the UI must show the total NPC count that will be assigned before apply. Add XML doc comments on the apply command/helper explaining this because Phase 2 visible-scope bulk operations create the opposite mental model. Undo must restore both prior NPC assignments and the previous AssignmentStrategy configuration, and strategy edit/save changes themselves must be undoable before Apply. Rule input controls use explicit text syntax by deliberate Phase 5 choice: PresetNames and RaceFilters are comma-separated token text fields with trimming, OrdinalIgnoreCase duplicate removal, and unknown-preset validation against current project presets before Apply is enabled; Weight uses a NumericUpDown-style numeric field with Min=0 and Max=1000; BucketName is a TextBox with per-project duplicate-name validation. Keep row parsing/validation in a small `AssignmentStrategyRuleRowViewModel` rather than expanding all rule-row state directly into MorphsViewModel.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ReactiveUI strategy state and apply command</name>
  <files>src/BS2BG.App/ViewModels/MorphsViewModel.cs, src/BS2BG.App/ViewModels/AssignmentStrategyRuleRowViewModel.cs, src/BS2BG.App/AppBootstrapper.cs, tests/BS2BG.Tests/MorphsViewModelStrategyTests.cs</files>
  <read_first>src/BS2BG.App/ViewModels/MorphsViewModel.cs; src/BS2BG.App/AppBootstrapper.cs; src/BS2BG.Core/Morphs/AssignmentStrategyContracts.cs; src/BS2BG.Core/Morphs/AssignmentStrategyService.cs; .planning/phases/05-automation-sharing-and-release-trust/05-UI-SPEC.md lines 123-131</read_first>
  <behavior>
    - Test 1: Selecting strategy type and seed updates ProjectModel.AssignmentStrategy and marks project dirty.
    - Test 2: ApplyStrategyCommand mutates NPC assignments through AssignmentStrategyService and records one undo operation.
    - Test 3: Undo restores previous NPC assignment snapshots and previous AssignmentStrategy configuration.
    - Test 4: Rule gaps set status/error copy to No eligible preset after strategy rules.
    - Test 5: Unknown preset tokens, duplicate bucket names, invalid weights, or empty required tokens disable ApplyStrategyCommand with visible validation text.
    - Test 6: StrategySummaryText states the full MorphedNpcs count that will be assigned and does not depend on filtered/visible row scope.
    - Test 7: Editing/saving strategy configuration without Apply records undo state and undo restores the previous ProjectModel.AssignmentStrategy.
    - Test 8: A project loaded with invalid strategy diagnostics shows visible strategy-panel error copy and disables Apply until repaired.</behavior>
  <action>Inject AssignmentStrategyService into MorphsViewModel through constructor and AppBootstrapper. Create AssignmentStrategyRuleRowViewModel as a ReactiveObject with [Reactive] properties Name, PresetNamesText, RaceFiltersText, Weight, BucketName, and ValidationMessage; add XML doc comments explaining comma-separated token input is a deliberate Phase 5 text-first control choice and can be revisited in v2 without changing the persisted Core schema. Row parsing rules: PresetNamesText and RaceFiltersText are comma-separated, trimmed, OrdinalIgnoreCase duplicate-removed; unknown presets are rejected against current project.SliderPresets; Weight numeric range 0..1000; BucketName duplicate names rejected where applicable. In MorphsViewModel add [Reactive] properties for SelectedAssignmentStrategyKind, StrategySeedText, StrategySummaryText, StrategyValidationMessage, InvalidLoadedStrategyMessage, and an ObservableCollection&lt;AssignmentStrategyRuleRowViewModel&gt;. Add IReadOnlyList&lt;AssignmentStrategyKind&gt; StrategyKinds. Add ReactiveCommand SaveStrategyConfigurationCommand that writes a valid strategy to ProjectModel.AssignmentStrategy, marks dirty, and records undo/redo for configuration-only edits. Add ReactiveCommand ApplyStrategyCommand with observable canExecute requiring at least one NPC, at least one preset, no external busy, valid strategy definition, and no InvalidLoadedStrategyMessage. Strategy apply deliberately ignores the current filter/visible/selected bulk scope and operates on the full MorphedNpcs collection for reproducibility; show the affected NPC count in StrategySummaryText before apply and add an XML doc comment on the helper/command explaining the Phase 2 visible-scope contrast. On apply, capture both assignments and the previous ProjectModel.AssignmentStrategy with existing snapshot helpers, save the new configuration into project.AssignmentStrategy, call AssignmentStrategyService.Apply(project, strategy, project.MorphedNpcs), set LastAssignmentEffectSummary and StatusMessage, record undo/redo using existing RecordAssignmentChange style extended to restore strategy config as well as assignments, and set explicit error copy No eligible preset after strategy rules when blocked results exist. If project load diagnostics indicate invalid strategy data, show a banner in the strategy panel, leave Apply disabled until the user repairs/saves a valid configuration, and avoid mutating assignments. Addresses review concern: row ViewModel decomposition, exact parsing semantics, full-project scope policy, invalid loaded strategy UX, and undo for both config edits and apply.</action>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~MorphsViewModelStrategy</automated></verify>
  <acceptance_criteria>AssignmentStrategyRuleRowViewModel.cs exists and contains PresetNamesText, RaceFiltersText, Weight, BucketName, ValidationMessage, and XML doc comments; MorphsViewModel.cs contains ApplyStrategyCommand, SaveStrategyConfigurationCommand, StrategySeedText, StrategySummaryText, InvalidLoadedStrategyMessage, "Saved in project; reproducible in CLI and bundles.", "No eligible preset after strategy rules", full MorphedNpcs count copy, and XML doc comment explaining full-project apply; AppBootstrapper registers AssignmentStrategyService; tests assert undo restores assignments and AssignmentStrategy plus configuration-only undo.</acceptance_criteria>
  <done>ViewModel can edit, save, apply, undo, and report deterministic strategies.</done>
</task>

<task type="auto">
  <name>Task 2: Add compiled-bound Morphs strategy UI</name>
  <files>src/BS2BG.App/Views/MainWindow.axaml, tests/BS2BG.Tests/MorphsViewModelStrategyTests.cs</files>
  <read_first>src/BS2BG.App/Views/MainWindow.axaml; .planning/phases/05-automation-sharing-and-release-trust/05-UI-SPEC.md Design System, Copywriting Contract, Accessibility and compiled binding contract</read_first>
  <action>Add a Strategy panel in the Morphs workspace near NPC bulk assignment controls. Use labels exactly: Seeded random, Round-robin, Weighted, Race filters, Groups / buckets, Seed, Apply Strategy, Save Strategy, Saved in project; reproducible in CLI and bundles., Matches imported NPC Race text only; no game-data lookup., No eligible preset after strategy rules. Add visible helper text that strategy apply affects all NPC rows in the project for deterministic replay. Represent PresetNames and RaceFilters as clearly labeled comma-separated token TextBoxes with validation messages, Weight as a numeric control constrained to 0..1000, and BucketName as a TextBox with duplicate validation. If InvalidLoadedStrategyMessage is non-empty, render it as explicit panel text and keep Apply disabled until repaired. Use existing spacing (8/16), text labels/badges not icon-only controls, AutomationProperties.Name on every actionable control, and x:DataType on every new DataTemplate. Bind to Morphs strategy properties, AssignmentStrategyRuleRowViewModel row properties, SaveStrategyConfigurationCommand, and ApplyStrategyCommand. Do not add a new Help menu or in-app setup wizard.</action>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~MorphsViewModelStrategy</automated></verify>
  <acceptance_criteria>MainWindow.axaml contains Apply Strategy, Save Strategy, Seeded random, Round-robin, Weighted, Race filters, Groups / buckets, comma-separated preset/race helper copy, all NPC rows/full project scope copy, invalid loaded strategy text binding, AutomationProperties.Name="Apply Strategy", AutomationProperties.Name="Save Strategy", and x:DataType on new DataTemplate nodes; no new Help menu setup wizard copy is added.</acceptance_criteria>
  <done>Morphs UI exposes strategy controls with accessible compiled bindings and required trust copy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verify Morphs strategy workflow</name>
  <files>src/BS2BG.App/Views/MainWindow.axaml</files>
  <action>Pause execution after automated tests/build pass so the human can visually verify the Morphs strategy workflow. Do not modify files during this checkpoint.</action>
  <what-built>Strategy editor and Apply Strategy action in the Morphs workspace.</what-built>
  <how-to-verify>1. Run dotnet run --project src/BS2BG.App/BS2BG.App.csproj. 2. Open Morphs. 3. Confirm strategy controls appear near NPC bulk assignment controls and use text labels, not icon-only actions. 4. Confirm helper text says Saved in project; reproducible in CLI and bundles. 5. Confirm trust/error states are visible as text and not color-only.</how-to-verify>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~MorphsViewModelStrategy</automated></verify>
  <done>Human approval recorded after confirming the strategy editor placement, copy, and text-visible trust states.</done>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| GUI inputs→ProjectModel strategy | User-entered seed/rule text changes persisted project state and assignments. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | MorphsViewModel ApplyStrategyCommand | mitigate | Validate strategy input, capture undo snapshots, and block no-eligible rows with visible diagnostics. |
| T-05-05-02 | Repudiation | Strategy UI | mitigate | Persist strategy configuration in project and expose summary text stating reproducibility in CLI/bundles. |
</threat_model>

<verification>
dotnet test BS2BG.sln --filter FullyQualifiedName~MorphsViewModelStrategy; dotnet build BS2BG.sln; human visual checkpoint approved.
</verification>

<success_criteria>
Users can configure and apply the full deterministic strategy menu from Morphs, and the result is persisted, undoable, accessible, and diagnostics-backed.
</success_criteria>

<output>
After completion, create .planning/phases/05-automation-sharing-and-release-trust/05-05-SUMMARY.md
</output>
