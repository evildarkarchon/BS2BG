---
phase: 05-automation-sharing-and-release-trust
plan: 06
type: execute
wave: 3
depends_on: [05-02, 05-04]
files_modified:
  - src/BS2BG.Core/Bundling/PortableProjectBundleService.cs
  - src/BS2BG.Core/Bundling/PortableProjectBundleContracts.cs
  - src/BS2BG.Core/Bundling/BundlePathScrubber.cs
  - src/BS2BG.Core/Diagnostics/DiagnosticReportTextFormatter.cs
  - tests/BS2BG.Tests/PortableBundleServiceTests.cs
autonomous: true
requirements: [AUTO-02]
user_setup: []
must_haves:
  truths:
    - "User can create one shareable zip with project, selected outputs, referenced custom profiles, and validation/report artifacts."
    - "Bundle manifests and reports contain no absolute paths, drive roots, user names, or original directories."
    - "D-06: Portable project bundle is one shareable zip artifact, not only an unpacked folder."
    - "D-07: Bundle zip uses project/, outputs/bodygen/, outputs/bos/, profiles/, and reports/ layout."
    - "D-08: Bundle profile copies include only referenced non-bundled custom profiles."
    - "D-09: Bundle manifests and reports use relative paths/source filenames and exclude private local paths."
    - "D-10: Bundle contains the .jbs2bg project, requested generated outputs, referenced custom profiles, and validation/report artifact."
  artifacts:
    - path: "src/BS2BG.Core/Bundling/PortableProjectBundleService.cs"
      provides: "Core zip bundle creation over existing generation/profile/report services"
    - path: "src/BS2BG.Core/Bundling/BundlePathScrubber.cs"
      provides: "Path privacy scrubbing and relative-entry validation"
  key_links:
    - from: "PortableProjectBundleService"
      to: "ProjectFileService project serialization API"
      via: "project/project.jbs2bg entry"
      pattern: "ProjectFileService"
    - from: "PortableProjectBundleService"
      to: "ZipArchive"
      via: "structured bundle entries"
      pattern: "ZipArchive"
---

<objective>
Build the Core portable bundle service that creates a structured, path-scrubbed zip artifact using existing project, generation, profile, and validation services.

Purpose: Implements D-06 through D-10 and AUTO-02 without leaking unrelated local profiles or private paths.
Output: Bundle contracts, path scrubber, zip writer, and privacy/layout tests.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md
@.planning/phases/05-automation-sharing-and-release-trust/05-RESEARCH.md
@.planning/phases/05-automation-sharing-and-release-trust/05-02-SUMMARY.md
@.planning/phases/05-automation-sharing-and-release-trust/05-04-SUMMARY.md
@src/BS2BG.Core/Serialization/ProjectFileService.cs
@src/BS2BG.Core/Diagnostics/ProjectValidationService.cs
@src/BS2BG.Core/Generation/TemplateGenerationService.cs
@src/BS2BG.Core/Generation/MorphGenerationService.cs
@src/BS2BG.Core/Export/BodyGenIniExportWriter.cs
@src/BS2BG.Core/Export/BosJsonExportWriter.cs

<interfaces>
Review decisions for bundle parity: bundle outputs must be generated by existing BodyGenIniExportWriter and BosJsonExportWriter into an isolated temp directory, then zipped from those exact bytes. Do not create "writer-equivalent" JSON/INI code in PortableProjectBundleService. Bundle Preview must use the same planner as Create without invoking ZipArchive. Validation Blockers prevent bundle creation; the result/preview includes the report and blocker reason. Use netstandard2.1-compatible SHA-256 (`SHA256.Create().ComputeHash`) in Core, not `SHA256.HashData`. Zip entry ordering is deterministic and every ZipArchiveEntry.LastWriteTime is pinned to request.CreatedUtc or the service default CreatedUtc so preview/create tests are deterministic. Review hardening: inject a clock or accept CreatedUtc on the request so preview tests are deterministic; include SourceProjectFileName/SourceProjectPath on the request so manifest filenames do not rely on unavailable state; define a PortableProjectBundleOutcome/status enum for Success, ValidationBlocked, OverwriteRefused, MissingProfile, and IoFailure so CLI/GUI mapping does not discover failure states ad hoc; wrap temp staging in try/finally cleanup and add a comment if cleanup exceptions are intentionally swallowed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement bundle contracts and path scrubber</name>
  <files>src/BS2BG.Core/Bundling/PortableProjectBundleContracts.cs, src/BS2BG.Core/Bundling/BundlePathScrubber.cs, src/BS2BG.Core/Diagnostics/DiagnosticReportTextFormatter.cs, tests/BS2BG.Tests/PortableBundleServiceTests.cs</files>
  <read_first>.planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md D-06 through D-10; .planning/phases/05-automation-sharing-and-release-trust/05-RESEARCH.md Pattern 4 and Pitfall 2; src/BS2BG.Core/Serialization/ProjectFileService.cs</read_first>
  <behavior>
    - Test 1: Bundle entry paths normalize to forward-slash relative paths and reject rooted paths or .. segments.
    - Test 2: Scrubber removes C:\Users\Example, drive roots, original import directories, and original export directories from manifest/report text.
    - Test 3: Request model supports selected output intent and overwrite flag.
    - Test 4: Manifest JSON schema contains schemaVersion=1, createdUtc, bundleSourceProjectName filename only, and entries with path/kind/sha256 lowercase hex.
    - Test 5: Preview result exposes entries, manifest JSON, validation report, fixed createdUtc from test clock/request, and privacy findings without writing a zip.
    - Test 6: Bundle contracts expose outcome/status values Success, ValidationBlocked, OverwriteRefused, MissingProfile, and IoFailure for CLI/GUI mapping.</behavior>
  <review_hardening>Addresses review concerns: PortableProjectBundlePreview and PortableProjectBundleResult must carry a PortableProjectBundleOutcome enum with Success, ValidationBlocked, OverwriteRefused, MissingProfile, and IoFailure; the service must set this outcome rather than modeling expected bundle failures only as exceptions. CreatedUtc must drive both manifest createdUtc and ZipArchiveEntry.LastWriteTime so deterministic tests do not depend on wall-clock archive timestamps. If ProjectFileService lacks string serialization, add SerializeToString(ProjectModel project, ProjectSaveContext? context) before bundling and test it instead of hand-serializing project JSON inside the bundle service.</review_hardening>
  <action>Create PortableProjectBundleContracts.cs with records PortableProjectBundleRequest(ProjectModel Project, string BundlePath, string SourceProjectFileName, OutputIntent Intent, bool Overwrite, DateTimeOffset? CreatedUtc, ProjectSaveContext? SaveContext, IReadOnlyList&lt;string&gt; PrivateRoots), PortableProjectBundlePreview(IReadOnlyList&lt;BundleManifestEntry&gt; Entries, string ManifestJson, ProjectValidationReport ValidationReport, IReadOnlyList&lt;string&gt; PrivacyFindings), PortableProjectBundleResult(string BundlePath, IReadOnlyList&lt;string&gt; Entries, string ManifestJson, ProjectValidationReport ValidationReport), BundleManifest(int SchemaVersion, DateTimeOffset CreatedUtc, string BundleSourceProjectName, IReadOnlyList&lt;BundleManifestEntry&gt; Entries), and BundleManifestEntry(string Path, string Kind, string Sha256). `SourceProjectFileName` must be reduced with Path.GetFileName before manifest use; never store a full source path in manifest/report. `CreatedUtc` lets tests pass a fixed timestamp; service defaults to DateTimeOffset.UtcNow when null. Manifest schema is fixed for Phase 5: `schemaVersion: 1`, `createdUtc` as UTC ISO-8601, `bundleSourceProjectName` as filename only, and `entries[].kind` one of project|bodygen|bos|profile|report|manifest|checksum. Create BundlePathScrubber with NormalizeEntryPath(string relativePath), IsPrivatePathLeak(string text), and Scrub(string text, IEnumerable&lt;string&gt; privateRoots) methods. NormalizeEntryPath must replace backslashes with '/', reject Path.IsPathRooted, reject empty segments and '..', and return deterministic relative names. IsPrivatePathLeak should detect drive-letter roots like C:, UNC prefixes, backslashes in manifest paths, and Environment.UserName when non-empty; add a comment noting possible false positives for literal text like `C:` are intentional because privacy leakage is higher risk. Add a Core DiagnosticReportTextFormatter that formats ProjectValidationReport without App/Avalonia references so PortableProjectBundleService does not depend on src/BS2BG.App/Services/DiagnosticsReportFormatter.cs. Add XML docs.</action>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~PortableBundle</automated></verify>
  <acceptance_criteria>BundlePathScrubber.cs contains Path.IsPathRooted, "..", Replace('\\', '/'), and IsPrivatePathLeak; PortableProjectBundleContracts.cs contains SourceProjectFileName, CreatedUtc, PortableProjectBundlePreview, PortableProjectBundleOutcome, Success, ValidationBlocked, OverwriteRefused, MissingProfile, IoFailure, and BundleManifest schema types; DiagnosticReportTextFormatter.cs lives in Core; tests assert rooted paths, .. rejection, filename-only manifest source, fixed createdUtc, manifest schema, explicit outcome values, and preview without zip writing.</acceptance_criteria>
  <done>Bundle privacy and entry-name contracts are testable before zip writing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create structured zip bundle service</name>
  <files>src/BS2BG.Core/Bundling/PortableProjectBundleService.cs, tests/BS2BG.Tests/PortableBundleServiceTests.cs</files>
  <read_first>src/BS2BG.Core/Bundling/PortableProjectBundleContracts.cs; src/BS2BG.Core/Serialization/ProjectFileService.cs; src/BS2BG.Core/Generation/TemplateGenerationService.cs; src/BS2BG.Core/Generation/MorphGenerationService.cs; src/BS2BG.Core/Diagnostics/ProjectValidationService.cs; .planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md D-06 through D-10</read_first>
  <behavior>
    - Test 1: Created zip contains project/project.jbs2bg, outputs/bodygen/templates.ini, outputs/bodygen/morphs.ini, outputs/bos/*.json when intent requests them, profiles/*.json for referenced non-bundled profiles only, reports/validation.txt, manifest.json, and SHA256SUMS.txt.
    - Test 2: Bundle excludes unrelated local custom profiles and bundled profile JSON duplicates.
    - Test 3: manifest.json and reports contain no absolute paths, usernames, original import directories, or original export directories.
    - Test 4: Existing bundle target is refused unless Overwrite is true.
    - Test 5: Validation Blocker prevents zip creation and returns the validation report.
    - Test 6: Missing referenced custom profile copies are reported as bundle blockers; they are not silently omitted.
    - Test 7: BodyGen INI and BoS JSON bytes inside the zip exactly equal files written by BodyGenIniExportWriter and BosJsonExportWriter for the same project/request.
    - Test 8: Duplicate zip entry names are rejected before archive write.
    - Test 9: Temp staging directory is removed on success and on simulated writer/zip failure.
    - Test 10: Referenced embedded project profiles are valid bundle profile sources even when no local catalog copy exists.
    - Test 11: Every ZipArchiveEntry.LastWriteTime equals the request CreatedUtc value used by the manifest.</behavior>
  <action>Create PortableProjectBundleService in Core.Bundling with Preview(PortableProjectBundleRequest request) and Create(PortableProjectBundleRequest request). Compose the actual ProjectFileService project serialization API after reading it: if SaveToString exists, use it; if not, add a Core method such as SerializeToString(ProjectModel project, ProjectSaveContext? context) to ProjectFileService and cover it with tests rather than hand-serializing project JSON inside Bundling. Compose ProjectValidationService.Validate, DiagnosticReportTextFormatter, ProfileDefinitionService.ExportProfileJson or existing Phase 4 export semantics for referenced non-bundled profiles only, BodyGenIniExportWriter, BosJsonExportWriter, netstandard-compatible SHA-256 via SHA256.Create().ComputeHash, and ZipArchive. Validation Blockers must stop Create before writing the zip while still returning/reporting the validation report. Missing referenced non-bundled custom profiles are blockers unless the definition is present as an embedded project profile from Phase 4; embedded project profiles are valid sources, unrelated local custom profiles and bundled profile JSON are excluded per D-08. For selected generated outputs, write into an isolated temp directory using existing BodyGenIniExportWriter and BosJsonExportWriter, then read those exact bytes into bundle entries under outputs/bodygen/ and outputs/bos/. Wrap temp directory staging in try/finally and delete it recursively on success and failure; if cleanup failure is intentionally swallowed, add a one-line comment explaining that the main write failure/result must not be masked. This explicitly addresses review concern: do not implement BosJsonExportWriter-equivalent preview content or duplicate filename sanitization in the bundle service. Use deterministic folders exactly: project/, outputs/bodygen/, outputs/bos/, profiles/, reports/. Include manifest.json and SHA256SUMS.txt at zip root. Sort all entries Ordinal, reject duplicate normalized entry names, and normalize all paths through BundlePathScrubber before hashing/manifest creation so path separators match checksum metadata. Generate validation/report text from findings with scrubbed source filenames and relative bundle paths only. Refuse existing BundlePath unless Overwrite true. Set every ZipArchiveEntry.LastWriteTime to the same CreatedUtc value used by the manifest so tests can assert deterministic preview/create metadata.</action>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~PortableBundle</automated></verify>
  <acceptance_criteria>PortableProjectBundleService.cs contains Preview and Create, PortableProjectBundleOutcome mapping, ZipArchive, LastWriteTime assignment from CreatedUtc, project/, outputs/bodygen/, outputs/bos/, profiles/, reports/, manifest.json, SHA256SUMS.txt, SHA256.Create().ComputeHash, BodyGenIniExportWriter, BosJsonExportWriter, temp-directory output staging, and try/finally cleanup; tests open ZipArchive and assert entry names, no duplicate names, no private path leaks, blocker behavior, missing custom profile behavior, embedded custom profile inclusion, cleanup on failure, fixed createdUtc, pinned entry LastWriteTime, and exact byte equality with existing writers.</acceptance_criteria>
  <done>Core can create a portable path-scrubbed project bundle zip with selected outputs and support report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Project/profile data→zip | Shared project data becomes an archive sent to another person. |
| Filesystem path→manifest/report | Local absolute paths may accidentally enter support artifacts. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | Zip entries | mitigate | Normalize relative entry paths, reject rooted paths and .. segments, assert ZipArchive entries in tests. |
| T-05-06-02 | Information Disclosure | manifest/report | mitigate | Central BundlePathScrubber and tests scan for drive roots, backslashes, usernames, and original directories. |
| T-05-06-03 | Tampering | bundle target | mitigate | Refuse existing zip target unless explicit overwrite is true. |
| T-05-06-04 | Tampering | generated bundle outputs | mitigate | Stage outputs with existing BodyGenIniExportWriter/BosJsonExportWriter and zip exact bytes, preventing writer drift. |
| T-05-06-05 | Information Disclosure | diagnostics formatter | mitigate | Use Core DiagnosticReportTextFormatter plus BundlePathScrubber; do not reference App formatter or raw local paths. |
</threat_model>

<verification>
dotnet test BS2BG.sln --filter FullyQualifiedName~PortableBundle; dotnet build BS2BG.sln
</verification>

<success_criteria>
Portable bundle creation produces one predictable zip, includes only scoped artifacts, and proves no private path leakage.
</success_criteria>

<output>
After completion, create .planning/phases/05-automation-sharing-and-release-trust/05-06-SUMMARY.md
</output>
