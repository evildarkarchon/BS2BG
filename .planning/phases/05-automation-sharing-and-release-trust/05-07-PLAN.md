---
phase: 05-automation-sharing-and-release-trust
plan: 07
type: execute
wave: 4
depends_on: [05-05, 05-06]
files_modified:
  - src/BS2BG.Cli/Program.cs
  - src/BS2BG.App/ViewModels/MainWindowViewModel.cs
  - src/BS2BG.App/Services/IFileDialogService.cs
  - src/BS2BG.App/Views/MainWindow.axaml
  - src/BS2BG.App/AppBootstrapper.cs
  - tests/BS2BG.Tests/PortableBundleServiceTests.cs
autonomous: false
requirements: [AUTO-01, AUTO-02]
user_setup: []
must_haves:
  truths:
    - "User can create a portable bundle from CLI and GUI preview/write paths."
    - "Bundle preview shows selected outputs, referenced custom profiles only, layout, and privacy status before writing."
  artifacts:
    - path: "src/BS2BG.Cli/Program.cs"
      provides: "bundle command"
    - path: "src/BS2BG.App/ViewModels/MainWindowViewModel.cs"
      provides: "bundle preview/create commands"
    - path: "src/BS2BG.App/Views/MainWindow.axaml"
      provides: "Portable bundle preview UI"
  key_links:
    - from: "MainWindowViewModel"
      to: "PortableProjectBundleService"
      via: "Preview/Create Portable Bundle commands"
      pattern: "PortableProjectBundleService"
---

<objective>
Wire portable bundle creation into CLI automation and the existing shell export/share workflow with a preview-first GUI surface.

Purpose: Makes AUTO-02 usable while preserving D-03 explicit output intent, D-05 overwrite safety, and UI-SPEC bundle preview requirements.
Output: CLI bundle command, GUI preview/create commands, accessible bundle UI, visual checkpoint.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-automation-sharing-and-release-trust/05-UI-SPEC.md
@.planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md
@.planning/phases/05-automation-sharing-and-release-trust/05-06-SUMMARY.md
@src/BS2BG.Cli/Program.cs
@src/BS2BG.App/ViewModels/MainWindowViewModel.cs
@src/BS2BG.App/Views/MainWindow.axaml
@src/BS2BG.App/AppBootstrapper.cs

<interfaces>
If the existing file dialog service lacks a zip save picker, add `Task<string?> PickSaveBundleFileAsync(CancellationToken cancellationToken)` or the repository's equivalent nullable-path signature to `IFileDialogService` and its implementation. GUI bundle requests must pass a filename-only source project name from the currently opened/saved `.jbs2bg` path when available; for unsaved projects use `unsaved-project.jbs2bg` in preview/report copy and require choosing a bundle target before Create. Dirty projects should preview the in-memory project state and use the same ProjectSaveContext semantics as normal project save so the zip reflects what the user sees.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI bundle command</name>
  <files>src/BS2BG.Cli/Program.cs, tests/BS2BG.Tests/PortableBundleServiceTests.cs</files>
  <read_first>src/BS2BG.Cli/Program.cs; src/BS2BG.Core/Bundling/PortableProjectBundleService.cs; .planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md D-03/D-05/D-06</read_first>
  <behavior>
    - Test 1: bundle command requires --project, --bundle, and --intent bodygen|bos|all.
    - Test 2: existing zip target returns overwrite-refused exit code when --overwrite is absent.
    - Test 3: successful bundle command prints the zip path and manifest entry count.</behavior>
  <action>Add System.CommandLine command bundle to Program.cs. Required options: --project FileInfo, --bundle FileInfo, --intent bodygen|bos|all, --overwrite bool default false. Load project through ProjectFileService, compose ProjectSaveContext/profile copy source through the same Core install-relative catalog/profile composition recipe from Plan 02; if referenced custom profiles cannot be resolved from embedded project data or configured local catalog, return a clear validation/bundle blocker rather than silently omitting them. Construct PortableProjectBundleRequest with SourceProjectFileName = Path.GetFileName(project option path), BundlePath = --bundle full path, Intent, Overwrite, CreatedUtc = null, and PrivateRoots including the project directory/output directory roots that should be scrubbed. Call PortableProjectBundleService.Preview(request) for preflight summary and PortableProjectBundleService.Create(request) for write, print bundle path and entries to stdout, return 0 on success, 2 on validation/bundle blockers, 3 on overwrite refusal, 4 on I/O failure. Reuse the existing automation exit-code enum from Plan 01/02; if the enum name is still HeadlessGenerationExitCode, either reuse it directly for bundle or rename it to `AutomationExitCode` with all CLI tests updated. Keep generate command unchanged. Do not create an unpacked-only workflow.</action>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~PortableBundle</automated></verify>
  <acceptance_criteria>Program.cs contains Command("bundle"), --bundle, --intent, --overwrite, PortableProjectBundleService, and no references to Avalonia/BS2BG.App.</acceptance_criteria>
  <done>CLI can create portable bundle zips with explicit intent and overwrite safety.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add GUI bundle preview and create commands</name>
  <files>src/BS2BG.App/ViewModels/MainWindowViewModel.cs, src/BS2BG.App/Services/IFileDialogService.cs, src/BS2BG.App/AppBootstrapper.cs, src/BS2BG.App/Views/MainWindow.axaml, tests/BS2BG.Tests/PortableBundleServiceTests.cs</files>
  <read_first>src/BS2BG.App/ViewModels/MainWindowViewModel.cs; src/BS2BG.App/Views/MainWindow.axaml; src/BS2BG.App/AppBootstrapper.cs; .planning/phases/05-automation-sharing-and-release-trust/05-UI-SPEC.md Portable bundle preview</read_first>
  <behavior>
    - Test 1: PreviewPortableBundleCommand calls PortableProjectBundleService.Preview and populates layout entries project/, outputs/bodygen/, outputs/bos/, profiles/, reports/, manifest.json, SHA256SUMS.txt without writing zip.
    - Test 2: CreatePortableBundleCommand refuses existing target unless explicit overwrite is selected/confirmed.
    - Test 3: UI exposes exact copy Portable bundle preview, Create Portable Bundle, Referenced custom profiles only, Bundle reports use relative paths and source filenames only.
    - Test 4: Unsaved and dirty project previews pass a filename-only source project name and in-memory project state without leaking local paths.</behavior>
  <action>Register PortableProjectBundleService in AppBootstrapper. Add or extend IFileDialogService with exact signature `Task<string?> PickSaveBundleFileAsync(CancellationToken cancellationToken)` (or repository-equivalent nullable path/async naming) and implement it with a `.zip` save picker; tests should use the service abstraction, not Avalonia dialogs. Add MainWindowViewModel bundle state: BundlePreviewSummary, BundlePreviewEntries collection, BundleOutputIntent selection, BundleOverwriteAllowed bool, BundleTargetPath, PreviewPortableBundleCommand, CreatePortableBundleCommand. Preview must call the Core Preview API from Plan 06, not create/delete a temp zip, and must list deterministic layout and selected output intents before write, profile copy summary Referenced custom profiles only, missing-profile/blocker status if applicable, and privacy status No absolute paths in manifest/report or Path privacy check failed. GUI requests must use current in-memory project data; for saved projects pass Path.GetFileName(current project path) as SourceProjectFileName, for unsaved projects pass `unsaved-project.jbs2bg` and require the user to pick BundleTargetPath before Create. Dirty projects preview current in-memory changes and should show neutral text that the bundle uses the current open project state. Add File menu item or clearly labeled shell action Create Portable Bundle and a preview panel using 12px Consolas for paths. Set AutomationProperties.Name on controls. No direct plugin editing or setup wizard UI.</action>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~PortableBundle</automated></verify>
  <acceptance_criteria>MainWindowViewModel.cs contains PreviewPortableBundleCommand, CreatePortableBundleCommand, BundleTargetPath, SourceProjectFileName construction, unsaved-project.jbs2bg fallback, and PortableProjectBundleService.Preview usage; IFileDialogService contains PickSaveBundleFileAsync or equivalent zip save method; MainWindow.axaml contains Portable bundle preview, Create Portable Bundle, Bundle reports use relative paths and source filenames only, missing profile/blocker copy, and AutomationProperties.Name="Create Portable Bundle".</acceptance_criteria>
  <done>GUI offers preview-first portable bundle creation through the same Core service as CLI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verify bundle preview workflow</name>
  <files>src/BS2BG.App/Views/MainWindow.axaml</files>
  <action>Pause execution after automated tests/build pass so the human can visually verify the portable bundle preview workflow. Do not modify files during this checkpoint.</action>
  <what-built>Portable bundle preview panel and Create Portable Bundle action.</what-built>
  <how-to-verify>1. Run dotnet run --project src/BS2BG.App/BS2BG.App.csproj. 2. Find Create Portable Bundle. 3. Confirm preview lists project/, outputs/bodygen/, outputs/bos/, profiles/, reports/, manifest.json, and checksum entries before writing. 4. Confirm privacy status is visible as text and existing zip overwrite requires an explicit choice.</how-to-verify>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~PortableBundle</automated></verify>
  <done>Human approval recorded after confirming bundle preview layout, privacy copy, and overwrite affordance.</done>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| GUI/CLI bundle target→filesystem | User-selected zip path is created or overwritten. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | CLI/GUI bundle target | mitigate | Existing zip targets require --overwrite or explicit GUI overwrite choice. |
| T-05-07-02 | Information Disclosure | Bundle preview | mitigate | Preview exposes privacy status and uses Core scrubber before write. |
</threat_model>

<verification>
dotnet test BS2BG.sln --filter FullyQualifiedName~PortableBundle; dotnet build BS2BG.sln; human visual checkpoint approved.
</verification>

<success_criteria>
Both CLI and GUI can create the same portable bundle zip through Core, with previewed layout, intent, profile scope, and privacy status.
</success_criteria>

<output>
After completion, create .planning/phases/05-automation-sharing-and-release-trust/05-07-SUMMARY.md
</output>
