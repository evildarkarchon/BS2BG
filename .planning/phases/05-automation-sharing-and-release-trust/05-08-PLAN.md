---
phase: 05-automation-sharing-and-release-trust
plan: 08
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - tools/release/package-release.ps1
  - docs/release/README.md
  - docs/release/UNSIGNED-BUILD.md
  - tests/BS2BG.Tests/ReleaseTrustTests.cs
autonomous: true
requirements: [AUTO-04]
user_setup: []
must_haves:
  truths:
    - "Release artifacts can be verified with SHA-256 checksums whether signed or unsigned."
    - "Signing is used when configured/available and optional when unavailable."
    - "Package assertions verify required files, checksums, docs, and path-safety smoke checks."
    - "D-17: Release automation supports signed and unsigned paths, with unsigned artifacts valid when checksum sidecars and docs are present."
    - "D-19: Package assertions verify required files, checksums, profile assets, docs, path safety, and extraction/launch smoke when available."
  artifacts:
    - path: "tools/release/package-release.ps1"
      provides: "Signed/unsigned release trust packaging and assertions"
    - path: "tests/BS2BG.Tests/ReleaseTrustTests.cs"
      provides: "Release artifact trust assertions"
  key_links:
    - from: "package-release.ps1"
      to: "docs/release/UNSIGNED-BUILD.md"
      via: "unsigned fallback docs packaged with checksums"
      pattern: "UNSIGNED-BUILD.md"
---

<objective>
Extend release packaging so signed and unsigned paths are both trustworthy, checksum-backed, and test-asserted.

Purpose: Implements D-17 and D-19 plus AUTO-04 without making SignTool/certificates mandatory.
Output: Optional signing metadata, checksum verification docs, and release package tests.
</objective>

<execution_context>
@C:/Users/evild/.config/kilo/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md
@.planning/phases/05-automation-sharing-and-release-trust/05-RESEARCH.md
@.planning/phases/05-automation-sharing-and-release-trust/05-01-SUMMARY.md
@.planning/phases/05-automation-sharing-and-release-trust/05-02-SUMMARY.md
@tools/release/package-release.ps1
@docs/release/README.md
@docs/release/UNSIGNED-BUILD.md

<interfaces>
Review constraints to address: release package must publish and include a functional BS2BG.Cli.exe plus CLI runtime/profile assets after Plan 02 implements `generate`; tests should inspect a generated package zip when feasible rather than only grepping script text; signing password values must never be written to logs, SIGNING-INFO.txt, or package metadata, and certificate paths should be redacted to filenames/status where possible. Heavy publish/package smoke tests must be category-gated or script-smoke scoped so ordinary `dotnet test` stays fast.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add release trust assertions</name>
  <files>tests/BS2BG.Tests/ReleaseTrustTests.cs</files>
  <read_first>tools/release/package-release.ps1; docs/release/README.md; docs/release/UNSIGNED-BUILD.md; .planning/phases/05-automation-sharing-and-release-trust/05-CONTEXT.md D-17/D-19</read_first>
  <behavior>
    - Test 1: Category-gated ReleaseSmoke package inspection packages README.md, UNSIGNED-BUILD.md, QA-CHECKLIST.md, SHA256SUMS.txt, BS2BG.App.exe, BS2BG.Cli.exe, and settings_FO4_CBBE.json without running under the default fast test filter.
    - Test 2: Release script text creates external zip .sha256 sidecar and package SHA256SUMS.txt using SHA256.
    - Test 3: Tests can inspect a zip and fail on absolute paths or backslash entry names.
    - Test 4: Tests treat SignTool as optional and do not fail when it is absent.
    - Test 5: Tests assert SIGNING-INFO.txt never contains certificate password values and does not leak full certificate paths.
    - Test 6: Extracted package smoke runs `BS2BG.Cli.exe --help` only inside the ReleaseSmoke category or focused script smoke, not ordinary unit-test runs.</behavior>
  <action>Create ReleaseTrustTests.cs using xUnit v3 and FluentAssertions. Split tests into fast source/zip-helper assertions and heavyweight release smoke. Mark any test that runs `tools/release/package-release.ps1`, `dotnet publish`, opens a generated release zip, or executes extracted `BS2BG.Cli.exe --help` with `[Trait("Category", "ReleaseSmoke")]`; ordinary verification for this plan uses the fast `FullyQualifiedName~ReleaseTrust` filter and must not run a 60-120 second publish unless the executor explicitly opts into ReleaseSmoke. Prefer extending existing ReleasePackagingScriptTests/package smoke helpers if they already run quickly; otherwise add a focused smoke helper that can run the script to artifacts/test-out for a test version/runtime and opens the resulting zip with ZipArchive under the ReleaseSmoke trait. Assert package entries are relative, no rooted paths, no backslashes, no duplicate names, and include BS2BG.App.exe, BS2BG.Cli.exe, README.md, UNSIGNED-BUILD.md, QA-CHECKLIST.md, SIGNING-INFO.txt, SHA256SUMS.txt, and settings_FO4_CBBE.json. Keep limited source-text assertions only for contracts hard to smoke (optional SignTool parameter names, redaction/no password output). Add optional SignTool detection test that accepts unavailable SignTool but asserts docs describe unsigned checksum verification. Do not require signtool.exe on PATH.</action>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~ReleaseTrust</automated></verify>
  <acceptance_criteria>ReleaseTrustTests.cs contains ReleaseSmoke trait gating for publish/package/extracted-exe tests, ZipArchive/package smoke inspection helper, BS2BG.Cli.exe assertion, BS2BG.Cli.exe --help smoke under ReleaseSmoke, SHA256SUMS.txt, SIGNING-INFO.txt, UNSIGNED-BUILD.md, SignTool optional assertion, signing secret redaction assertion, and no unconditional requirement that signtool exists.</acceptance_criteria>
  <done>Release trust tests define required package and verification invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Extend release script and trust docs</name>
  <files>tools/release/package-release.ps1, docs/release/README.md, docs/release/UNSIGNED-BUILD.md, tests/BS2BG.Tests/ReleaseTrustTests.cs</files>
  <read_first>tools/release/package-release.ps1; docs/release/README.md; docs/release/UNSIGNED-BUILD.md; .planning/phases/05-automation-sharing-and-release-trust/05-RESEARCH.md Pitfall 5 and Code Examples</read_first>
  <action>Extend package-release.ps1 with optional signing parameters/env-var support without making signing mandatory: add parameters such as [string]$SignToolPath, [string]$CertificateSubject, [string]$CertificatePath, and [string]$CertificatePasswordEnvVar. Avoid `[switch]$SkipSigning` unless the current script architecture truly needs it; preferred behavior is signing enabled only when CertificateSubject or CertificatePath is provided and SignTool is available, otherwise unsigned checksum-backed release. Publish/package both BS2BG.App and BS2BG.Cli artifacts into the Windows portable zip so AUTO-01 is distributed, including CLI runtime dependencies and copied profile JSON assets; CLI publish must rely on the PublishSingleFile/apphost properties from Plan 01 and a functional generate command from Plan 02. If signing is configured and SignTool is available, sign/verify BS2BG.App.exe and BS2BG.Cli.exe where applicable and write SIGNING-INFO.txt with Signed status and verification command. If signing is not configured or SignTool is unavailable, write SIGNING-INFO.txt with Unsigned status and keep UNSIGNED-BUILD.md packaged. Never Write-Host/Write-Output the resolved certificate password or full command line containing secrets; SIGNING-INFO.txt must not include password values and should record certificate subject/status plus certificate filename only, not full private user paths. Always generate SHA256SUMS.txt and external zip .sha256. Add package assertions after zip creation: required files exist (including BS2BG.Cli.exe and settings_FO4_CBBE.json), checksum file contains entries, docs exist, profile assets exist, no package file path is rooted relative to packageDir. Before adding a no-backslash ZIP assertion, verify current package creation behavior; if Compress-Archive emits backslash entries, update package-release.ps1 to create the final zip through [System.IO.Compression.ZipFile]/ZipArchive with normalized forward-slash entry names before asserting it. Update README/UNSIGNED-BUILD with CLI availability, SignTool optional wording, SIGNING-INFO.txt explanation, and exact PowerShell checksum commands. Preserve Windows-first win-x64 runtime validation.</action>
  <verify><automated>dotnet test BS2BG.sln --filter FullyQualifiedName~ReleaseTrust</automated></verify>
  <acceptance_criteria>package-release.ps1 contains SIGNING-INFO.txt, BS2BG.Cli.exe packaging, settings_FO4_CBBE.json assertion, Get-FileHash -Algorithm SHA256, optional SignTool handling triggered by cert subject/path plus tool availability, no password output/no secret command-line logging, required-file assertions, and normalized zip-entry handling if needed; README.md mentions CLI, checksum sidecar, and SIGNING-INFO.txt; UNSIGNED-BUILD.md says unsigned is valid when SHA-256 verification succeeds.</acceptance_criteria>
  <done>Release packaging supports signed and unsigned trust paths with smoke assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Release script→distributed zip | Local build artifacts become user-downloaded files. |
| Optional signing config→executable | Optional certificate/signing inputs affect trust metadata. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Spoofing | Release artifacts | mitigate | Generate SHA-256 sidecars/SHA256SUMS and optional SignTool verification metadata. |
| T-05-08-02 | Tampering | package-release.ps1 | mitigate | Add required-file/checksum/package assertions and ReleaseTrustTests. |
| T-05-08-03 | Denial of Service | SignTool dependency | mitigate | Treat SignTool as optional and keep unsigned checksum path valid per D-17. |
| T-05-08-04 | Information Disclosure | signing config | mitigate | Never log certificate password env-var values; redact certificate paths in signing metadata. |
| T-05-08-05 | Tampering | release composition | mitigate | Package smoke test inspects actual zip for BS2BG.Cli.exe, checksums, docs, profile assets, and path-safe entries. |
</threat_model>

<verification>
dotnet test BS2BG.sln --filter FullyQualifiedName~ReleaseTrust; pwsh -File tools/release/package-release.ps1 -Version 1.0.0 -Runtime win-x64 if targeted release smoke is acceptable.
</verification>

<success_criteria>
Release packages include verifiable checksums, optional signing metadata, and tests that fail if required trust artifacts disappear.
</success_criteria>

<output>
After completion, create .planning/phases/05-automation-sharing-and-release-trust/05-08-SUMMARY.md
</output>
