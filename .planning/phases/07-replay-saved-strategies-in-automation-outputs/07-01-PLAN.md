---
phase: 07-replay-saved-strategies-in-automation-outputs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BS2BG.Core/Automation/AssignmentStrategyReplayService.cs
  - src/BS2BG.Core/Automation/AssignmentStrategyReplayContracts.cs
  - src/BS2BG.Core/Morphs/MorphAssignmentService.cs
  - tests/BS2BG.Tests/AssignmentStrategyReplayServiceTests.cs
autonomous: true
requirements: [AUTO-03]
must_haves:
  truths:
    - "D-01/D-02: Automation BodyGen requests have a Core helper that automatically replays a saved AssignmentStrategy when present."
    - "D-03: Requests with no saved AssignmentStrategy keep the existing project assignments unchanged."
    - "D-06/D-07: Blocked NPC rows are represented as fatal replay blockers without fallback or stale assignment output."
    - "D-08/D-09: Replay results expose concise success counts and actionable blocked-NPC details for callers."
    - "Review: Seeded deterministic strategies replay through the existing deterministic provider path while preserving eligibleRows/scoped GUI semantics."
  artifacts:
    - path: "src/BS2BG.Core/Automation/AssignmentStrategyReplayService.cs"
      provides: "Core-only replay seam returning request-scoped project state plus replay summary/blockers"
      exports: ["AssignmentStrategyReplayService", "AssignmentStrategyReplayResult"]
    - path: "src/BS2BG.Core/Morphs/MorphAssignmentService.cs"
      provides: "Provider-compatible strategy application that honors saved seeds for automation and GUI callers"
    - path: "tests/BS2BG.Tests/AssignmentStrategyReplayServiceTests.cs"
      provides: "Direct replay decision, no-strategy, blocked-row, and clone/no-mutation coverage"
  key_links:
    - from: "AssignmentStrategyReplayService"
      to: "MorphAssignmentService.ApplyStrategy"
      via: "provider-compatible strategy application that delegates seeded strategies to the deterministic provider path"
      pattern: "ApplyStrategy\(.*AssignmentStrategy"
    - from: "AssignmentStrategyReplayResult.Project"
      to: "ProjectModel.ReplaceWith"
      via: "request-scoped clone for mutation isolation"
      pattern: "ReplaceWith\(sourceProject\)"
---

<objective>
Create the reusable Core replay seam that all automation paths will consume before BodyGen morph generation.

Purpose: Phase 7 must centralize saved assignment strategy replay so CLI and bundle behavior cannot drift, while preserving the existing random-provider abstraction and fatal blocker semantics.
Output: A tested `AssignmentStrategyReplayService` and result contract that callers can use without touching writers or UI/App code.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-CONTEXT.md
@.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-RESEARCH.md
@.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-REVIEWS.md
@.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-VALIDATION.md
@.planning/phases/06-compose-custom-profiles-in-headless-generation/06-01-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@src/BS2BG.Core/Automation/HeadlessGenerationContracts.cs
@src/BS2BG.Core/Morphs/MorphAssignmentService.cs
@src/BS2BG.Core/Morphs/AssignmentStrategyService.cs
@src/BS2BG.Core/Models/ProjectModel.cs

<interfaces>
From `HeadlessGenerationContracts.cs`:
```csharp
public enum OutputIntent { BodyGen, BosJson, All }
public enum AutomationExitCode { Success = 0, UsageError = 1, ValidationBlocked = 2, OverwriteRefused = 3, IoFailure = 4 }
```

From `MorphAssignmentService.cs`:
```csharp
public AssignmentStrategyResult ApplyStrategy(ProjectModel project, AssignmentStrategyDefinition strategy, IReadOnlyList<Npc>? eligibleRows = null);
```

From `AssignmentStrategyService.cs`:
```csharp
public sealed record AssignmentStrategyResult(int AssignedCount, IReadOnlyList<AssignmentStrategyBlockedNpc> BlockedNpcs);
public sealed record AssignmentStrategyBlockedNpc(Npc Npc, string Reason);
```

From `ProjectModel.cs`:
```csharp
public AssignmentStrategyDefinition? AssignmentStrategy { get; set; }
public void ReplaceWith(ProjectModel source);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — specify replay helper behavior</name>
  <files>tests/BS2BG.Tests/AssignmentStrategyReplayServiceTests.cs</files>
  <read_first>tests/BS2BG.Tests/AssignmentStrategyServiceTests.cs, tests/BS2BG.Tests/CliGenerationTests.cs, src/BS2BG.Core/Morphs/AssignmentStrategyService.cs, src/BS2BG.Core/Morphs/MorphAssignmentService.cs, src/BS2BG.Core/Automation/HeadlessGenerationContracts.cs</read_first>
  <behavior>
    - Test 1: `[Theory]` covers both `OutputIntent.BodyGen` and `OutputIntent.All` with `ProjectModel.AssignmentStrategy`; each intent replays through `MorphAssignmentService.ApplyStrategy`, assigns every eligible NPC, and reports `Replayed=true`, `AssignedCount`, `BlockedNpcs.Count == 0`, and `StrategyKind` per D-01/D-08.
    - Test 2: BosJson intent does not replay and preserves stale assignments per D-01.
    - Test 3: Null `AssignmentStrategy` with `cloneBeforeReplay: true` returns `Replayed=false`, returns an isolated clone, keeps current assignments, and has no blocked rows per D-03/D-05.
    - Test 4: A rule that leaves one NPC with no eligible preset returns that NPC in `BlockedNpcs`, sets `IsBlocked == true`, does not silently fall back to all presets, documents that eligible rows may have been mutated on the working project, and leaves the blocked row identifiable by `Mod`, `Name`, `EditorId`, `Race`, and `FormId` per D-06/D-07/D-09. This locks the caller contract that `result.Project` MUST NOT be consumed for generation when `IsBlocked` is true.
    - Test 5: `cloneBeforeReplay: true` returns a working project whose generated assignments differ from the source project while the original `ProjectModel` assignments, `AssignmentStrategy`, `IsDirty`, and `ChangeVersion` remain unchanged per D-05 and review feedback.
    - Test 6: `cloneBeforeReplay: false` mutates the source project assignments while preserving `AssignmentStrategy`, proving the no-clone contract explicitly instead of leaving it implicit.
    - Test 7: A saved seeded strategy has a pinned expected assignment sequence and repeated replay produces the same assignments, addressing review concern: seeded deterministic replay must not be composed with a plain random provider.
    - Test 8: A project with an `AssignmentStrategy` and zero `MorphedNpcs` returns `Replayed=true`, `AssignedCount=0`, and no blockers so empty automation inputs remain successful no-ops.
    - Test 9: A seeded strategy applied through `MorphAssignmentService.ApplyStrategy(project, strategy, eligibleRows)` assigns only `eligibleRows` and leaves non-eligible rows unchanged, addressing the HIGH review concern that seeded dispatch must not expand GUI/scoped bulk operations to the full project.
  </behavior>
  <action>Create `AssignmentStrategyReplayServiceTests.cs` with FluentAssertions tests that initially fail because `AssignmentStrategyReplayService` and `AssignmentStrategyReplayResult` do not exist and because seeded replay currently needs explicit automation coverage. Build fixtures with two or more presets and NPC rows whose preexisting assignments are intentionally stale, so the tests prove replay rather than existing assignment use. Include a pinned seeded-strategy fixture using the existing `AssignmentStrategyDefinition.Seed` path; do not introduce new strategy kinds or new RNG logic. Add the seeded `eligibleRows` regression test either in this new file or in `AssignmentStrategyServiceTests.cs`, but keep this plan's files_modified list unchanged by preferring the new file unless existing helper visibility makes that impossible. Addresses review concerns: seeded deterministic replay must be locked before CLI/bundle wiring inherits the helper, blocked results must expose `IsBlocked`, and seeded dispatch must preserve scoped eligible-row behavior.</action>
  <verify>
    <automated>dotnet test tests/BS2BG.Tests/BS2BG.Tests.csproj --filter "FullyQualifiedName~AssignmentStrategyReplayServiceTests|FullyQualifiedName~AssignmentStrategyServiceTests|FullyQualifiedName~MorphsViewModelStrategyTests"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BS2BG.Tests/AssignmentStrategyReplayServiceTests.cs` contains `BodyGen`, `All`, `BosJson`, `cloneBeforeReplay`, `BlockedNpcs`, `IsBlocked`, `Seed`, `eligibleRows`, `IsDirty`, `ChangeVersion`, and `AssignmentStrategyReplayService`.
    - The targeted test command fails before Task 2 only because the replay service/result production types are missing.
  </acceptance_criteria>
  <done>Replay helper behavior is specified before implementation and covers D-01, D-03, D-05, D-06, D-07, D-08, and D-09.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement Core replay seam</name>
  <files>src/BS2BG.Core/Automation/AssignmentStrategyReplayService.cs, src/BS2BG.Core/Automation/AssignmentStrategyReplayContracts.cs, src/BS2BG.Core/Morphs/MorphAssignmentService.cs, tests/BS2BG.Tests/AssignmentStrategyReplayServiceTests.cs</files>
  <read_first>src/BS2BG.Core/Automation/HeadlessGenerationContracts.cs, src/BS2BG.Core/Morphs/MorphAssignmentService.cs, src/BS2BG.Core/Morphs/AssignmentStrategyService.cs, src/BS2BG.Core/Models/ProjectModel.cs, tests/BS2BG.Tests/AssignmentStrategyReplayServiceTests.cs</read_first>
  <action>Implement `public sealed class AssignmentStrategyReplayService` under `BS2BG.Core.Automation` with a constructor dependency on `MorphAssignmentService`. Add XML doc comments. Add `public AssignmentStrategyReplayResult PrepareForBodyGen(ProjectModel sourceProject, OutputIntent intent, bool cloneBeforeReplay)` with this exact behavior: throw `ArgumentNullException` for null source; clone by `var workingProject = new ProjectModel(); workingProject.ReplaceWith(sourceProject);` when `cloneBeforeReplay` is true; otherwise use source directly; return no-op result when intent is `OutputIntent.BosJson` or `AssignmentStrategy` is null; otherwise call `morphAssignmentService.ApplyStrategy(workingProject, workingProject.AssignmentStrategy)` and return the working project, `Replayed=true`, strategy kind, assigned count, and blocked NPC list. Add `AssignmentStrategyReplayResult` as a sealed record in `src/BS2BG.Core/Automation/AssignmentStrategyReplayContracts.cs` with properties `Project`, `Replayed`, `StrategyKind`, `AssignedCount`, `BlockedNpcs`, and `IsBlocked`. XML docs on the result must state: when `IsBlocked` is true, `Project` may contain partial replay mutations and callers MUST NOT generate output from it. Update `MorphAssignmentService.ApplyStrategy` so saved seeded strategies honor the existing deterministic provider path without dropping `eligibleRows`: create `var service = strategy.Seed.HasValue ? new AssignmentStrategyService(new DeterministicAssignmentRandomProvider(strategy.Seed.Value)) : new AssignmentStrategyService(randomAssignmentProvider);` and then `return service.Apply(project, strategy, eligibleRows);`. Add this exact WHY comment near the branch: `Seeded strategies must use the deterministic provider directly so saved seeds reproduce identically across automation runs; unseeded calls keep the injected provider so tests can substitute their own draws.` Do not call the two-argument `AssignmentStrategyService.Apply(project, strategy)` overload from `MorphAssignmentService.ApplyStrategy`, because that drops scoped eligible rows and would regress WORK-03 GUI/bulk semantics. Do not edit byte-sensitive writers or add App/Avalonia references. Addresses review concerns: seeded deterministic replay, eligibleRows preservation, result-contract location, partial working-project mutation documentation, source dirty-state invariants.</action>
  <verify>
    <automated>dotnet test tests/BS2BG.Tests/BS2BG.Tests.csproj --filter "FullyQualifiedName~AssignmentStrategyReplayServiceTests|FullyQualifiedName~AssignmentStrategyServiceTests|FullyQualifiedName~MorphsViewModelStrategyTests"</automated>
  </verify>
  <acceptance_criteria>
    - `src/BS2BG.Core/Automation/AssignmentStrategyReplayService.cs` contains `PrepareForBodyGen`, `cloneBeforeReplay`, `new ProjectModel()`, `ReplaceWith(sourceProject)`, and `morphAssignmentService.ApplyStrategy`.
    - `src/BS2BG.Core/Automation/AssignmentStrategyReplayContracts.cs` contains `AssignmentStrategyReplayResult`.
    - `src/BS2BG.Core/Morphs/MorphAssignmentService.cs` contains `strategy.Seed.HasValue`, `new DeterministicAssignmentRandomProvider(strategy.Seed.Value)`, and `service.Apply(project, strategy, eligibleRows)`.
    - `src/BS2BG.Core/Morphs/MorphAssignmentService.cs` does not contain a new call to the two-argument `AssignmentStrategyService.Apply(project, strategy)` overload inside `ApplyStrategy`.
    - `AssignmentStrategyReplayResult` exposes `IsBlocked => BlockedNpcs.Count > 0` or equivalent.
    - Targeted replay tests pass.
  </acceptance_criteria>
  <done>Core-only replay seam exists, preserves provider abstraction, supports request-scoped working state, and exposes blocker/summary data for downstream automation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved `.jbs2bg` project → Core replay service | User-controlled strategy data and NPC/preset rows determine generated morph output. |
| replay working state → output services | Mutated working assignments feed byte-sensitive BodyGen writers in later plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | AssignmentStrategyReplayService | mitigate | Treat `BlockedNpcs.Count > 0` as `IsBlocked` so callers can stop before output writes instead of emitting stale/partial morphs. |
| T-07-02 | Tampering | AssignmentStrategyReplayService | mitigate | Call `MorphAssignmentService.ApplyStrategy` rather than direct RNG/assignment logic to preserve Phase 5 provider-compatible deterministic behavior. |
| T-07-03 | Information Disclosure | AssignmentStrategyReplayService | accept | The helper returns NPC identity already present in local project data; no external service or private path surface is introduced. |
</threat_model>

<verification>
Run the plan's targeted replay and shared-strategy regression tests, then run the phase targeted command after later waves: `dotnet test tests/BS2BG.Tests/BS2BG.Tests.csproj --filter "FullyQualifiedName~AssignmentStrategyReplayServiceTests|FullyQualifiedName~AssignmentStrategyServiceTests|FullyQualifiedName~MorphsViewModelStrategyTests|FullyQualifiedName~CliGenerationTests|FullyQualifiedName~PortableBundleServiceTests"`.
</verification>

<success_criteria>
`AssignmentStrategyReplayService` exists, direct tests pass, and all locked decisions D-01/D-03/D-05/D-06/D-07/D-08/D-09 are represented in helper behavior without changing output writer bytes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-01-SUMMARY.md`
</output>
