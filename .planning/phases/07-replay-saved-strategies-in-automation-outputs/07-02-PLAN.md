---
phase: 07-replay-saved-strategies-in-automation-outputs
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/BS2BG.Core/Automation/HeadlessGenerationService.cs
  - src/BS2BG.Cli/Program.cs
  - tests/BS2BG.Tests/CliGenerationTests.cs
autonomous: true
requirements: [AUTO-03]
must_haves:
  truths:
    - "D-01/D-02: CLI generate BodyGen/all automatically replays a saved AssignmentStrategy before writing morphs.ini."
    - "D-03: CLI generate with no saved strategy keeps current assignment behavior."
    - "D-06/D-07: CLI replay blockers return ValidationBlocked and create no output files."
    - "D-08/D-09: CLI/service results include concise success replay counts and actionable blocked-NPC details."
    - "Review: Seeded CLI replay is deterministic, validation uses replayed state for BodyGen/all after successful replay, success summaries reach stdout, and blocked OutputIntent.All writes no BodyGen or BoS files."
  artifacts:
    - path: "src/BS2BG.Core/Automation/HeadlessGenerationService.cs"
      provides: "Headless generation replay integration before validation/overwrite preflight/writer calls for BodyGen/all"
    - path: "tests/BS2BG.Tests/CliGenerationTests.cs"
      provides: "CLI/service regression coverage for replay, non-replay, blocked replay, and output text"
    - path: "src/BS2BG.Cli/Program.cs"
      provides: "CLI composition and stdout/stderr surfacing for replay summaries without App dependencies"
  key_links:
    - from: "HeadlessGenerationService.Run"
      to: "AssignmentStrategyReplayService.PrepareForBodyGen"
      via: "before validation/overwrite/output writes for BodyGen/all intents"
      pattern: "PrepareForBodyGen\(project, request.Intent"
    - from: "HeadlessGenerationService.Run"
      to: "MorphGenerationService.GenerateMorphs"
      via: "replayResult.Project"
      pattern: "GenerateMorphs\(.*replay.*Project"
---

<objective>
Wire saved strategy replay into CLI/headless BodyGen generation.

Purpose: CLI automation must be reproducible from saved project strategy data rather than depending on stale or pre-mutated NPC assignments.
Output: `HeadlessGenerationService` uses the replay seam before morph generation, and CLI tests prove success, no-strategy, BoS-only, and blocker behavior.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-CONTEXT.md
@.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-RESEARCH.md
@.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-01-SUMMARY.md
@src/BS2BG.Core/Automation/HeadlessGenerationService.cs
@src/BS2BG.Core/Automation/AssignmentStrategyReplayService.cs
@src/BS2BG.Cli/Program.cs
@tests/BS2BG.Tests/CliGenerationTests.cs

<interfaces>
From Plan 01 output:
```csharp
public sealed class AssignmentStrategyReplayService
{
    public AssignmentStrategyReplayResult PrepareForBodyGen(ProjectModel sourceProject, OutputIntent intent, bool cloneBeforeReplay);
}
public sealed record AssignmentStrategyReplayResult(ProjectModel Project, bool Replayed, AssignmentStrategyKind? StrategyKind, int AssignedCount, IReadOnlyList<AssignmentStrategyBlockedNpc> BlockedNpcs);
```

From current `HeadlessGenerationService.Run`:
```csharp
var requestProfileCatalog = profileCatalogComposer.BuildForProject(project);
var validationReport = ProjectValidationService.Validate(project, requestProfileCatalog);
...
var plannedTargets = PlanTargets(request, project).ToArray();
...
var morphs = morphGenerationService.GenerateMorphs(project).Text;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI replay regressions</name>
  <files>tests/BS2BG.Tests/CliGenerationTests.cs</files>
  <read_first>tests/BS2BG.Tests/CliGenerationTests.cs, tests/BS2BG.Tests/AssignmentStrategyServiceTests.cs, src/BS2BG.Core/Automation/HeadlessGenerationService.cs, src/BS2BG.Core/Automation/AssignmentStrategyReplayService.cs</read_first>
  <behavior>
    - Service-level test: a project saved with stale/wrong NPC assignments and a round-robin or seeded deterministic `AssignmentStrategy` produces `morphs.ini` from replayed assignments for `OutputIntent.BodyGen` per D-01/D-02.
    - Service-level test: a saved seeded strategy produces identical `morphs.ini` bytes across repeated `OutputIntent.BodyGen` runs and matches the pinned direct replay sequence from Plan 01, addressing seeded determinism review feedback.
    - Service-level test: `OutputIntent.All` with successful replay writes `templates.ini`, `morphs.ini`, and BoS JSON while `templates.ini` and BoS JSON bytes match the same project generated without replay, proving assignment replay does not alter non-assignment-sensitive outputs.
    - Service-level test: the same project with `OutputIntent.BosJson` does not replay and does not write `morphs.ini` per D-01.
    - Service-level test: a project with no saved strategy still writes `morphs.ini` from existing assignments per D-03.
    - Blocker test: a strategy whose race/rule filters leave one NPC with no eligible preset returns `AutomationExitCode.ValidationBlocked`, creates no output files for `OutputIntent.All` (`templates.ini`, `morphs.ini`, and BoS JSON all absent), includes blocked NPC details in `Message`, and `Message` does not contain `ProjectPath` or output directory strings per D-06/D-07/D-09.
    - Validation-order test: source project assignments contain a stale/invalid preset reference or stale strategy-derived assignment that existing validation would flag, saved strategy replay replaces it, and `OutputIntent.BodyGen` succeeds, proving validation and missing-profile checks that depend on assignments run against the successful replay project.
    - Gate-order test: successful replay followed by existing targets without overwrite returns `AutomationExitCode.OverwriteRefused`, proving replay happens before overwrite preflight and writes still do not occur.
    - Program.Main test: `generate --intent bodygen` prints a multi-line success message with `Assignment strategy replayed: {kind}; assigned NPCs: {assigned}; blocked NPCs: 0.` before/with `Generation completed successfully.` on stdout when replay succeeds, and maps blocked replay to exit code 2/stderr without stack traces per D-08/D-09.
  </behavior>
  <action>Add failing tests to `CliGenerationTests.cs`. Reuse existing `ConsoleCaptureCollection` for any `Program.Main` tests. Build fixture helpers in the test class or reuse existing helpers so the saved project's NPC assignments are intentionally stale and only the saved `AssignmentStrategy` can produce the expected `morphs.ini`. Assert output bytes/text contain replayed preset names and do not contain stale preset names. Assert blocked messages do not contain `ProjectPath` or output directory strings beyond existing overwrite/I/O handling. Add explicit byte-parity assertions for `templates.ini` and BoS JSON because the chosen invariant is: replayed state is consumed for assignment-dependent `morphs.ini`; non-assignment-sensitive template and BoS outputs must remain byte-equivalent to source-state generation. Add a validation-order fixture proving assignment-dependent validation succeeds after replay replaces stale assignments. Do not add a CLI flag; D-02 requires automatic replay from saved project data. Addresses review concerns: seeded replay, stdout visibility, `OutputIntent.All` blocked no-write behavior, overwrite ordering, template/BoS byte parity, validation project selection, and path-leak assertions.</action>
  <review_update>REVIEWS.md supersedes any conflicting wording in the action above. Use this explicit project-state invariant: for `OutputIntent.BodyGen` and `OutputIntent.All`, call replay before assignment-dependent validation and before writes; if `replayResult.IsBlocked`, stop before `PlanTargets`, overwrite preflight, directory creation, or any writer. Run assignment-dependent validation/missing-profile checks that inspect NPC/custom-target assignments against `replayResult.Project` after successful replay so stale source assignments do not block a replay-fixed run. Generate `morphs.ini` from `replayResult.Project`. Keep `PlanTargets` based on request intent/output paths. Treat templates.ini and BoS JSON as non-assignment-sensitive outputs: preserve byte-equivalence to source-state generation and prove it with tests rather than widening replay blast radius without evidence. Format success `HeadlessGenerationResult.Message` as replay summary first (`Assignment strategy replayed: {kind}; assigned NPCs: {assigned}; blocked NPCs: 0.`), then `Generation completed successfully.` If the saved strategy has no seed, append `(unseeded strategy; assignments may vary between runs)` to the replay summary. Blocked replay messages must include blocked NPC `Mod`, `Name`, `EditorId`, `Race`, `FormId`, and `Reason`, and must not include `request.ProjectPath` or output-directory strings. Addresses review concerns: validation project selection, exact success message semantics, no path leakage, unseeded saved-strategy disclosure, and limiting replay state to assignment-dependent output.</review_update>
  <verify>
    <automated>dotnet test tests/BS2BG.Tests/BS2BG.Tests.csproj --filter FullyQualifiedName~CliGenerationTests</automated>
  </verify>
  <acceptance_criteria>
    - `CliGenerationTests.cs` contains new tests mentioning `AssignmentStrategy`, `Seed`, `OutputIntent.BodyGen`, `OutputIntent.All`, `OutputIntent.BosJson`, `ValidationBlocked`, `OverwriteRefused`, `templates.ini`, BoS JSON output, `ProjectPath`, output directory, validation after replay, and `Stale` or equivalent stale-assignment fixture marker.
    - The targeted test command fails before Task 2 because headless generation has not yet called the replay service.
  </acceptance_criteria>
  <done>CLI/headless replay expectations are locked before production wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire replay before CLI output writes</name>
  <files>src/BS2BG.Core/Automation/HeadlessGenerationService.cs, src/BS2BG.Cli/Program.cs, tests/BS2BG.Tests/CliGenerationTests.cs</files>
  <read_first>src/BS2BG.Core/Automation/HeadlessGenerationService.cs, src/BS2BG.Core/Automation/AssignmentStrategyReplayService.cs, src/BS2BG.Core/Automation/HeadlessGenerationContracts.cs, src/BS2BG.Cli/Program.cs, tests/BS2BG.Tests/CliGenerationTests.cs</read_first>
  <action>Add `AssignmentStrategyReplayService replayService` as an explicit constructor dependency of `HeadlessGenerationService`; guard it with `ArgumentNullException` and store it in a readonly field. Update all call sites: `src/BS2BG.Cli/Program.cs` `CreateGenerationService()` must instantiate `new AssignmentStrategyReplayService(new MorphAssignmentService(new RandomAssignmentProvider()))`, and `tests/BS2BG.Tests/CliGenerationTests.cs` factories must pass the same service or a test-composed equivalent. In `Run`, call `PrepareForBodyGen(project, request.Intent, cloneBeforeReplay: true)` immediately after project load and request profile catalog construction, then run validation/missing-profile checks/overwrite preflight/output planning against the correct project state: use `replayResult.Project` for BodyGen validation and BodyGen `PlanTargets`/template/morph generation; keep BoS JSON using the same `requestProfileCatalog` and byte-equivalent slider presets, with tests proving `templates.ini` parity. If `replayResult.IsBlocked`, return `HeadlessGenerationResult(AutomationExitCode.ValidationBlocked, message, Array.Empty<string>(), validationReport)` before `PlanTargets`, overwrite preflight, directory creation, or any writer call so `OutputIntent.All` creates no BodyGen or BoS files. Append success message text such as `Assignment strategy replayed: {kind}; assigned NPCs: {assigned}; blocked NPCs: 0.` when `replayResult.Replayed` is true, without per-NPC success listings. Update `Program.WriteResult` (or the existing success output path in `Program.cs`) so a non-empty success `HeadlessGenerationResult.Message` containing `Assignment strategy replayed` is printed to stdout before/with written file paths; keep blocked replay on stderr with exit code 2 and no stack trace. Failure message must include blocked NPC `Mod`, `Name`, `EditorId`, `Race`, `FormId`, and `Reason`, and must not include `request.ProjectPath` or output-directory strings. Preserve existing missing-profile, overwrite, and I/O exit-code behavior. Addresses review concerns: constructor ripple effects, stdout summary surfacing, seeded determinism via Plan 01, validation/replay ordering, and blocked `OutputIntent.All` no-write semantics.</action>
  <verify>
    <automated>dotnet test tests/BS2BG.Tests/BS2BG.Tests.csproj --filter FullyQualifiedName~CliGenerationTests</automated>
  </verify>
  <acceptance_criteria>
    - `HeadlessGenerationService.cs` constructor contains `AssignmentStrategyReplayService replayService` and `PrepareForBodyGen(project, request.Intent, cloneBeforeReplay: true)`.
    - `src/BS2BG.Cli/Program.cs` contains `new AssignmentStrategyReplayService(new MorphAssignmentService(new RandomAssignmentProvider()))` or equivalent explicit Core-only composition.
    - `HeadlessGenerationService.cs` returns `AutomationExitCode.ValidationBlocked` when `replayResult.IsBlocked` before `bodyGenIniExportWriter.Write`, `bosJsonExportWriter.Write`, or output directory creation for the request.
    - Successful replay messages contain `Assignment strategy replayed`, `assigned NPCs`, `blocked NPCs: 0`, and `Generation completed successfully.`; failure messages contain blocked NPC identity fields and do not contain `ProjectPath` or output-directory values.
    - `CliGenerationTests.cs` includes assertions proving assignment-dependent validation uses post-replay state and templates/BoS JSON bytes remain equivalent to source-state generation.
    - Targeted CLI tests pass.
  </acceptance_criteria>
  <done>CLI/headless generation replays saved strategies for BodyGen/all, blocks bad replay before writes, and remains script-friendly through stable exit codes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved project file → headless generation | User-controlled saved project strategy decides generated output. |
| headless service → filesystem | Generated files are written to caller-selected output directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | HeadlessGenerationService | mitigate | Return ValidationBlocked before `PlanTargets`/writer calls when replay has blocked rows, preventing stale `morphs.ini`. |
| T-07-03 | Denial of Service | CLI output | accept | Additional replay work is bounded by existing NPC/preset collections and targeted tests cover normal datasets; no external calls added. |
| T-07-05 | Information Disclosure | CLI blocked replay message | mitigate | Include only project NPC identity/rule reason needed to fix configuration; do not include private file paths beyond existing output error handling. |
</threat_model>

<verification>
Run `dotnet test tests/BS2BG.Tests/BS2BG.Tests.csproj --filter FullyQualifiedName~CliGenerationTests`, then after Plan 03 run the combined phase command and `dotnet test`.
</verification>

<success_criteria>
CLI `generate --intent bodygen` and `--intent all` replay saved strategies automatically; `--intent bos` does not; blocked replay returns exit code 2 without output files; success and failure messages satisfy D-08/D-09.
</success_criteria>

<output>
After completion, create `.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-02-SUMMARY.md`
</output>
