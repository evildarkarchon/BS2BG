---
phase: 07-replay-saved-strategies-in-automation-outputs
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/BS2BG.Core/Bundling/PortableProjectBundleService.cs
  - src/BS2BG.Core/Bundling/PortableProjectBundleContracts.cs
  - tests/BS2BG.Tests/PortableBundleServiceTests.cs
autonomous: true
requirements: [AUTO-02, AUTO-03]
must_haves:
  truths:
    - "D-01/D-02: Portable bundle BodyGen/all output automatically replays saved AssignmentStrategy before adding morphs.ini."
    - "D-04/D-05: `project/project.jbs2bg` in the zip remains the original source project state while generated morph output uses request-scoped replay state."
    - "D-06/D-07: Blocked bundle replay fails before zip creation and does not emit stale prior assignments."
    - "D-08/D-09: Bundle preview/report surfaces concise success replay counts and actionable blocked-NPC details."
  artifacts:
    - path: "src/BS2BG.Core/Bundling/PortableProjectBundleService.cs"
      provides: "Bundle replay integration with cloned working project for generated BodyGen output"
    - path: "tests/BS2BG.Tests/PortableBundleServiceTests.cs"
      provides: "Bundle regression coverage for replay output, source project preservation, and blocked replay"
  key_links:
    - from: "PortableProjectBundleService.BuildPlan"
      to: "AssignmentStrategyReplayService.PrepareForBodyGen"
      via: "request-scoped cloned project before generated BodyGen entries"
      pattern: "PrepareForBodyGen\(request.Project, request.Intent, cloneBeforeReplay: true\)"
    - from: "project/project.jbs2bg"
      to: "ProjectFileService.SaveToString(request.Project, request.SaveContext)"
      via: "serialize original request project, not replay result"
      pattern: "SaveToString\(request.Project"
---

<objective>
Wire saved strategy replay into portable bundle generated BodyGen output while preserving the bundled project source state.

Purpose: Portable bundles must be reproducible automation artifacts without rewriting shared project data or leaking replay side effects into `project/project.jbs2bg`.
Output: `PortableProjectBundleService` generates `outputs/bodygen/morphs.ini` from replayed request-scoped state, reports replay status, and blocks bad replay before zip creation.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-CONTEXT.md
@.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-RESEARCH.md
@.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-01-SUMMARY.md
@.planning/phases/06-compose-custom-profiles-in-headless-generation/06-LEARNINGS.md
@src/BS2BG.Core/Bundling/PortableProjectBundleService.cs
@src/BS2BG.Core/Bundling/PortableProjectBundleContracts.cs
@src/BS2BG.Core/Automation/AssignmentStrategyReplayService.cs
@tests/BS2BG.Tests/PortableBundleServiceTests.cs

<interfaces>
From `PortableProjectBundleService.BuildPlan`:
```csharp
var requestProfileCatalog = profileCatalogComposer.BuildForProject(request.Project, request.SaveContext);
var validationReport = ProjectValidationService.Validate(request.Project, requestProfileCatalog);
Entry("project/project.jbs2bg", "project", Utf8NoBom.GetBytes(projectFileService.SaveToString(request.Project, request.SaveContext)));
AddGeneratedOutputEntries(entries, request, stagingDirectory, requestProfileCatalog);
```

From `PortableProjectBundleContracts.cs`:
```csharp
public enum PortableProjectBundleOutcome { Success, ValidationBlocked, OverwriteRefused, MissingProfile, IoFailure }
public sealed record PortableProjectBundlePreview(... ProjectValidationReport ValidationReport, IReadOnlyList<string> PrivacyFindings);
public sealed record PortableProjectBundleResult(... ProjectValidationReport ValidationReport, IReadOnlyList<string> PrivacyFindings);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bundle replay regressions</name>
  <files>tests/BS2BG.Tests/PortableBundleServiceTests.cs</files>
  <read_first>tests/BS2BG.Tests/PortableBundleServiceTests.cs, tests/BS2BG.Tests/CliGenerationTests.cs, src/BS2BG.Core/Bundling/PortableProjectBundleService.cs, src/BS2BG.Core/Automation/AssignmentStrategyReplayService.cs</read_first>
  <behavior>
    - Bundle BodyGen/all success test: source project has stale NPC assignment plus saved deterministic strategy; `outputs/bodygen/morphs.ini` contains replayed preset names and not stale preset names per D-01/D-02.
    - Project entry preservation test: `project/project.jbs2bg` still contains the original stale/source assignment and the saved `AssignmentStrategy`, proving D-04/D-05.
    - BoS-only test: `OutputIntent.BosJson` does not replay or mutate request project and has no BodyGen entries per D-01.
    - Blocked replay test: strategy rule leaves an NPC with no eligible preset; `Create` returns `PortableProjectBundleOutcome.ValidationBlocked`, no zip exists, and report/preview text or privacy findings contain blocked NPC details per D-06/D-07/D-09.
    - Preview test: `Preview` reports the same replay success/blocker outcome without writing the zip per existing preview semantics and D-08/D-09.
  </behavior>
  <action>Add failing tests to `PortableBundleServiceTests.cs`. Use `ZipFile.OpenRead` and existing `ReadEntryText/ReadEntryBytes` helpers. Ensure fixtures distinguish the project entry from generated output: the serialized project entry should retain the stale/source assignment, while `outputs/bodygen/morphs.ini` should use replayed assignments. Do not change expected golden fixtures; derive expected output through existing generation/writer services where bytes are asserted.</action>
  <verify>
    <automated>dotnet test tests/BS2BG.Tests/BS2BG.Tests.csproj --filter FullyQualifiedName~PortableBundleServiceTests</automated>
  </verify>
  <acceptance_criteria>
    - `PortableBundleServiceTests.cs` contains new tests mentioning `AssignmentStrategy`, `outputs/bodygen/morphs.ini`, `project/project.jbs2bg`, `ValidationBlocked`, and stale/source assignment preservation.
    - The targeted test command fails before Task 2 because bundle generation still uses `request.Project` assignments for BodyGen output.
  </acceptance_criteria>
  <done>Bundle replay and source-state preservation expectations are locked before production wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate bundle BodyGen entries from replay working state</name>
  <files>src/BS2BG.Core/Bundling/PortableProjectBundleService.cs, src/BS2BG.Core/Bundling/PortableProjectBundleContracts.cs, tests/BS2BG.Tests/PortableBundleServiceTests.cs</files>
  <read_first>src/BS2BG.Core/Bundling/PortableProjectBundleService.cs, src/BS2BG.Core/Bundling/PortableProjectBundleContracts.cs, src/BS2BG.Core/Automation/AssignmentStrategyReplayService.cs, tests/BS2BG.Tests/PortableBundleServiceTests.cs</read_first>
  <action>Compose `AssignmentStrategyReplayService` in `PortableProjectBundleService` using `MorphAssignmentService(new RandomAssignmentProvider())` or an equivalent Core-only service field. In `BuildPlan`, after validation/missing-profile checks and before creating entries, call `PrepareForBodyGen(request.Project, request.Intent, cloneBeforeReplay: true)`. If `replayResult.IsBlocked`, return a `ValidationBlocked` blocked plan before staging output entries or creating a zip. Keep `Entry("project/project.jbs2bg", ..., SaveToString(request.Project, request.SaveContext))` unchanged so the bundled project file remains source state per D-04/D-05. Change `AddGeneratedOutputEntries` to accept/use `replayResult.Project` for BodyGen template/morph generation but continue to use the original request project for BoS JSON if appropriate; template generation can use the replay working project's `SliderPresets` because cloning preserves slider data. Add replay summary/blocker text to report/preview-facing text: success should include strategy kind, assigned NPC count, and blocked NPCs 0; failure should include blocked NPC Mod, Name, EditorId, Race, FormId, and reason. Preserve path scrubbing and no-private-path behavior by passing report text through existing scrub/privacy paths.</action>
  <verify>
    <automated>dotnet test tests/BS2BG.Tests/BS2BG.Tests.csproj --filter FullyQualifiedName~PortableBundleServiceTests</automated>
  </verify>
  <acceptance_criteria>
    - `PortableProjectBundleService.cs` contains `PrepareForBodyGen(request.Project, request.Intent, cloneBeforeReplay: true)`.
    - `PortableProjectBundleService.cs` still contains `SaveToString(request.Project, request.SaveContext)` for `project/project.jbs2bg`.
    - Blocked replay returns `PortableProjectBundleOutcome.ValidationBlocked` before `ZipArchive` creation and before BodyGen entry files are added.
    - Targeted bundle tests pass.
  </acceptance_criteria>
  <done>Portable bundle BodyGen output replays saved strategies from request-scoped state while the project entry remains the saved/source project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory/shared project → bundle zip | Bundle service serializes project data and generated outputs into a shareable archive. |
| bundle report/manifest → recipient | Bundle metadata and reports may be shared outside the developer's machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | PortableProjectBundleService BodyGen output | mitigate | Use replay working project for generated BodyGen entries and block before zip creation when replay has blocked rows. |
| T-07-04 | Information Disclosure | Bundle reports/manifests | mitigate | Preserve existing `BundlePathScrubber` and privacy-findings path so replay details do not introduce private local paths. |
| T-07-06 | Repudiation | Bundle project entry vs generated output | mitigate | Keep `project/project.jbs2bg` as source state and add concise replay summary to report text so generated output provenance is visible. |
</threat_model>

<verification>
Run `dotnet test tests/BS2BG.Tests/BS2BG.Tests.csproj --filter FullyQualifiedName~PortableBundleServiceTests`, then run the combined targeted command and `dotnet test` after Wave 2.
</verification>

<success_criteria>
Portable bundle BodyGen/all output replays saved strategy data; BoS-only does not replay; blocked replay creates no zip; `project/project.jbs2bg` remains the original source state with strategy configuration intact.
</success_criteria>

<output>
After completion, create `.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-03-SUMMARY.md`
</output>
