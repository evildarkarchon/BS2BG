---
phase: 07-replay-saved-strategies-in-automation-outputs
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/BS2BG.Core/Bundling/PortableProjectBundleService.cs
  - src/BS2BG.Core/Bundling/PortableProjectBundleContracts.cs
  - src/BS2BG.Cli/Program.cs
  - src/BS2BG.App/AppBootstrapper.cs
  - src/BS2BG.App/ViewModels/MainWindowViewModel.cs
  - tests/BS2BG.Tests/PortableBundleServiceTests.cs
autonomous: true
requirements: [AUTO-02, AUTO-03]
must_haves:
  truths:
    - "D-01/D-02: Portable bundle BodyGen/all output automatically replays saved AssignmentStrategy before adding morphs.ini."
    - "D-04/D-05: `project/project.jbs2bg` in the zip remains the original source project state while generated morph output uses request-scoped replay state."
    - "D-06/D-07: Blocked bundle replay fails before zip creation and does not emit stale prior assignments."
    - "D-08/D-09: Bundle preview/report surfaces concise success replay counts and actionable blocked-NPC details."
    - "Review: Bundle morph output uses replay working state while non-assignment-sensitive outputs preserve source-state byte parity; replay summary/report surfaces are explicit, manifest-covered, privacy-scrubbed contracts."
  artifacts:
    - path: "src/BS2BG.Core/Bundling/PortableProjectBundleService.cs"
      provides: "Bundle replay integration with cloned working project for generated BodyGen output"
    - path: "tests/BS2BG.Tests/PortableBundleServiceTests.cs"
      provides: "Bundle regression coverage for replay output, source project preservation, and blocked replay"
    - path: "src/BS2BG.Cli/Program.cs"
      provides: "Bundle CLI composition updated for replay service constructor dependency"
    - path: "src/BS2BG.App/AppBootstrapper.cs"
      provides: "GUI DI composition updated for replay-aware bundle service"
    - path: "src/BS2BG.App/ViewModels/MainWindowViewModel.cs"
      provides: "Design-time/fallback bundle service construction updated for replay dependency only if it directly constructs PortableProjectBundleService"
  key_links:
    - from: "PortableProjectBundleService.BuildPlan"
      to: "AssignmentStrategyReplayService.PrepareForBodyGen"
      via: "request-scoped cloned project before generated BodyGen entries"
      pattern: "PrepareForBodyGen\(request.Project, request.Intent, cloneBeforeReplay: true\)"
    - from: "project/project.jbs2bg"
      to: "ProjectFileService.SaveToString(request.Project, request.SaveContext)"
      via: "serialize original request project, not replay result"
      pattern: "SaveToString\(request.Project"
---

<objective>
Wire saved strategy replay into portable bundle generated BodyGen output while preserving the bundled project source state.

Purpose: Portable bundles must be reproducible automation artifacts without rewriting shared project data or leaking replay side effects into `project/project.jbs2bg`.
Output: `PortableProjectBundleService` generates `outputs/bodygen/morphs.ini` from replayed request-scoped state, reports replay status, and blocks bad replay before zip creation.
</objective>

<execution_context>
@C:/Users/evild/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/evild/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-CONTEXT.md
@.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-RESEARCH.md
@.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-01-SUMMARY.md
@.planning/phases/06-compose-custom-profiles-in-headless-generation/06-LEARNINGS.md
@src/BS2BG.Core/Bundling/PortableProjectBundleService.cs
@src/BS2BG.Core/Bundling/PortableProjectBundleContracts.cs
@src/BS2BG.Core/Automation/AssignmentStrategyReplayService.cs
@src/BS2BG.Cli/Program.cs
@src/BS2BG.App/AppBootstrapper.cs
@src/BS2BG.App/ViewModels/MainWindowViewModel.cs
@tests/BS2BG.Tests/PortableBundleServiceTests.cs

  <dependency_note>Plan 03 intentionally remains Wave 3 and depends on 07-02 because it touches `src/BS2BG.Cli/Program.cs` and must avoid same-wave file conflicts with CLI replay composition. The bundle service logic itself depends only on the 07-01 replay seam.</dependency_note>

  <interfaces>
From `PortableProjectBundleService.BuildPlan`:
```csharp
var requestProfileCatalog = profileCatalogComposer.BuildForProject(request.Project, request.SaveContext);
var validationReport = ProjectValidationService.Validate(request.Project, requestProfileCatalog);
Entry("project/project.jbs2bg", "project", Utf8NoBom.GetBytes(projectFileService.SaveToString(request.Project, request.SaveContext)));
AddGeneratedOutputEntries(entries, request, stagingDirectory, requestProfileCatalog);
```

From `PortableProjectBundleContracts.cs`:
```csharp
public enum PortableProjectBundleOutcome { Success, ValidationBlocked, OverwriteRefused, MissingProfile, IoFailure }
public sealed record PortableProjectBundlePreview(... ProjectValidationReport ValidationReport, IReadOnlyList<string> PrivacyFindings);
public sealed record PortableProjectBundleResult(... ProjectValidationReport ValidationReport, IReadOnlyList<string> PrivacyFindings);
```

From Plan 02 output:
```csharp
public sealed class HeadlessGenerationService(..., AssignmentStrategyReplayService replayService);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bundle replay regressions</name>
  <files>tests/BS2BG.Tests/PortableBundleServiceTests.cs</files>
  <read_first>tests/BS2BG.Tests/PortableBundleServiceTests.cs, tests/BS2BG.Tests/CliGenerationTests.cs, src/BS2BG.Core/Bundling/PortableProjectBundleService.cs, src/BS2BG.Core/Automation/AssignmentStrategyReplayService.cs</read_first>
  <behavior>
    - Bundle BodyGen/all success test: source project has stale NPC assignment plus saved deterministic strategy; `outputs/bodygen/morphs.ini` contains replayed preset names and not stale preset names per D-01/D-02.
    - Seeded repeatability test: repeated bundle creation from the same saved seeded strategy produces identical `outputs/bodygen/morphs.ini` bytes, matching the CLI output bytes from Plan 02 for the same fixture project.
    - Non-assignment output parity test: bundle `templates.ini` and BoS JSON bytes remain byte-equivalent to source-state generation while only `outputs/bodygen/morphs.ini` reflects replayed assignments.
    - Project entry preservation test: parse `project/project.jbs2bg` back through `ProjectFileService.Load` and assert it still contains the original valid stale/source assignment and saved `AssignmentStrategy`, proving D-04/D-05 without relying only on raw JSON string contains.
    - Caller mutation test: after `Create(request)` returns, the caller's `request.Project.MorphedNpcs` assignments, `AssignmentStrategy`, `IsDirty`, and `ChangeVersion` match their pre-call snapshot per D-05.
    - BoS-only test: `OutputIntent.BosJson` does not replay or mutate request project and has no BodyGen entries per D-01.
    - Blocked replay test: strategy rule leaves an NPC with no eligible preset; `Create` returns `PortableProjectBundleOutcome.ValidationBlocked`, no zip exists, the explicit replay report surface contains blocked NPC details, and replay text does not contain source project or destination bundle paths per D-06/D-07/D-09.
    - Preview test: `Preview` reports the same replay success/blocker/no-strategy outcome through the explicit replay report surface without writing the destination zip; temporary staging files may be created and must be cleaned up by existing `finally` cleanup per existing preview semantics and D-03/D-08/D-09.
    - Manifest/report test: successful replay adds `reports/replay.txt`, and the bundle manifest lists that entry with a valid SHA-256 checksum and a report-style kind/value matching existing manifest conventions.
  </behavior>
  <action>Add failing tests to `PortableBundleServiceTests.cs`. Use `ZipFile.OpenRead` and existing `ReadEntryText/ReadEntryBytes` helpers. Ensure fixtures distinguish the project entry from generated output: the serialized project entry should retain a valid stale/source assignment, while `outputs/bodygen/morphs.ini` should use replayed assignments. Add a cross-automation parity assertion that the same saved fixture's CLI-generated `morphs.ini` bytes from Plan 02 equal the bundle `outputs/bodygen/morphs.ini` bytes. Test the explicit replay-report contract selected in Task 2 (`ReplayReportText`) for preview, create, and no-strategy behavior rather than overloading `PrivacyFindings`. Assert successful replay adds `reports/replay.txt` and that the manifest includes the entry and checksum; assert no saved strategy omits `reports/replay.txt` while `ReplayReportText` says `No saved assignment strategy; generated from existing project assignments.` Do not change expected golden fixtures; derive expected output through existing generation/writer services where bytes are asserted. Addresses review concerns: seeded bundle repeatability, project-entry semantic preservation, caller mutation, explicit reporting surface, manifest/checksum coverage, no-strategy behavior, privacy scrubbing, non-assignment output parity, and CLI/bundle byte parity.</action>
  <review_update>REVIEWS.md supersedes any conflicting wording in the action above. Use this explicit bundle contract: add `ReplayReportText` to `PortableProjectBundlePreview` and `PortableProjectBundleResult` as non-positional `init` properties (`public string ReplayReportText { get; init; } = string.Empty;`) so existing positional record constructor call sites do not break unnecessarily. In `BuildPlan`, build the request-scoped catalog from `request.Project`, call `PrepareForBodyGen(request.Project, request.Intent, cloneBeforeReplay: true)` before assignment-dependent validation and before generated output entries, and if `replayResult.IsBlocked`, return `PortableProjectBundleOutcome.ValidationBlocked` before `project/project.jbs2bg`, staging output entries, manifest/checksum entries, zip archive creation, or destination commit. Preserve `SaveToString(request.Project, request.SaveContext)` for `project/project.jbs2bg` per D-04/D-05. Use `replayResult.Project` only for assignment-dependent `outputs/bodygen/morphs.ini`; keep templates.ini and BoS JSON byte-equivalent to source-state generation and prove this with tests. Add `reports/replay.txt` only when `replayResult.Replayed == true`; include it in the manifest/checksum entry list using existing manifest conventions for report entries. When no saved strategy exists, do not add `reports/replay.txt`; set `ReplayReportText` to `No saved assignment strategy; generated from existing project assignments.` for preview/result visibility per D-03. Route `ReplayReportText` through existing privacy scrubbing/finding paths and test that blocked details do not leak source project or bundle destination paths. Update `AppBootstrapper` DI composition for the new dependency; touch `MainWindowViewModel` only if it directly constructs `PortableProjectBundleService` for design-time/fallback paths. Addresses review concerns: non-positional contract additions, validation/replay ordering, manifest inclusion, no-strategy behavior, privacy testing, and limiting replay state to morph output.</review_update>
  <verify>
    <automated>dotnet test tests/BS2BG.Tests/BS2BG.Tests.csproj --filter FullyQualifiedName~PortableBundleServiceTests</automated>
  </verify>
  <acceptance_criteria>
    - `PortableBundleServiceTests.cs` contains new tests mentioning `AssignmentStrategy`, `Seed`, `outputs/bodygen/morphs.ini`, `reports/replay.txt`, manifest checksum validation, `project/project.jbs2bg`, `ValidationBlocked`, `ReplayReportText`, `ChangeVersion`, no saved assignment strategy behavior, path leak assertions, and stale/source assignment preservation.
    - The targeted test command fails before Task 2 because bundle generation still uses `request.Project` assignments for BodyGen output.
  </acceptance_criteria>
  <done>Bundle replay and source-state preservation expectations are locked before production wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate bundle BodyGen entries from replay working state</name>
  <files>src/BS2BG.Core/Bundling/PortableProjectBundleService.cs, src/BS2BG.Core/Bundling/PortableProjectBundleContracts.cs, src/BS2BG.Cli/Program.cs, src/BS2BG.App/AppBootstrapper.cs, src/BS2BG.App/ViewModels/MainWindowViewModel.cs, tests/BS2BG.Tests/PortableBundleServiceTests.cs</files>
  <read_first>src/BS2BG.Core/Bundling/PortableProjectBundleService.cs, src/BS2BG.Core/Bundling/PortableProjectBundleContracts.cs, src/BS2BG.Core/Automation/AssignmentStrategyReplayService.cs, src/BS2BG.Cli/Program.cs, src/BS2BG.App/AppBootstrapper.cs, src/BS2BG.App/ViewModels/MainWindowViewModel.cs, tests/BS2BG.Tests/PortableBundleServiceTests.cs</read_first>
  <action>Add `AssignmentStrategyReplayService replayService` as an explicit constructor dependency of both the public and internal `PortableProjectBundleService` constructors; guard/store it like existing services. Update every constructor call site identified in review: `src/BS2BG.Cli/Program.cs` `CreateBundleServiceAndRequest`, `src/BS2BG.App/AppBootstrapper.cs` singleton registration, `src/BS2BG.App/ViewModels/MainWindowViewModel.cs` fallback construction, and test factories `CreateService`, `CreateBundledOnlyService`, and `CreateServiceWithCommitter` in `PortableBundleServiceTests.cs`. Compose the service as `new AssignmentStrategyReplayService(new MorphAssignmentService(new RandomAssignmentProvider()))` or via DI-resolved `MorphAssignmentService`, relying on Plan 01's seeded-strategy fix. Extend `PortableProjectBundlePreview` and `PortableProjectBundleResult` with a concrete `string ReplayReportText` property. In `BuildPlan`, after request-scoped catalog composition but before validation/missing-profile checks that could be affected by stale assignments, call `PrepareForBodyGen(request.Project, request.Intent, cloneBeforeReplay: true)`. If `replayResult.IsBlocked`, return a `ValidationBlocked` plan before creating `project/project.jbs2bg`, staging output entries, manifest/checksum entries, or the destination zip. Keep `Entry("project/project.jbs2bg", ..., SaveToString(request.Project, request.SaveContext))` unchanged so the bundled project file remains source state per D-04/D-05. Change `AddGeneratedOutputEntries` to accept a `ProjectModel outputProject` and use `replayResult.Project` consistently for BodyGen templates, BodyGen morphs, and BoS JSON generated output entries; this eliminates the reviewed dual-project footgun while preserving `project/project.jbs2bg` from the original request project. Add a bundle entry `reports/replay.txt` on successful replay containing exactly the concise summary form `Assignment strategy replayed: {kind}; assigned NPCs: {assigned}; blocked NPCs: 0.`; set `ReplayReportText` to the same scrubbed text for success. For blocked replay, set `ReplayReportText` to scrubbed details containing blocked NPC `Mod`, `Name`, `EditorId`, `Race`, `FormId`, and reason; do not create a zip. Include `ReplayReportText` in privacy scanning with existing `BundlePathScrubber.Scrub`/`FindPrivacyFindings`, and do not use `PrivacyFindings` as the user-visible replay report. Addresses review concerns: constructor ripple effects, explicit replay summary surface, dual-project generation, seeded determinism, blocked details visibility, and path scrubbing.</action>
  <verify>
    <automated>dotnet test tests/BS2BG.Tests/BS2BG.Tests.csproj --filter FullyQualifiedName~PortableBundleServiceTests</automated>
  </verify>
  <acceptance_criteria>
    - `PortableProjectBundleService.cs` constructors contain `AssignmentStrategyReplayService replayService` and `PrepareForBodyGen(request.Project, request.Intent, cloneBeforeReplay: true)`.
    - `PortableProjectBundleContracts.cs` contains non-positional init property `ReplayReportText` on both `PortableProjectBundlePreview` and `PortableProjectBundleResult`.
    - `PortableProjectBundleService.cs` still contains `SaveToString(request.Project, request.SaveContext)` for `project/project.jbs2bg`.
    - `PortableProjectBundleService.cs` contains `reports/replay.txt`, adds it to the manifest/checksum list only when replay occurs, and omits it when no saved strategy exists while setting `ReplayReportText` to the no-strategy message.
    - `PortableProjectBundleService.cs` passes `replayResult.Project` or an `outputProject` parameter to `MorphGenerationService.GenerateMorphs` for `morphs.ini` only; templates and BoS JSON generation remain byte-equivalent to source-state generation.
    - Blocked replay returns `PortableProjectBundleOutcome.ValidationBlocked` before `ZipArchive` creation and before generated output entry files are added.
    - Targeted bundle tests pass.
  </acceptance_criteria>
  <done>Portable bundle BodyGen output replays saved strategies from request-scoped state while the project entry remains the saved/source project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory/shared project → bundle zip | Bundle service serializes project data and generated outputs into a shareable archive. |
| bundle report/manifest → recipient | Bundle metadata and reports may be shared outside the developer's machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | PortableProjectBundleService BodyGen output | mitigate | Use replay working project for generated BodyGen entries and block before zip creation when replay has blocked rows. |
| T-07-04 | Information Disclosure | Bundle reports/manifests | mitigate | Preserve existing `BundlePathScrubber` and privacy-findings path so replay details do not introduce private local paths. |
| T-07-06 | Repudiation | Bundle project entry vs generated output | mitigate | Keep `project/project.jbs2bg` as source state and add concise replay summary to report text so generated output provenance is visible. |
</threat_model>

<verification>
Run `dotnet test tests/BS2BG.Tests/BS2BG.Tests.csproj --filter FullyQualifiedName~PortableBundleServiceTests`, then run the combined targeted command and `dotnet test` after Wave 2.
</verification>

<success_criteria>
Portable bundle BodyGen/all output replays saved strategy data; BoS-only does not replay; blocked replay creates no zip; `project/project.jbs2bg` remains the original source state with strategy configuration intact.
</success_criteria>

<output>
After completion, create `.planning/phases/07-replay-saved-strategies-in-automation-outputs/07-03-SUMMARY.md`
</output>
